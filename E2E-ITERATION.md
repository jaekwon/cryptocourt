# E2E / scenario work — state file

## Owner directive (verbatim, 2026-08-18)

> ok, how about first you implement the test clock system, audit it, then give it
> to the other session and tell it to use it to implement more integration tests,
> and meanwhile you design the declarative scenario file DSL, audit that until
> convergence, and then use it to populate the data as DEMO is, and once you're
> done with that, figure out how to make it easy to serve that either with gnodev
> or a test throwaway node initialized with the scenario file, connected to the
> website? set a loop and start working on this

Earlier rulings that bind this work (owner, same session):

- The clock override is a REALM entrypoint (there are no dev-only builds here),
  armed by a one-shot latch: `init()` captures the deployer, enabling requires
  that address AND a still-virgin realm, and any real use closes it forever.
  "otherwise is off forever."
- Published deadlines already gate on wall-clock time (clock.gno, round 94), so
  advancing the test clock is enough to walk a claim through its lifecycle.
- Accounting clocks stay on height and must not be faked.

## Phases

| # | phase | state |
|---|---|---|
| P1 | test clock system in the realm (latch + advance + a visible "test clock is on" read) | DONE (r1) |
| P2 | audit P1 with a fresh adversarial critic; apply findings | DONE (r3) |
| P3 | hand the clock to the other session (HANDOFF doc + BRANCHING.md coordination note) for integration tests | DONE (r2) |
| P4 | design the declarative scenario DSL; converge with lens agents until stable | CONVERGED (r5) |
| P5 | implement the runner; reproduce today's DEMO dataset FROM a scenario file | RUNNER + BOTH EMITTERS GREEN (r6, r7); the DEMO reproduction (O6 dump side) is the one piece still open |
| P6 | serve it: gnodev or a throwaway node seeded from the scenario, with the website pointed at it in live mode | DONE (r8) — `scripts/seed-node.sh`, ~25s cold to serving |

## Process per phase

Converge (lens agents) before implementing anything with design freedom; implement;
gate with `REQUIRE_GNO=1 make check` + `make isolation-test` + `make txtar-test`;
audit with a fresh critic; log the round here with evidence. Never git-commit
(BRANCHING.md: the human owns commits). Realm conventions per BRANCHING.md.

## Round log

| round | date | what | outcome |
|---|---|---|---|
| 1 | 2026-08-18 | P1 SHIPPED + P3 HANDED OFF, P2/P4 launched. The clock: `init()` captures the deployer via unsafe.OriginCaller() (meta.gno's own precedent); `EnableTestClock` demands that address on a virgin realm (CourtCount()<=1, latch untouched); the first `startCourtUser` — or an explicit `SealTestClock` — seals it ONE-WAY and zeroes the skew; `AdvanceTestClock` is forward-only; `TestClockActive`/`TestClockSkew` are public so no surface can show fabricated dates silently; `clock.gno`'s `nowTime()` now routes through the shim, so every published deadline follows it while accounting clocks stay on height. DESIGN CHANGE FOUND BY A REAL TEST, not review: the OTHER SESSION is already working in this shared tree and had written `gnoland/testdata/kourtv2_testclock.txtar` (untracked, 14:45) whose boundary assertion — advance to one second short of the dead-claim timeout, expect refusal — FAILED, because a skew added to a live clock cannot express a boundary: the seconds a node spends mining the intervening blocks are added underneath. The clock is now FROZEN while armed (now = base captured at arming + advanced), so nothing moves unless the scenario moves it; their txtar passes unchanged. Two of my own tests also had to be reshaped: the in-package suite shares one realm and an earlier test always creates a court, so the VIRGIN arming path is unreachable there — it lives in `z_testclock_filetest.gno` instead (which also proves a REFUSED arming allocates nothing, budgeted in check-storage as "wrote nothing", so strangers cannot make the realm pay for attempts). Gotcha recorded in the handoff: a zero stamp means "pre-upgrade claim" and silently falls back to HEIGHT gating — a hand-built test claim without stamps tests the wrong path. GATES: REQUIRE_GNO=1 make check exit 0 (105 reads, none allocating), make isolation-test — all 553 tests across 11 packages pass alone as well as together, make txtar-test ok including the other session's file. NOT COMMITTED (BRANCHING.md: the human owns commits). | P1 done, P2+P4 out |
| 2 | 2026-08-18 | P3 CLOSED: the coordination note is appended to BRANCHING.md under a new "Coordination log" heading — the file itself says to coordinate there rather than assume, and both sessions are in cryptocourt-mod right now. It names what I touched (testclock.gno, clock.gno's shim, startCourtUser's seal, a check-storage budget entry), points at HANDOFF-TESTCLOCK.md, and states the frozen-not-skewed semantics that make their boundary assertion hold. Also verified their new gate against my work: `check-paths.py` (which they wired into realm-test while I was mid-round) passes — 185 files scanned, 4 retired spellings, 0 stale hits in anything I added. P2 audit + P4 DSL lens both at fire 1. | P3 done |
| 2w-1 | 2026-08-18 | WAIT — P2 clock audit + P4 DSL lens at fire 2. No collidable work: P5 waits on the DSL spec, P6 on P5. | WAIT |
| 2w-2 | 2026-08-18 | WAIT — P2 audit + P4 lens at fire 3 (ping at 4). | WAIT |
| 2w-3 | 2026-08-18 | WAIT — both pinged at fire 4, each told the clock changed under them mid-flight (frozen while armed, not skewed) so neither audits nor specs a stale design. Duplicate at 7. | WAIT |
| 2w-4 | 2026-08-18 | WAIT — fire 5, both post-ping. | WAIT |
| 3 | 2026-08-18 | P2 CLOSED — the audit came back with FOUR HIGH findings, all with working proofs, and all are fixed. (H1) `AdvanceTestClock` checked NOTHING: arming handed the wheel to any address, and a PoC matured every deadline in the realm from a stranger's key — deployer-gated now, like its siblings. (H2) `CourtCount()<=1` was not a virginity test: the meta court seated at init is a fully working court that takes coin and claims, so a chain with months of activity was still "virgin" — arming now also requires the meta court to carry no claims and no coin. (H3) `tcSkew += secs` wrapped int64: two maximal "forward" steps walked the clock BACKWARDS (proven) — the step is capped at ten years and the total at a hundred. (H4) sealing zeroed the base, throwing already-written stamps into the FUTURE and reopening expired escrow windows, triggerable by any address via permissionless StartCourt — sealing now keeps a floor at the highest instant ever shown. Also applied: M1 the clock can never return the zero sentinel that clock.gno reads as "pre-stamp claim"; M2 the disclosure was half-built (accessor with no consumers) so `Render()` now banners every page while armed and `ClaimTimeline` carries `testclock:<skew>:0`; L1 all three entrypoints gained the `cur.IsCurrent()` guard the other 59 have; L2 deployer-check-first so a stranger learns nothing about the latch, plus a re-arm refusal. DESIGN REVERSAL, twice-confirmed: the implicit seal on StartCourt is GONE — the audit killed it for the rewind, and the P4 DSL lens independently found it makes the whole scenario system impossible ("you cannot seed a court and then advance time"). Added R2 from that spec: `EnableTestClockAt(cur, base)` so a scenario's dates are a pure function of its file. COORDINATION: the other session was editing `kourtv2_testclock.txtar` in lockstep (it referenced `tcFloor` minutes after I wrote it); their pins on the old sealing rule and the old abort strings would have left the suite red, so I reconciled their file with a comment naming the change and why, and wrote a detailed entry in BRANCHING.md's coordination log. GATES: check 0, isolation 0 (557 tests, was 553), txtar 0 including their file. P4 spec received — 8 sections, a worked scenario, a line-oriented DSL over YAML/JSON with the Norway-problem argument (this domain's tokens are literally `yes`/`no`), a Python compiler emitting txtar AND a live plan, a static two-clock model that turns the repo's gotchas into compile errors, and a hard finding for P5: time and height are independent axes, so a scenario that advances 12 weeks and mines 0 blocks yields a court of dead, worthless claims — both knobs are mandatory. | P2 done, P4 spec in |
| 4 | 2026-08-18 | P5 GROUNDWORK — measured the thing the DSL spec named as its own acceptance criterion, and it changes the plan. TWO FINDINGS. (1) THE HARNESS HAS NO WAY TO MINE A BLOCK ON PURPOSE: `gnoland` accepts only start/restart/stop (testscript_gnoland.go:275/366/399) and the command map is {gnoland, gnokey, adduser, adduserfrom, patchpkg, loadpkg, scanf, genesiscall, input} — there is no `wait-for-new-block`, which the spec assumed. Since `CreateEmptyBlocks=false`, the ONLY way to advance height is to broadcast a real transaction, so `mine n` compiles to n self-transfers, not a harness verb. (2) THE COST, measured twice on this machine with scratch txtars (written, run, deleted — never committed): 50 blocks = 8.5s, 250 blocks = 33.8s → a marginal ~126ms/block and ~2.2s of node boot. So the spec's batching rule (one `mine 2160` to mature every claim's answerability ring) costs ≈ 4.6 MINUTES per scenario — past the spec's own "revisit if over 3 minutes" line, and a fifth of txtar-test's 20-minute budget for a single file. RESOLUTION, no realm change needed: the two uses have different budgets. The DEMO-SEED scenario (P6, run by hand, once) pays the 4.6 minutes happily — it buys real conviction, real trailing rings, real series. CI SCENARIOS stay under ~200 blocks (~25s) and exercise the mechanics that do not need a matured ring: open, stake, unstake, moderation, folders, the clock itself. Answering — and therefore settling, crystallizing and drawing — belongs to the seed run and to the existing unit tests, which reach those states in-package for free. This also kills the temptation to shrink `answerWindow` under the test clock: the realm's accounting constants stay honest, and the only cost is that CI scenarios do not answer claims. P4 second lens (adversarial) is out at fire 1. | measurement in hand |
| 4w-1 | 2026-08-18 | WAIT — P4 second lens at fire 2. P5's shape is blocked on its format ruling (line DSL vs generated-from-Python), so no runner code yet; the measurement work it needed is already banked. | WAIT |
| 4w-2 | 2026-08-18 | WAIT (fire 3) + format-agnostic P5 groundwork banked: extracted the realm's REAL entrypoint surface straight from source — 63 write entrypoints (first arg `cur realm`) and 105 reads — into scratchpad/catalog.json, so whichever emitter wins gets its step table from the code rather than from a spec written by memory. Checked every step the DSL spec named against it: all 37 exist with the signatures claimed, so the grammar needs no correction. Confirmed the trap the spec flagged, mechanically rather than by eye: six functions READ like getters and are WRITES — AuthorBonus, AnswererBonus, PullCarrot, WithdrawStake, WithdrawBonus, PullSenior — so an `assert` that accepted any exported name would silently broadcast a transaction while pretending to query; the compiler must gate assertions on the reads list, which now exists as data. | groundwork banked |
| 4w-3 | 2026-08-18 | WAIT — P4 second lens pinged at fire 4, and handed both measurements (no mining verb in the harness; ~126ms/block, so `mine 2160` = ~4.6 min) so its format ruling is made against the real cost model rather than the spec's assumed one. | WAIT |
| 4w-4 | 2026-08-18 | WAIT — fire 5, post-ping. | WAIT |
| 4w-5 | 2026-08-18 | WAIT — fire 6 (duplicate next fire if silent). | WAIT |
| 5 | 2026-08-18 | P4 CONVERGED — and the second lens corrected ME on a load-bearing fact I had already reported as measured. I claimed the harness has no way to mine a block ("gnoland takes only start/restart/stop"). FALSE, twice over: `gnoland wait-for-new-block` exists at testscript_gnoland.go:401 with a helper at :722 that builds a raw-RPC self-transfer explicitly "for txtar tests that need to burn a deterministic number of blocks" — and I had been grepping /gnolang/gno_atomone while go.mod:106 replaces the dependency with ../../gnolang/gno, i.e. THE WRONG TREE. Both facts re-verified in the correct tree before writing this row. The lens's own numbers supersede mine: 102.6ms/block via the harness verb (vs my 126ms via full gnokey), boot ~1.5s (not 2.2s), so answerWindow's 2,160 blocks is ~3.7 min and votingBlocks' 120,960 is ~3.4 HOURS. RULINGS ADOPTED, all with evidence I checked: (O1) the compiler CANNOT predict height — tm2 proposes a "proof block" whenever AppHash changed (consensus/state.go:856 + needProofBlock), and the lens measured the same script finishing at heights 6/7/7/6/8 across five runs; so the wall-clock axis stays statically simulated (sound, because tcNow() is frozen) and the height axis becomes ASSERTED against the chain via a custom testscript command the repo may register itself. (O2) `gnokey maketx` defaults to `-simulate test`, and BroadcastHandler RETURNS BEFORE BroadcastTxCommit when the simulation errors — measured HEIGHT: 0 on a refusal vs HEIGHT: 7 on success — so every `! gnokey ... / stderr` line in all seven existing txtars asserts that the SIMULATION refused, not the chain; refusal steps need `-simulate skip` and cost 0 blocks. (O4) scenarios become Python data modules against a builder API, not a bespoke line grammar: the domain needs quoted free text, trees (nested folders), and repetition (three failed rounds, N blocks), and a hand-rolled parser would rebuild kwargs badly and need its own test suite. (O5) the demo node must NOT be left armed+unsealed: the frozen clock means "settles in 2 hours" never settles, the site projects dates from Date.now() so the two clocks diverge visibly, the realm's own banner is discarded by the site's parser (zero matches for TestClockActive in web/index.html), and `test1` is a PUBLISHED mnemonic so "deployer-only" protects nothing on a LAN — seed with a fresh key, seal at the end by default, and teach the site to read TestClockActive before P6 ships. (O6) INVERT the demo direction: DEMO's fields are DERIVED (burn = ceil(minted²/2e9), conviction per block, TWAP series, and the whole set pinned at a pretend height of 4.8M that a fresh node reaches in 5.7 days) — so `make seed-demo` runs the scenario and `make dump-demo` writes the dataset FROM the node, turning parity from an inverse problem into a function, with nested folders/relations staying in the existing kourtCuration overlay where they already live. (O7) R3 (short votingBlocks) is NOT v1: it is a new permanent entrypoint mutating an ACCOUNTING clock, which the handoff forbids faking. (O10) assertions are 75-90% of every existing txtar, so `expect`/`expect refuse` are the language, not an extension. FLAGGED, NOT DONE: the lens's highest-leverage experiment is that ~100ms of the 102.6ms per block is TimeoutCommit, and this repo owns the gno fork — but that fork is outside the worktree BRANCHING.md scopes me to, so it needs the owner's word. | P4 converged; P5 reshaped |
| 6 | 2026-08-18 | P5 FIRST HALF SHIPPED — `scripts/scenario.py` (a builder API + IR + txtar emitter) and `scenarios/smoke.py`, generating `gnoland/testdata/scn_smoke.txtar`, which RUNS GREEN against a real node in 5.7s and leaves `make txtar-test` passing whole. Built in the converged shape: scenarios are Python data modules (quoted free text, real tracebacks, constants that cannot coerce), assertions are first-class, and the emitter enforces the two rules the lenses proved matter — an `expect` naming any of the six write-shaped getters (AuthorBonus, AnswererBonus, PullCarrot, WithdrawStake, WithdrawBonus, PullSenior) raises rather than silently broadcasting, and every refusal emits `-simulate skip` so it is the CHAIN refusing in a block, not the simulator refusing in a sandbox. No height prediction anywhere; `mine n` emits the harness's own `wait-for-new-block`, and the scenario says out loud why it mines 5 blocks and not 2,160. THE SMOKE SCENARIO IS THE ARGUMENT FOR THE WHOLE FEATURE: it sits one second short of the twelve-week dead-claim timeout, asserts the chain still refuses, advances two seconds, and closes the claim — a boundary no integration test could reach by waiting, and one that only a FROZEN clock can express. FOUR BUGS FOUND BY RUNNING IT, all mine: `__file__` missing from the exec namespace; a `note` attribute shadowing the `note()` verb; qeval args needing gno literal syntax (strings quoted, numbers bare) where tx args do not; and my own assertion that ClaimCount would be 3 after two claims — it is 2, because nextID is pre-incremented (claim.gno:276-277), which is exactly the class of error the catalogue work was meant to catch and did not, since the catalogue records signatures, not semantics. Deliberately NOT touched: the Makefile (the other session is editing it) — `make scenarios` gets added once that settles. | P5 runner green |
| 7 | 2026-08-18 | P5 SECOND EMITTER — the same IR now also emits a live-node seed plan (`--emit plan`), shell-checked clean with `sh -n`, and the txtar output is byte-identical to before (one IR, two backends, no divergence). The plan differs from the txtar only where a live chain genuinely differs from a genesis harness, and each difference is written down in the emitter: funding becomes a transfer because there is no genesis phase to `adduser` into; actors get real keys made deterministically from their names into a throwaway keyring; refusal steps are DROPPED, because a seed run is not a test and a deliberate failure just leaves a confusing error in an operator's log; and assertions become greps, since testscript's stdout matching does not exist off-harness. It also carries the warning the second lens insisted on — a seeded node arms the realm's clock, so its dates are fabricated, and the deployer key on a throwaway chain is a PUBLISHED mnemonic, which is a LAN hazard, not a theoretical one. One bug found by checking rather than assuming: an apostrophe in a `${VAR:?message}` default broke the generated shell (`sh -n` caught it; the message is now apostrophe-free). Confirmed the P6 tools exist locally: gnoland, gnodev and gnokey are all on PATH, so seeding a real node needs no build step. txtar suite still green (11.4s). | plan emitter green |
| 8 | 2026-08-18 | **P6 SHIPPED — the website now has a real seeded chain to read.** `scripts/seed-node.sh <scenario>` brings up a throwaway gnodev, premines fresh random keys, deploys the realm, replays the scenario and seals the clock; measured end-to-end at ~25s from cold to serving. Verified against a live node rather than argued: `ClaimTimeline("orem",1)` answers `opened:1780000000:11;now:1787257601:44` — the dates moved twelve weeks while height moved 33 blocks, which is the entire point of the exercise — and `ClaimSeries("orem",1,"hourly")` returns real recorded history (`40000000:12000000`), not a synthesized shape. THE BUG THE LIVE NODE FOUND, which no unit test would have: sealing the clock cleared every flag including the disclosure, so a chain full of invented dates reported a perfectly clean clock — the exact moment a reader is most likely to trust a fabricated date. Split the capability from the disclosure: `TestClockActive()` now means "can still be driven", the new `TestClockFabricated()`/`TestClockPeakSkew()` outlive sealing, and Render, ClaimTimeline and the overlay all key on Fabricated. Two tests pin it. The overlay gained a node-level banner (`#tcbanner`, outside `#main` so it survives route swaps, cached per RPC endpoint, silent in demo and silent on a realm that lacks the accessor) with a 19-case harness, all passing. Also fixed: my own generated plan used flags that do not exist (`gnokey add --recover --entropy "str"` — `-entropy` is a bool), referenced `$DEPLOYER_ADDR` without setting it, and signed as `test1` on a chain where the key is named `deployer`. Gates: realm-test, txtar-test and `REQUIRE_GNO=1 make check` all green; scn_smoke.txtar byte-identical, so the two emitters have not drifted. | P6 done |
| 9 | 2026-08-18 | ROUND OPENED on the one piece still missing — P5's DEMO reproduction (the O6 dump side) — with both lenses out at fire 1: a design lens on what `make dump-demo` emits and how it splices, and an adversarial lens attacking whether the inversion is sound at all given DEMO is pinned at a pretend height of 4.8M while a seeded node sits near 50. Phase table corrected first: it still showed P6 unstarted although round 8 shipped it. INTERRUPTED BY A REAL BUG THE OWNER HIT ON THE SEEDED NODE, now fixed: my banner used `grid-column:1/-1`, which spans the RAIL as well, and the rail is `position:sticky` with an opaque background — so it painted over the first third of the sentence and "Test chain — the dates here are fabr" was invisible. Fixing it exposed a WORSE bug I would otherwise have shipped: `grid-row:1/-1` is meaningless when the grid declares no explicit ROWS (`-1` counts back to line 1), so the rail spanned a single row and `.main` auto-placed into COLUMN 1 BELOW IT — measured `main.left=0, main.top=900`, i.e. the entire page body pushed off-screen on every route, banner or no banner. `body` now declares `grid-template-rows:auto 1fr`, the banner is pinned to column 2 row 1, main to column 2 row 2, and the single-column breakpoint releases all three. Verified by MEASURING GEOMETRY in headless Chrome rather than by reading CSS: 20 assertions at 1440/1100/720px — banner starts clear of the rail (346 vs rail-right 290), the bold opening words are visible, it sits above the page body, aligns with the content column, fits the viewport, no horizontal scroll — plus the hidden case proving an ordinary chain is pixel-unchanged (`main.top=0`, rail still 900 tall). Screenshotted live against the seeded node on 26707 for confirmation. Harness: `scratchpad/banner_layout.js`. | banner fixed; lenses out |
| 10 | 2026-08-18 | **BOTH LENSES IN, AND THE ADVERSARIAL ONE KILLS P5's ACCEPTANCE CRITERION.** Lens B's verdict, with source citations I spot-checked: "reproduce today's DEMO dataset FROM a scenario file" is not expensive, it is IMPOSSIBLE — and it proved this three ways. (1) DEMO's conviction column is 8×–74× ABOVE what the realm's own formula can produce over each claim's stated lifetime (`stake.gno:46-48,271-278`, pinned by `stake_test.gno:230-249`: one week at the launch floor yields 0.6375% of stake, 1.6065% at the unreachable ceiling). So today's DEMO is not merely unreproducible, it is a state the realm CANNOT OCCUPY — a real, unnoticed bug, and the best argument for O6 that exists. (2) Reaching DEMO's conviction honestly needs ~10.1M blocks (~12 days of continuous mining) for one court, and doing so would contradict DEMO's own timelines, which `deadClaimTimeout` (12 weeks) forbids. (3) The live plan mines by paying `-gas-fee 1000000ugnot` per block against a 100,000-GNOT premine, so it runs the deployer BROKE at ~100,000 blocks — short of the 120,960 needed for the first emission roll, one dispute round, one flag vote, or a mature week-ratio. Structural findings that stand regardless of direction: a fresh court accrues ZERO emission before block 120,960 (`emission.gno:71,127-132` — `curPeriodBudget` is left at zero by `startCourt`), `Crystallize` is barred before block 17,280 (`crystallize.gno:39-41`), and `trailYes` needs 120,960 blocks and then comes out FLAT — which cascades, because `claimSeries()` bails without it and the docket sparkline and hero chart disappear. Lens A independently reached the same height conclusion and recommends `NOW` become the seeded chain's real height. BOTH lenses converge on one structural point I am adopting: DEMO must be SPLIT — a generated chain-true half and a hand-written overlay half (`desc`, nested `folders`, `relations`, `voteEndsAt`) that has NO chain source and that O6 wrongly assumed already lived elsewhere; it does not, it lives inside the literal `curationFor()` reads. VERIFIED MYSELF rather than trusting the reports: the demo/chain status-string drift is REAL (`web/index.html:950` vs `render.gno:420-440` — four of seven branches differ, and the chain's are strictly more informative), and the `minted = supply − emitted` identity at `index.html:786` is FALSE on any chain with CC burns (8+ `coin.Burn` sites). OWNER DECISION NEEDED before P5 can close; recorded, not acted on. Gates: web harnesses (search/folders/tcbanner/banner-layout) all pass; `node --check` clean; isolation-test still running. | lenses converged; P5 criterion challenged |
| 11 | 2026-08-18 | **VERIFIED THE ADVERSARIAL LENS'S HEADLINE MYSELF, AND IT IS WORSE THAN REPORTED — THEN FIXED IT AND BUILT THE GUARD.** Rather than take the report on trust I recomputed every conviction value in DEMO against the realm's own formula, using the constants read from source and the identity pinned by `stake_test.gno:TestConvictionAccruesExactly` (one 120,960-block week of stake S yields S x rateFP / 1e10; rateFP = 255 x (r0WeeklyBps 25e6 + dEffBpsFP) / 100, dEff capped at budgetWeeklyBps 38e6 and only ever stepping down). ALL 20 conviction values in the shipped demo exceed what the realm can produce — 6.0x to 29.5x at the UNREACHABLE ceiling, 15x to 74x against the launch floor. The demo has been teaching readers a state the chain cannot occupy, on every claim page, and no test noticed because the demo is data and data had no tests. FIXED: all 20 recomputed at the launch-floor rate over each claim's own timeline — a value every court can reach regardless of its emission history, so the dataset cannot be wrong about it (e.g. orem/2 convYes 24,000,000 -> 323,114, which was 74.3x over). BUILT THE GUARD, which is the durable part: `scripts/check-demo-physics.py` reads periodBlocks, r0WeeklyBps and budgetWeeklyBps FROM THE REALM (a check that hardcodes the number it checks only pins the moment it was written), derives the ceiling, and fails on any claim whose stated conviction exceeds what its own stake and lifetime allow. Proven with a NEGATIVE CONTROL, not just a green run: restoring one pre-round value makes it exit 1 with the exact arithmetic, and restoring the fix makes it exit 0. Wired into `realm-test` beside the other eight checkers. It reports rather than fails the 9 claims that state no timeline, since an unconstrained value contradicts nothing. NOTE the three invariants a human had written into DEMO's comments (burn curve, senior-queue sum, series/pool agreement) ALL HOLD — the one that broke is the one nobody wrote down, which is the argument for writing them as code. GATES ALL GREEN: `REQUIRE_GNO=1 make check` exit 0 with the new checker in the chain, `make isolation-test` exit 0 (559 tests across 11 packages, up from 557), four web harnesses pass. | demo physics fixed + guarded |
| 12 | 2026-08-18 | **CLEARED THE ADVERSARIAL LENS'S "FIX REGARDLESS OF DIRECTION" LIST — all seven, each verified before it was fixed.** (1) QUOTING, the one that mattered: `_arg()` wrapped a token in single quotes whenever it held a space, so any possessive emitted broken tokens — and 8 of the demo's own 19 titles carry an apostrophe. testscript does NOT quote like sh: it uses rc/Pascal rules where `''` is a literal quote (go-internal testscript.go:1287, read, not assumed). There are now two rules, `_q_txtar` and `_q_sh`, one per target. PROVEN, not asserted: the smoke scenario now OPENS a claim titled "The mayor's office said the Center St. bridge inspection was 'complete'." and reads it back, so both quoting layers are exercised on every run. That assertion immediately found something real — `ClaimTitle` serves through `sanitize.InlineText` (modrender.gno:123), so "St. bridge" comes back markdown-escaped as "St\\. bridge"; the overlay already undoes this with `unesc()` at all three ClaimTitle sites, so it is correct behaviour, now documented in the scenario. (2) The live plan paid a gas fee per mined block against a FIXED premine and would have run the deployer broke mid-seed; the premine is now sized from the scenario's own block and tx counts, and `mine()` refuses anything over 500,000 blocks with the wall-clock cost spelled out. (3) `SealTestClock` was appended unconditionally, permanently barring a later arming on a node whose scenario never armed the clock — now conditional on `_clock_armed`. (4) The write-shaped-getter DENYLIST became an ALLOWLIST derived from the realm source at emit time; the old list had already missed `ClaimMetaFranchise(cur realm)`. Verified both ways: it refuses ClaimMetaFranchise/PullCarrot/AuthorBonus and allows TestClockFabricated, a read that did not exist when the list was written. (5) The `.replace("  "," ")` that only the txtar emitter applied is gone — one IR was producing two different chain states. (6) The module docstring claimed height was "ASSERTED against the chain"; no such verb was ever built, so the claim is retracted in place rather than left to mislead. (7) `make scenarios` did not exist although every generated file names it; it exists now, with `make scenarios-check` wired into `check` so a stale generated txtar fails the build instead of quietly disagreeing with its source. ALSO FOUND AND FIXED A MEASUREMENT BUG OF MY OWN: my height parser read `"latest_block_height":"N"` but gnodev emits a space after the colon, so it silently returned empty — which is why round 8's height line printed nothing and why the first mining run died on a division by zero. MEASURED with it fixed: gnodev SURVIVES sustained mining (1,646 blocks and climbing, RSS oscillating 154-543 MB, no monotonic leak), answering lens A's open question. But the RATE is ~525 ms/block via `gnokey maketx send`, four times the ~126 ms this log has carried since round 4 — so 2,200 blocks is ~19 minutes, not ~4.6. Measured under concurrent gate runs, so treat it as a loaded-machine upper bound; either way the old figure should not be planned against. GATES: `REQUIRE_GNO=1 make check` exit 0 (now including demo-physics and scenarios-check), `make txtar-test` exit 0 with the apostrophe claim. | runner defects cleared |
| 13 | 2026-08-18 | **CORRECTED A CLAIM I MADE ABOUT MY OWN P6 DELIVERABLE, and settled the open memory question.** I had assumed, after a failed headless probe, that the overlay could not reach a seeded node from a `file://` page — plausibly CORS. WRONG, and the assumption was mine, not the code's: gnodev answers `Access-Control-Allow-Origin: *` (measured with a raw POST carrying `Origin: null`), and the overlay loaded from `file://` reads the node correctly — mode=live, banner shown, `CourtCount()` returns 2, court page renders. My earlier probe failed only because it wrote localStorage AFTER the page script had already read it. So READING works from disk. SIGNING does not, for a different reason entirely: browser extensions are not injected into `file://` pages, so a wallet is invisible there — which is exactly the "no wallet found" the owner hit. `seed-node.sh` now states both facts and gives the one-liner to serve the folder over http when a wallet is needed; before, it told the reader to open the file and said nothing about the wallet. FIXED AN INCONSISTENCY ROUND 12 INTRODUCED: making the seal conditional turned the launcher's unconditional "The clock is sealed" banner into a potential lie, so the PLAN now reports its own clock state ("sealed" or "never armed") and the launcher stops asserting it. SETTLED lens A's open question by measurement rather than argument: gnodev SURVIVES sustained mining — 2,046 blocks reached, RSS oscillating 154-586 MB with no monotonic climb, node still answering. Rate held steady at ~510-570 ms/block across the run. Re-verified end to end on a fresh seed: the apostrophe claim round-trips (`ClaimTitle("orem",2)` returns the mayor's-office title, markdown-escaped as expected), the clock line prints, `TestClockFabricated()` stays true past sealing. OPERATIONAL NOTE, my error: a `pkill -f 'gnodev local /Users'` to clear my own nodes also killed the seeded node the owner was reading; reseeded on the same port so their browser config still worked, but a narrower match would have avoided it. The port guard did its job in the same run — it refused 26657, which is the owner's own long-running node. Fresh adversarial critic on the round-12 runner changes is out at fire 1. | file:// settled; critic out |
| 14 | 2026-08-18 | **ENCODED THE THREE INVARIANTS THE HUMANS *DID* WRITE DOWN — and encoding them immediately found a fourth violation.** Round 11's lesson was that the invariant nobody wrote down is the one that broke; the corollary is that "it holds today" is an observation, not a property. DEMO states three more in prose comments: `burned` is the exact curve cost of `minted` (ceil(minted^2/2e9)), the senior queue's sum(amount-paid) equals the court's `seniorOwed`, and the last row of each `seriesH`/`seriesD` equals the claim's pools. The adversarial lens reported all three as holding; it had spot-checked. THEY DID NOT: the annex court stated `burned: 18,000,000,000` — a round number — under a comment asserting the value is exact, where its own curve gives 17,999,760,001, off by 239,999. Corrected, and all three are now checks in `scripts/check-demo-physics.py`. THE PART THAT MATTERED MOST WAS NOT THE CHECKS BUT PROVING THEY CAN FAIL. Negative controls on all three: the burn check fired, the series/pool check fired — and the senior-queue check DID NOT. It was vacuous: the literal closes `]}}` on one line and my newline-anchored regex matched nothing, so it passed while reading zero entries, which reads as coverage and is worse than no check at all. Rewritten with brace matching, and it now fires on three separate corruptions (a wrong seniorOwed, a wrong entry amount, and a RENAMED queue key — the last being the shape-change case that produced the vacuous pass, so the failure mode itself is now guarded). GATES: `REQUIRE_GNO=1 make check` exit 0, four web harnesses pass, generated txtars unchanged. Fresh critic on the round-12 runner changes at fire 2 (ping at 4). | three invariants pinned; a vacuous check caught |
| 15 | 2026-08-18 | **TWO MORE REALM-DERIVED INVARIANTS PINNED; ONE MORE SELF-CONTRADICTION FOUND.** Extended `check-demo-physics.py` from three checks to five, and the new pair found a defect the same way the last pair did. (a) SETTLE DEADLINE: `settleAt` is `SettleDeadline` = answerHeight + `settleDelay` (51,840, read from court.gno). Checked two ways — against the claim's OWN timeline where one states a `settle` height, and against answerHeight+delay for claims still in phase "answered". Disputed and provisional claims are deliberately NOT checked by the second rule, because a later answer moves the anchor and a mismatch there is not evidence of a bug. FOUND: `orem/6` contradicted itself — its timeline stated settle at block 4,797,800 (which is exactly answered 4,745,960 + 51,840) while its `settleAt` field said 4,798,000, so the resolution ladder and the docket clock quoted DIFFERENT BLOCKS for the same event on the same page. The timeline was the correct side; `settleAt` is written relative (`NOW-2_000`), now `NOW-2_200`. (b) QUORUM FLOOR: `quorumFloor()` takes a max() against 5% of supply (`quorumSupplyBps = 500`, read from dispute.gno), so that arm is a hard lower bound even though the other arm — min(xBarFrozen, votable/3) — uses quantities the demo does not state. Verified the demo's own comment while I was there: 5% of orem's 118,500,000,000 supply is exactly the 5,925,000,000 it claims. ALSO CHECKED AND DELIBERATELY NOT "FIXED": the demo/chain status-string drift reported in round 10. The chain's `claimStatus` emits block numbers ("disputable until block 4809000") and the page's `statusText` does not — but that is the OWNER'S RULING (prefer dates to heights, heights on hover), so copying the chain's wording into the page would have regressed it. Recorded rather than acted on. Every new check proven with a negative control: all five fire on a targeted corruption, including the answered-rule, which needed the control applied to the answer height rather than to `settleAt` because that field is written relative to NOW. GATES: `REQUIRE_GNO=1 make check` exit 0, web harnesses pass, generated txtars unchanged. Critic on the round-12 runner changes at fire 3 — ping next round. | 5 invariants pinned; ladder/clock contradiction fixed |
