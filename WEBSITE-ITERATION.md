# Website (web/) iteration state — phase 2

Machine-maintained by the website loop (influence session, cron `d6b47efa`,
fires every minute; one fire = one round). Same discipline as the whitepaper
loop: verify critic findings against code before applying; log accept/reject
with evidence; CLEAN = nothing survived verification.

## Convergence rule

≥ 10 substantive rounds AND 4 consecutive CLEAN. Then: finalize
HANDOFF-WEBSITE.md, `CronDelete d6b47efa`, report convergence, await owner
direction (standing order: don't stop).

**STATUS: CONVERGED 2026-08-17 — 41 rounds, 27 critic sweeps, ~90 verified
findings applied, 4 consecutive CLEAN (rounds 38, 39, 40, 41: reward-pull
paths, transaction-argument fidelity, read-path contract fidelity, and the
full final coherence read all returned zero findings). Cron d6b47efa deleted.**

## Standing constraints

- Owner directive (2026-08-17): polish + TEST; first-visitor friendly — spell
  out what a court is, what a claim costs, what minting voice means, BEFORE
  jargon; integrate Adena (window.adena) for connect + signing; every user
  action shows a CLI helper (inline gnokey command builder and/or the realm's
  gnoweb $help link); §7.4 comms hygiene binds all copy.
- Canonical sources: WHITEPAPER.md (converged) for content; realm code for
  mechanics; web/README.md's own §7.4 interface contract stays binding
  (verbatim titles, no live tallies while sealed, verdict shows route,
  principal never framed as wager, no backing/redeem/APR language).
- Writable: web/**, this file, HANDOFF-WEBSITE.md. Never commit. The site must
  remain fully self-contained (no external scripts/fonts) and keep working
  with NO wallet installed — Adena is an enhancement, the gnoweb links remain
  the baseline path.
- The existing design is good. This is a polish-and-extend loop, not a
  rewrite. Demo mode must keep making zero network calls.

## Recon findings (round 1, 2026-08-17)

- R1 web/ last touched by the other session 2026-08-16 (rename + courtv2 port);
  git-clean now. 965-line self-contained index.html: hash-routed views
  (directory/court/claim/me/needs/about), ABCI reads via vm/qrender +
  vm/qeval, demo mode with zero network calls, every action a gnoweb
  `$help&func=` link with args pre-filled (tx() at ~line 543).
- **F1 rename misses**: title line 6 "CryptoCourt — the ledger of record";
  comment line 10 (also says r/kourt/courtv2 — wrong path); brand h1 line 266;
  line 578 "courtv2 realm" prose; demo markdown line 918 "# CryptoCourt V2".
  README.md line 3 same path typo.
- **F2 Adena: zero integration** (grep count 0). Memory adena-api.md (8 days
  old, researched from docs.adena.app + extension source; not yet
  browser-verified) gives the shape: AddEstablish(name) → GetAccount()
  (.data.address) → DoContract({messages:[{type:"/vm.m_call",value:{caller,
  send,pkg_path,func,args}}]}) signs AND broadcasts; envelope {code,status,
  data}: 0 ok, 4000 user-reject, 4001 already-connected (fine), 3001
  caller≠connected; GetNetwork()/SwitchNetwork(chainId) before calling;
  window.adena injected async — check on click, never at load. Search
  confirms docs.adena.app + an official TS adena-sdk exist; memory is
  current enough to build against, browser verification still pending.
  **CRITICAL UX FACT: extensions generally do not inject into file:// pages**
  — wallet features must detect absence gracefully and README must say "serve
  over http(s) for wallet features" (python3 -m http.server). Baseline gnoweb
  links keep working from file://.
- **F3 first-visitor onboarding**: "How it works" view exists but the landing
  (directory) assumes vocabulary. Owner wants the essentials before jargon:
  a dismissible first-visit strip (localStorage) on the directory — what a
  court is, what a claim costs (burned GNOT → coin → stake), principal
  always returns 1×, verdicts are the court's conclusion. Copy adapted from
  WHITEPAPER §2/§4/§7 (canonical).
- **F4 CLI helpers**: $help links exist (half the owner ask). Add per-action
  "CLI" reveal showing the equivalent gnokey command with a copy button:
  `gnokey maketx call -pkgpath "gno.land/r/kourt/kourtv2" -func <F> -args …
  [-send Xugnot] -gas-fee 1000000ugnot -gas-wanted 10000000 -broadcast
  -chainid <id> -remote <rpc> <key>` — flag shape per the gno skill's
  gnokey reference; chainid/remote filled from the configured node; key left
  as a placeholder. Verify exact flags against docs before shipping (round 3).
- **F5 hosts**: default gnoweb host is https://gno.land; target testnet is
  sapphire (rpc.sapphire.testnets.gno.land / sapphire.testnets.gno.land).
  Add preset dropdown (local gnodev / sapphire / custom) for both RPC and
  gnoweb host; sapphire chain-id sapphire-1.
- **F6 testing**: each JS-touching round: extract <script> and `node --check`
  it; serve via python3 http.server from scratchpad and curl the page; note
  any console-visible errors via critics with browser reasoning (no live
  browser available — flag for owner manual pass at the end).
- F7 title fix: keep "the ledger of record" tagline (record framing,
  §7.4-safe), fix the name only.
- F8 README.md line 3 path typo (r/kourt/courtv2 → r/kourt/kourtv2).

## Plan (priority order)

1. Round 2: F1+F8 renames; F5 host presets; F3 onboarding strip. Smoke test.
2. Round 3: F4 CLI command builder on every action (stake/unstake/answer/
   dispute/buy/flag/vote…), verified flags. Smoke test.
3. Rounds 4-5: F2 Adena — rail Connect button (AddEstablish("Kourt") →
   GetAccount → persist addr → "Your positions"/"What needs you" use it);
   per-action "Sign with Adena" using DoContract /vm.m_call (send only on
   Buy); GetNetwork/SwitchNetwork guard; graceful degradation (no wallet →
   button explains + gnoweb path unaffected; file:// → hint to serve http).
4. Then critic panels (first-visitor walkthrough, Adena-correctness vs
   adena-sdk/docs, copy-vs-whitepaper consistency, a11y, mobile, skeptic)
   until convergence.

## PHASE 4 (2026-08-17, owner directive, verbatim) — CRON: 2a5b919f (1m, session-only)

"in a 1m loop think of this as an informational system and iterate on the overall design to make it a good system of information discovery. in particular, screen3 in the wireframes, the map, make the map page dedicated and good, interactive with many features. actually render the output and check that it is legible. the default view should look like screen3 more or less. before, when asked to draw the graph nodes were overlapping with each other and icouldn't read it. there needs to be zoom toggle, text toggle, perhaps a slide bar or something. the folder and subclaim relationship is primary. besides the map, think of what it takes for the initial creator of a court, or its moderators, to fill in details without having to put up bonds. consider making a tool for exporting and importing nodes. in general, keep iterating on the website UX. consult the original wireframes and keep the layout like the original wireframes more or less. do not stop. work on one aspect a time, and use subagents until convergence, and then implement, then move onto the next issue."

NOTE: "screen3 in the wireframes" = the REFERENCE wireframe's Screen 3 (courts-wireframe.html — the claim MAP: 620px pan/zoom viewport, stance edges with arrowheads, zoom-dependent labels, "214 claims, 23 shown"). Not our doc's Screen 3.

BACKLOG (one aspect per convergence cycle, in order):
- C1 THE MAP: a dedicated interactive map page (#/c/:slug/map). Default view ≈ reference Screen 3. FOLDER and PART-OF (subclaim) relations are the PRIMARY structure; bears/supersedes secondary. Hard requirements: no overlapping nodes (the owner could not read earlier attempts) — layout must be computed and LEGIBILITY-VERIFIED by actually rendering output and measuring (node-side collision checks on the generated SVG at minimum; check label collisions too); zoom toggle; text/label toggle; a slider (zoom or detail level); pan. Demo data first (relations are overlay); live degrades honestly.
- C2 CREATOR/MOD TOOLING: what a court creator or moderator needs to fill in details WITHOUT bonds (descriptions, folders, relations, scope). An export/import tool for nodes (the overlay dataset as JSON out/in; local, no chain writes; §7.4-safe framing). Check what folders.gno mod calls already allow bond-free.
- C3 CONTINUE UX ITERATION: remaining richness items (B5 needs view, B6 me view per reference Screens 7/9), reference-layout conformance sweeps, whatever the critic surfaces. Keep the records-office look; keep the left rail (owner ruling).

PROCESS: unchanged — converge with 2-3 lens subagents + judge per aspect, implement, smoke (node --check + serve + curl), fresh critic, log with evidence, keep wireframes.html reconciled (GENERATE doc figures from live functions). §7.4 binds. Never git-commit. Realm wants → Owner notes.

ROUND LOG (phase 4):
| 97 | 2026-08-18 | BRAND + VOCABULARY (owner: "KOURT:XYZ tokens", then "fix it everywhere, 'money' should not be mentioned anywhere"). I first claimed the site had no KOURT reference — WRONG, and worth recording: my grep was case-sensitive against an uppercase ticker while the file carries 25 lowercase `r/kourt/kourtv2` path hits. The convention is documented and I should have read it first: MODERATION.md §13.9 (the Pleadger supersession) and HANDOFF-WHITEPAPER.md fix the canonical display as **KOURT:SLUG**, review court KOURT:META. APPLIED: the overlay now displays KOURT:OREM wherever it named a court's token — the ballot's eligibility line, the vote-help modal, and the buy panel's three ticker sites — while demoRender is deliberately LEFT BARE (it mirrors the chain's own Render, which stores uppercase(slug) as the symbol; the namespaced form is a presentation convention, and faking it in the "as the chain serves it" view would make that view a lie). VOCABULARY: every instance of "money" is gone from web/index.html — 7 total, 3 in visible copy ("Anyone with money on the claim" → "Anyone holding KOURT:OREM staked on the claim"; "bonds spend real money" → "real tokens"; the about page's "no money ever moves between adversaries" → "nothing ever moves"), 4 in engineering comments (balances / an amount field / holdings views), plus the vote modal's section retitled "Where the tokens go" and its burn/mint sentences naming KOURT:OREM. The buy panel's closing line said "money spent for a voice" about GNOT, which was ALSO imprecise about which asset — now "GNOT spent for a voice, not GNOT moved". EVIDENCE: a 10-route browser sweep finds "money" on no page; the quality line and about page render the new wording; 13 harnesses + 7 probe suites green, with 3 pins updated for the ticker and a new guard asserting the word never returns to the file. | NOT-CLEAN (2 applied) |
| 96 | 2026-08-18 | BALLOT FINE PRINT → A HELPER MODAL, IN PLAIN PROSE (owner: move the sealed-tally paragraph and the "nothing moves between adversaries" block into a popup, reworded simply). NEW COMPONENT: a native <dialog class="helper"> — Escape, the backdrop, focus containment and the return of focus all come from the platform rather than hand-rolled JS; opened by a delegated [data-help] handler, closed by its button, Escape, or a backdrop click. A11y CATCH: the triggers were first written as <a class="helplink"> with no href, which is NOT keyboard-reachable — the probe caught it (Enter did nothing, focus never entered the dialog); they are now real <button type="button"> styled as links, verified focusable + Enter-openable. THE REWRITE: three sections in ordinary sentences — "Why you can't see the count yet" (every vote is public; the page just won't total them mid-vote, because a running total makes copying the biggest voter the winning move; sealed means un-summed, not secret), "Who can vote" (any court-coin holder, weighted by the snapshot before the round opened, so buying now adds nothing; author, answerer and stakers are shut out because money on the outcome disqualifies you from judging it), and "Where the money goes" (nothing crosses between sides — forfeits burn, awards are newly minted — then the three outcomes as bullets, each with its real limits: half-burn on a failed round, the answer bond burned whole on an overturn, make-goods capped at 2× the bond and 80% of the burn, and staked coin returning 1× regardless). THE TICKET now carries three short lines and three links instead of two dense paragraphs plus an inset; the eligibility line was also tightened to one sentence (its live #voteelig fill slot preserved and verified). EVIDENCE: modal opens/closes by click, Enter, Escape and backdrop; focus lands inside; aria-labelledby present; fits a 390px phone at ≤92vw; 6 new pins hold the contract (native dialog + labelling, buttons-not-anchors, the short lines, the dense blocks' absence from the ticket, the plain-English sentence, a §7.4 sweep) and 6 older fact-pins were RETARGETED at the modal's new wording so the facts stay pinned rather than deleted; 13 harnesses + 7 probe suites green. | NOT-CLEAN (feature shipped) |
| 95 | 2026-08-18 | TICKET ROWS + LADDER ORIENTATION (owner report on the ballot: "the left column should be bolded", "the right side should still be left-adjusted, not right adjusted — this isn't arabic", "'threshold' and 'overturn needs more than…' are touching"). ROOT CAUSE, one rule: `.ticket .line` was `display:flex; justify-content:space-between` with `.r{font-weight:600}` — so every ticket in the app put its EMPHASIS on the right, right-adjusted (ragged left, which is what reads as reversed), and space-between collapses to a ZERO gutter the moment a row's two texts fill the width, which is exactly what "threshold"/"overturn needs more than…" did. FIX: the row is now two real grid tracks — `minmax(0,20ch)` label + `minmax(0,1fr)` value, `column-gap:24px`, baseline-aligned — with the LABEL bold in ink and the value normal-weight, `text-align:left`, tabular-nums retained for the receipts; below 640px the pair stacks. Applies to every ticket at once (ballot, stake, /me, senior queue, buy receipt) since they all share the row. Measured after: value columns align within each ticket and the minimum gutter is 24px (was 0); zero rows overflow their container. LADDER (same round, owner: "switch the left and right, dates on the right, doesn't need to be all the way to the right"): the clock ladder moved from per-row flex to a container-level grid (`.clockrows`, `minmax(0,30ch) max-content`, `justify-content:start`) so the event is the bold thing on the left, its caption sits under it, and the DATES align in one column that starts just past the labels instead of being flung to the container's right edge — verified: every date cell shares one left edge (616px) and ends at 844 inside a 1056px ticket. EVIDENCE: ballot/stake/me/ladder screenshots; 13 harnesses + 7 probe suites green; 5 new pins in dispute_test hold the row contract (grid + gutter, bold-left, left-aligned values, the flex rule's absence, the narrow-screen stack). | NOT-CLEAN (2 applied) |
| 94 | 2026-08-18 | THE CLOCK: PUBLISHED DEADLINES ARE NOW DATES (owner ruling: "just use time, not height, and also store height along the way for reference; show the height on hover"). The ladder used to be a column of ≈block numbers with one row labelled "derived: settle deadline − 72h" — the answer height was BACK-COMPUTED because no read exposed it, even though the realm stores cs.answerHeight. REALM (clock.gno, new): every claim now stamps the block's own time at open/answer/verdict plus an absolute reopen deadline, and the five PUBLISHED deadlines gate on those stamps — settle window, dispute window (its partition twin), reopen/escrow, dead-claim timeout, polish window. ACCOUNTING clocks stay on height, deliberately and documented: conviction accrues per block, emission periods are block-counted, the coin's voting snapshots are height epochs, and the stake-history grain buckets heights — re-denominating those would change what claims pay. PRE-UPGRADE SAFETY: every converted gate falls back to its original height arithmetic when the stamp is zero, so a claim opened before this change keeps exactly the deadline it was opened under (pinned by a test that zeroes the stamps and proves both the refusal and the settle still land on the height). Trade-off recorded in the file: block time is proposer-written (bounded drift, seconds) where height is pure consensus counting — the right trade for a deadline measured in days, the wrong one for money math. NEW READ: ClaimTimeline(slug,id) → "opened:<unix>:<height>;answered:…;verdict:…;settle:…;reopen:…;now:…" — one read, both clocks, zero stamp meaning "predates the stamps". WEB: the ladder is rewritten — past rows are exact recorded dates ("12 Aug 2026, 00:40 UTC · 6 days ago"), the future row is the realm's own deadline, a projected row (the unexposed vote close) is the ONLY one marked ≈, every row carries its block on hover, and the footnote says exactly that. The "derived:" label is gone. DEMO: 10 claims gained timelines computed from their own heights against a fixed sample now (18 Aug 2026 12:00 UTC), so demo and live parse one grammar. EVIDENCE: make realm-test green (103 reads incl. ClaimTimeline; 3 new clock tests — stamps exist, the settle gate follows the DATE across a pace change, pre-stamp claims keep their height gate — using the house uassert.AbortsContains idiom after plain recover() proved useless against cross-call aborts); 13 harnesses + 7 probe suites green; 6 chart pins added for the timeline grammar, UTC formatting, relative words, and dataset sanity (nothing stamped in the future); ladders screenshotted for disputed/settled/answered/provisional. | NOT-CLEAN (feature shipped) |
| 93 | 2026-08-18 | OWNER-REPORTED FIXES (relation chips). (1) From orem/3 the "contradicts" chip collided with the wrapped claim title — measured overlap −6px (title's first line ended at x=799, chip began at 793), and on orem/11 −55px. ROOT CAUSE: `.crow.assocrow` (specificity 0,2,0) was being outranked by `.docket a.crow` (0,2,1), so the argument rows silently kept the DOCKET's four-track grid (52px/1fr/64px/124px) instead of their own — the chip sat in a 64px column hard against the title's ragged edge, and vertical centring parked it mid-sentence on a two-line title. FIX: the rule now matches `.docket a.crow.assocrow` (0,3,1) with a three-track grid (52px / 1fr / 268px), both pills ride one right-aligned `.rt` cluster (`justify-self:end`, `flex-wrap`), and a ≤820px branch stacks the cluster under the title. Measured after: gutters 90-188px on every argument row, and the cluster still hugs the right edge on the D6-3 strip rows that share the class (14px, unchanged). (2) The contradiction family now reads BRIGHT RED: new `--contra` token per theme (#d92d20 light / #ff6b5e dark, with washes) driving `.pill.contra` for "contradicts"/"contradicted by this" — and the MAP's contradicts edge switched from the muted `--no` brick to the same token, so one vocabulary covers chip, edge, and legend. EVIDENCE: browser-measured before/after on orem/3, /9, /11 wide + a 760px narrow pass (chip stacks, 13px below the title); arg_test +7 pins (cluster markup, the specificity trap as a regression guard, contra classing for both directions, 4 theme tokens, the map edge, the narrow branch); all 13 harnesses + all 7 probe suites green. | NOT-CLEAN (2 applied) |
| 92 | 2026-08-17 | REAL STAKE HISTORY SHIPPED, realm + web (owner commission: "for each claim a compact representation that allows for an actual graph… every hour for X hours, every day for X days… no storage when unchanged… gas efficient… used in both places"). CONVERGENCE: 3 lenses (storage/gas, read-surface+§7.4, web chart), judged: batched ClaimSeries read beat per-point (2 qevals vs ~170/chart); two Series per grain (yes,no — one-sided ops write one entry, exact reconstruction) over (total,share); parallel hourly+daily tiers over prune-time downsampling (the second SetAt is free in-epoch; replay could desync); step-after rendering (change-only data: interpolation would invent motion). REALM: checkpoint gained Trim (whole-pages-below-horizon EXCEPT the newest such page — the floor-preservation invariant keeps ValueAt exact inside the kept window through every bounded partial trim; collect-then-remove, ≤maxPages), WalkDesc, Series.Prev; kourtv2: 4 Series VALUE fields on claimState (inline = a pointer would be a second object) + one shared csArch per Court (keys cs:<id>:yh/nh/yd/nd — NEVER beClaimKey, its zero bytes ARE checkpoint.Sep), recordPools after both pool mutations w/ setIfChanged (change-only is the CALLER's job — SetAt archives unconditionally on a roll, the grc20votes addVotes discipline), trim rides ≤once/hourly-epoch budget 2 + settle-time clip in SettleUndisputed/Finalize, ClaimSeries(slug,id,grain)→"grain,width,now,more;epoch:yes:no,…" (union-merge forward-fill, newest-suffix cap 400 w/ union clipping so every emitted row is exact, NO purge gate — numbers not text, the StakePools posture — and no lazy allocation: 102 reads pass check-read-purity). Freeze is free: Stake/Unstake panic post-answer so the record just stops; WithdrawStake deliberately doesn't write it (principal drain ≠ market movement). make realm-test GREEN incl. new checkpoint Trim/WalkDesc tests + stakeseries_test (exact-string grammar pins, no-change-no-storage, 200-epoch trim+routing — hourly answers the week, daily answers below, freeze, pre-upgrade lazy-init). WEB: parseStakeSeries (one bad row distrusts the whole read) + mergeStakeSeries (daily-below-hourly, epoch-seal heights, 0:0 = a real change drawn as a gap) + fetchStakeSeries (+2 qevals, claim route only); signalChart real branch FIRST, both old branches byte-untouched as fallbacks (older deployments/failed reads/series-less demo claims land exactly on the pre-change display incl. the old srcnote, which stays true THERE); step-after M/H/V path + area, lifetime ref kept, trailing band real-mode-dropped, −7d seam hairline + windowed label, the 65% knee when the daily tail exceeds a week, freeze clamp, terminal snap >0.05, records-begin event (suppressed when capped); labels: live "the chain's own record — hourly points for the last week, daily beyond; a flat run means the pools did not change", demo "a sample series in the chain's real form", figcaption "the recorded path · lifetime · now". DEMO exemplars in the chain's exact grammar (one parser both modes): orem/1 lively (daily prefix + dense week, seam, no knee), ledger/1 sparse (knee at Xs=217.3), orem/2 frozen (last change ≤ ansH; flat tail to now) — machine-validated (last row == pools; ascending; freeze bound). EVIDENCE: chart_test REWRITTEN to slice the live functions (the old copy had drifted; its exit-hook even masked a crash as ALL PASS — caught) — 32 pins green incl. the no-L/C step discriminator, knee x=217.3, fallback byte-strings, §7.4 sweep, P8 dataset consistency; all 13 harnesses + d2-d7 probes green (d7 = 19 chart checks incl. per-exemplar geometry + labels + docket/raw non-leakage); wireframes chart figure REGENERATED from the live function (now the recorded path; the synthesized figure is gone from the doc), divs 345/345; screenshots captured (chart_lively/sparse-knee/frozen.png). Independent critic launched (realm+web, incl. the union-clip one-side-capped case and the WalkDesc mutation hazard). | NOT-CLEAN (feature shipped) |
| 91-STOP | 2026-08-17 | LOOP STOPPED by owner order ("finish what you're doing and stop the loop") after round 90 shipped. Wrap-up: the D6-4 critic + D6-5..8 lens (launched minutes earlier) stopped unread — D6-4 closes on its own evidence (d64_probe 10/10 + full regression sweep; the critic's sharpest assigned angle — a staked ballot older than the newest-100 probe window landing votable under the generic hedge — is logged UN-ADJUDICATED for the next session); cron 2a5b919f deleted. FINAL SWEEP at stop: 14 harnesses ALL PASS, 6 probe suites ALL PASS, both pages 200. HANDOFF-WEBSITE.md updated (status STOPPED, open threads, backlog D6-5..8, the un-adjudicated hedge question, Owner-notes pointers). Session arc: rounds 79-90 shipped D2 close (search), D3 (cross-links + orientation), D4 (sort chips + always-caption pager), D5 (disclosure + clocks + the annex), D6-1 (mod-log passage), D6-2 (provenance banners), D6-3 (the chain's policing lists, zero reads), D6-4 (open ballots for holders) — every batch converged via lens panels, implemented, browser-verified, and critic-checked or self-verified per the logged ladder. | STOPPED |
| 90w-1 | 2026-08-17 | WAIT — D6-4 critic launched (rider: round-89 fixes; sharpest attack angle handed to it: a staked ballot OLDER than the newest-100 probe window lands in the VOTABLE list — hedge sufficient or silent lie?); D6-5..8 combined lens launched (elections w/ the never-render-ElectionTally seal rule, answerability from possibly-already-loaded TrailingOI, spendable/locked, folder open-counts zero-read). | WAIT |
| 90 | 2026-08-17 | D6-4 IMPLEMENTED (open ballots for holders on /needs, per the received spec — one combined lens, judge pass mine). READ PATH as specced: per court where CoinBalanceOf>0 (the one new per-court read; allCourtSlugs already enumerates), ONE qrender via listClaims (docket rows classified phaseClass(...).short==="in dispute" — NEVER a bare /disput/ regex, "answered" contains "disputable/undisputed") + a strip-completion pass (strip rows absent from the docket = hidden or beyond the window → 1 ClaimStatus qeval each live, dataset in demo — hidden disputes stay discoverable), stripMore hedged in the caption. EXCLUDED SPLIT: posBySlug captured from the positions probe /needs already runs — staked ballots render under "You are excluded from these · you staked this claim — the rest of the court votes" (the page-sub's own phrase), void pill, same deep link; author/answerer-only conflicts stay votable under the hedge (per-row ClaimAuthor/Answerer reads REFUSED — that pattern is the claim page's). COPY: sealed-means-un-summed + no-close-height + the snapshot hedge ("buying now adds nothing") + realm-refuses-at-signing, assembled ONLY from vetted strings; NO tallies, NO amounts, NO countdowns, NO carrot line (the lens's ruling adopted verbatim: "a reward line beside a live ballot is a vote-buying advertisement"). PLACEMENT: between Against-your-side and With-your-side; empty-state reworked (both lanes empty → "…and no open ballots in the courts you hold"; ballots-only → section renders alone). DEMO: ledger/3 disputed exemplar added (DEMO_ME holds LEDGER, no position → VOTABLE; orem/3 staked → EXCLUDED — both presentations demoable); bond/quorum fields curve-consistent per the spec. EVIDENCE: d64_probe 10/10 in Chromium (section, votable ledger/3, excluded orem/3, sealed discipline, no amounts in rows, snapshot hedge, ?at deep link lands on the ballot, zero errors); ripple-synced: d5_probe claims-filed 13→14, d63_check ledger strip [2,3], record_test census live 2→3 (the exemplar counts live); ALL 14 harness runs + d2-d6 probes green; wireframes D6-4 anno, divs balanced; both pages 200. D6-4 critic next fire (with the round-89 fixes as rider scope). Remaining D6 backlog: 5 (elections), 6 (answerability), 7 (spendable/locked), 8 (folder open-counts). | NOT-CLEAN (batch shipped) |
| 89 | 2026-08-17 | D6-1..3 CLOSED by TWO independent critic verdicts (the "silent" original completed seconds after its dup — both counted; 39-check and 41-tool-use Chromium runs, realm-diffed). CONSENSUS CLEAN: banner wording realm-verbatim (purged/redacted), precedence semantically forced-correct (TextRedacted=global||purged), the four reads fail-silent with zero invented state, no text leak possible live (ClaimTitle chain-gated), total annex containment (no listing/data-q/map/strip leak; the strip showing hidden #5 is realm-sanctioned), /raw gating repels every hostile path with zero RPC on refused slugs, live /raw path shape matches render.gno's "mod" carve-out, I5 wire-verified. MERGED FINDINGS APPLIED (8): F1+F2 MED the demo mod-log SAMPLE taught a fictional format on a page captioned "Exactly what Render() returns" (wrong court's story on annex; invented sections/tails; member list the chain never prints; folder acts that can never appear — folders never appendLog; the charter sentence missing; act rows lacking the I11 row-id handle + actor) → rewritten byte-shaped per renderModLog and PER COURT: orem = threshold + Awaiting approval (`hide:9` — 1 of 2 approved, expires at height …; reasons withheld until an act carries) + acts rows "row N · height H · code · by actor — reason"; annex = its OWN story (hide #3, global-hide:code-2026-041 by the review court, hide #5 — and seeding correctly absent: the realm has no seed act code); ledger = the c.mod-nil 1-of-1 bootstrap case verbatim. F2-orig MED the moderated court's own jump box denied claims it lists ("no claim #5 — this court has 2" beside a strip showing #5) → demo totalClaims counts the whole court; the demo now computes its own hiddenGap ("3 hidden or unreadable claims omitted…"); qCaption demo idle/zero stop certifying "the sample is complete" when loaded<total ("hidden claims are omitted; their pages still answer by id"). F3 LOW hidden claims offered a map link dead-ending in a FALSE nonexistence message → mHidden gates the link. F4 LOW demo raw pages omitted the chain's own moderation lines → demo raw claims now carry the hideBanner blockquote + seeded italic; demo raw courts link their mod log (the chain always does). F5 NIT seeded punctuation aligned to the realm (semicolon). F6 NIT the seeded read failed OPEN (a failed read could offer the author-draw the realm refuses) → fails CLOSED ("unread = never offer the draw"). F8 VERY-LOW tour lead no longer claims "each state" (no purged specimen exists — said so instead). RESIDUALS ACCEPTED + LOGGED: purged-null+redacted-true renders the softer banner (fail-soft, single-node unlikely); demo ballot voteEndsAt (pre-existing, F4-of-round-86 family). REALM-SIDE → Owner notes: HiddenBits per-authority read (the banner is forcibly authority-blind) + the suspension-mask ERRATUM (modrender claims masking moderation.gno never applies). EVIDENCE: all 14 harness runs green (page_test link-count scoped off the new sections; search_test +3 caption-family asserts; d5_test +9); d2-d6 probes + d6_extra ALL PASS after string sync; both pages 200. D6-4 SPEC RECEIVED meanwhile (open ballots for holders: listClaims-parse + strip-completion pass, phaseClass-only classification — never bare /disput/; placement between Against/With; excluded-split from the positions /needs already probes; ledger/3 demo exemplar; carrot copy OMITTED — "a reward line beside a live ballot is a vote-buying advertisement") — implementation NEXT fire. | NOT-CLEAN (8 applied, 2 residual) |
| 88w-5 | 2026-08-17 | WAIT — D6-4 lens pinged at fire 4; D6 critics: one more silent fire → self-verify. | WAIT |
| 88w-4 | 2026-08-17 | WAIT — D6 critics silent (2 more fires to self-verify); D6-4 lens fire 3. | WAIT |
| 88w-3 | 2026-08-17 | WAIT — D6 critic dup post-ping; D6-4 lens fire 2. If BOTH D6 critics stay silent 3 more fires, self-verify D6-1..3 from probe evidence (the D2/D4 precedent). | WAIT |
| 88w-2 | 2026-08-17 | WAIT — D6 critic orig ~11 / dup ~4 (ping the dup); D6-4 lens fire 1. | WAIT |
| 88w-1 | 2026-08-17 | WAIT — D6 critic pair out (orig ~10, dup ~3); D6-4 lens launched (open-ballot discovery for holders on /needs; key questions: cheapest honest read path per court-with-balance, participant-exclusion honesty using positions /needs already knows, sealed-vote copy discipline). | WAIT |
| 88 | 2026-08-17 | D6-3 IMPLEMENTED (the chain's policing lists) — the lens original delivered a definitive spec right after its dup launched (dup stopped unread). THE HEADLINE: zero new reads — the "## Needs review" and "## Awaiting an answer" rows were already inside the render markdown listClaims downloads and DISCARDED (its parser kept only "## Claims"); the parser is now a three-section state machine attaching out.strip/out.pending/(strip|pending)More, with a bare-tail row regex for untagged pending rows and per-section dedupe. PLACEMENT: after Recently settled — the chain's own page order (hoisting the queue above the docket was REJECTED: re-ordering the chain's page would be an overlay editorial act; this is not the refused invented-urgency grouping, it is the chain's published surface, attributed in the sec-h "· the chain's own list"). Both sections: data-qfold (fold during search — a filtered strip is no longer the chain's list, and dupe data-q rows would double-count the search caption), OUTSIDE data-sortable (the deadline order IS the content — "a mill cannot manufacture a past deadline"), page-1 only, fold-when-empty WITHOUT a zero line (the chain omits empty sections, so absent-vs-empty is indistinguishable — never certify), overflow footer honestly scoped ("…and K more — the chain's page shows the nearest 50" linking #/raw). Rows: id + verbatim title + the chain's own "settles at height N" sentence + a status pill ONLY when this page's parse already knows it (hidden claims render pill-less, zero reads). Dupes with the Open docket KEPT (the chain lists both). DEMO: demoStrips() one derivation for listClaims-demo AND demoRender (raw view stays faithful; the parser is exercisable offline); membership mirrors strips.gno (enter at answer, exit at OPEN THE REWARDS not settle — settled-unrewardsOpened stays flaggable); settleAt stamps added to orem/3 (NOW−110k), orem/7 (NOW−250k), ledger/2 (NOW−130k) — parity, not invention (live claimDetail reads SettleDeadline on all answered) — orem strip = [7,3,6,2], ledger = [2]; NEW annex/5 (hidden AND answered, settleAt NOW+4k): absent from the docket, present on the strip — the load-bearing property demonstrable offline; annex pending = [seeded 2]. "Awaiting an answer" SHIPPED (not redundant: pending also ignores hide bits — a hidden seeded claim appears NOWHERE else). EVIDENCE: d63_check unit ALL PASS (orders + rewardsOpened-off + pending sets); d6_probe 24/24 (strip renders in order — one probe selector scoped off the folders section which shares data-qfold; folds during search; never sortable; annex #5 on the strip but not the docket; raw view carries both sections); d2-d5 probes ALL PASS; 14 harness runs green (page_test gained a demoStrips stub; d5_test +8 asserts incl. annex now FIVE claims); gen_court passes the strips so Screen 3 mirrors the real page (regenerated, divs 343/343); wireframes anno added. D6-3 verification rides the outstanding D6 critic pair. | NOT-CLEAN (batch shipped) |
| 87w-9 | 2026-08-17 | WAIT — four out: D6 critic orig 9 / dup 2; D6-3 lens orig 8 / dup 2. | WAIT |
| 87w-8 | 2026-08-17 | WAIT — D6-3 lens duplicate launched at fire 7 (late original counts). Out: D6-1+2 critic orig 8 / dup 1; D6-3 lens orig 7 / dup 1. | WAIT |
| 87w-7 | 2026-08-17 | WAIT — D6-1+2 critic duplicate launched at fire 7 (late original counts); D6-3 lens fire 6 (duplicate next fire). | WAIT |
| 87w-6 | 2026-08-17 | WAIT — D6-1+2 critic fire 6 (duplicate next fire); D6-3 lens fire 5 post-ping. | WAIT |
| 87w-5 | 2026-08-17 | WAIT — D6-3 lens pinged at fire 4; D6-1+2 critic fire 5 post-ping. | WAIT |
| 87w-4 | 2026-08-17 | WAIT — D6-1+2 critic pinged at fire 4; D6-3 lens fire 3. | WAIT |
| 87w-3 | 2026-08-17 | WAIT — D6-1+2 critic fire 3; D6-3 lens fire 2. | WAIT |
| 87w-2 | 2026-08-17 | WAIT — D6-1+2 critic fire 2; D6-3 lens fire 1. | WAIT |
| 87w-1 | 2026-08-17 | WAIT — D6-1+2 critic fire 1; D6-3 lens launched (the chain's own needs-review strip; key question: parse the render section the overlay ALREADY downloads and skips = zero new reads, vs StripPage qevals; hidden-but-flaggable claims must NOT be filtered here — the chain includes them while flaggable, that is the strip's whole point). | WAIT |
| 87 | 2026-08-17 | D6-2 IMPLEMENTED (moderation provenance banners). Lens original delivered right at its dup threshold (dup stopped unread); spec adopted nearly whole: FOUR boolean reads (HiddenFromListing/TextRedacted/ClaimPurged/ClaimSeeded — purged is NOT derivable from the title prefix, the spoofable-prefix rule) on the claim route only (+4 qevals vs its ~23), Promise.all, null-safe, only ===true renders (the page makes no completeness certificate, so failure = absence, per the opened-by precedent); modBanner() one .notice.void register with the CHAIN'S OWN wording, precedence mirroring modrender.gno's switch (purged › redacted › hidden), each branch linking the D6-1 mod log ("who and why —"); the plain-hide banner says "De-listed by moderation" NOT "by this court's moderators" because no read separates court from meta authority (HiddenBits per-bit read → wishlist); "reachable by direct link — you are on that link now" addresses exactly the shared-link visitor the gap named; seeded = a tagrow small-muted span (the chain renders it a register lighter than the blockquote) keeping the realm's verbatim "the author earns nothing from it"; the tombstone is NEVER re-rendered by the banner (the h1 carries it through safeInline). DEMO: orem untouched (hiding an orem claim would desync docket-11/tour/census/map/folders in one move — lens argument adopted); the annex hosts the specimens — annex/2 seeded, annex/3 hidden, annex/4 hidden+redacted with the title stored AS the gated "[text withheld]" literal (the chain never serves raw text either); purged exemplar CUT (one bold lead apart from redacted; additive later); demo listClaims + demoRender now omit hidden claims exactly as render.gno:136 does; the about tour gained a 3-row "Moderation specimens" docket (additive; the 7-row block byte-unchanged). RIDER (lens-found adjacent defect, applied): live claimables tests !d.seeded but live claimDetail never read the bit — /me could offer an AuthorBonus the realm refuses at signing (I5) → meDetail's rewardsOpened branch now reads ClaimSeeded (+1 qeval, bounded). EVIDENCE: d6_probe 18/18 (banners on 3/4 with precedence — redacted BEATS hidden on annex/4; gated title verbatim; seeded span not a banner; annex docket omits 3,4; ordinary claims bannerless; tour section; D6-1 checks still green); d2/d4/d5 probes ALL PASS; d3_probe synced (the tour assert scoped to the first docket + a specimens assert) then ALL PASS; 13 harnesses green (d5_test: annex now 4 claims + 11 new D6-2 asserts incl. precedence-order and the me rider); wireframes anno added, divs 343/343. D6-1+D6-2 critic launched (realm-verification mandate: banner wording vs modrender.gno, the live /raw path shape, annex containment). | NOT-CLEAN (batch shipped) |
| 86w-1 | 2026-08-17 | WAIT — D6-2 lens fire ~6 post-ping (duplicate next fire if silent). | WAIT |
| 86 | 2026-08-17 | D5 CLOSED by critic verdict (the ORIGINAL delivered late — late deliveries count; the dup stopped mid-run). Verdict quality: realm-verified — annex containment total (no DOM/data-q/caption/tour/map leakage; /me still money-scans it and surfaces nothing); hidden-count honesty byte-checked in all FOUR variants incl. the zero-but-total-failed certificate refusal; slug charset realm-enforced [a-z0-9-]{1,11} so the count parse has no name-shaped attack surface; clocks hand-recomputed against the dataset AND the realm (session.gno:14: SettleUndisputed gates only frozen/undisputed/unsettled/aged — an open FLAG does not block settling, so orem/6's "anyone may settle it now" is realm-true); opened-by XSS-inert; answer record fired once, nonzero-only; zero page errors. FOUR findings, three APPLIED: F1 MED the short-window note branded READ FAILURES as moderation (listClaimsPage fails closed on HiddenFromListing errors, the arithmetic counted them, and the note asserted "de-listed by moderation" for rows never read — a direct never-guess breach on a flaky node) → the note now hedges in the folder page's own idiom: "K hidden or unreadable claims omitted from this window — a hidden claim's page still answers by id"; F2 MED demo claimDetail never set statusText so every /me position pill read "open" (undefined→phaseClass default; live unaffected; me_test had no coverage) → demo branch now carries statusText: statusText(d), matching the SAME-shapes contract, + me_test assertion; F3 LOW clock/spark fills + the settle re-apply could write into a NEWER render (floating .then keyed by element id; seq guards covered paints, not fills) → fills capture seq0=renderSeq and no-op when stale (3 guards). F4 LOW ACCEPTED as spec-inherent residual: the demo docket's sealed line sits one click from the demo ballot's dataset voteEndsAt ("vote closes ≈block …") — live cannot diverge (no close-height read exists); the register states the realm's rule; logged, not fixed (the demo ballot teaches the close-height concept and predates D5). Critic observations (no action): p3 pager ordinals over a shortened window reconcile via the note; the maybe-hedge wording stays; directory idle-search total omits moreFeatured (matters only past 50 featured). All 13 harnesses green post-fix (d5_test synced to the hedged note + 2 new asserts); d5_probe re-run ALL PASS. | NOT-CLEAN (3 applied, 1 residual) |
| 85w-6 | 2026-08-17 | WAIT — D5 dup + D6-2 lens fire 5 post-ping. | WAIT |
| 85w-5 | 2026-08-17 | WAIT — D5 dup + D6-2 lens both pinged at fire 4. | WAIT |
| 85w-4 | 2026-08-17 | WAIT — D5 dup fire 3; D6-2 lens fire 3 (pings at 4). | WAIT |
| 85w-3 | 2026-08-17 | WAIT — D5 dup fire 2; D6-2 lens fire 2. | WAIT |
| 85w-2 | 2026-08-17 | WAIT — D5 orig critic (long-silent), D5 dup fire 1, D6-2 lens fire 1. | WAIT |
| 85w-1 | 2026-08-17 | WAIT — D5 critic duplicate launched at its threshold (original silent past ping; late original counts); D6-2 combined lens launched (ruling: ONE design+impl lens for an S-size item with realm wording available — proportionality; the judge pass is mine). | WAIT |
| 85 | 2026-08-17 | D6-1 SHIPPED (the moderation-log passage, per the gap review's DO-FIRST). The /raw route regex now accepts /mod (numeric ids unaffected), the crumb names it "Moderation log", the court tagrow and the curate page both link "moderation log →", and demoRender gained a deterministic sample mirroring renderModLog's shape (mod set, 2-of-3 threshold, suspension state, one in-flight proposal with approvals + the 7-day-expiry sentence, acts log with reasons). What it unlocks: who governs a court, what is half-approved and when it lapses, and the full acts log — previously a sealed room (#/raw/orem/mod was "No such page"). EVIDENCE: d6_probe.py 9/9 in Chromium (log renders w/ threshold + in-flight + acts; crumb; both links present and land; numeric raw unaffected; zero errors); 13 harnesses PASS (+3 D6-1 asserts in d5_test); Screen 3 regenerated (tagrow gained the link), divs 343/343; wireframes anno added. Verification note: D6-1 is chain-render passthrough — the probe covers the overlay's part; the content is the realm's own render. D6-1 rides the next fresh critic for independent eyes. NEXT: D6-2 (claim-page moderation provenance banners) gets the full converge treatment. | NOT-CLEAN (1 shipped) |
| 84w-2 | 2026-08-17 | D6 GAP REVIEW delivered (full file + realm read surface + 3-persona Chromium walk). Central finding: the realm EXPOSES discovery surfaces the overlay never touched — parity, not invention. RANKED BACKLOG D6-1..8: (1) moderation-log passage — #/raw/:slug/mod 404s today (route regex numeric-only) while the chain's court render links its own mod log (mod set, m-of-n threshold, suspension, IN-FLIGHT proposals w/ approvals + 7-day expiry, acts log) — S, 1 qrender on demand; (2) claim-page moderation provenance banners (HiddenFromListing/ClaimSeeded/TextRedacted — a shared link to a de-listed claim shows an ordinary page today; chain wording exists) — S, +2-3 qevals; (3) court-page "Needs review" = the realm's own policing strip (StripPage; "a claim nobody discovers in time is a claim nobody flags"; the chain's OWN deadline order + caption, distinct from the refused invented-urgency grouping) — M; (4) /needs: open ballots in courts where the HOLDER has coin (voter discovery is load-bearing — quorum failure burns bonds; stakers are barred, holders never signaled) — M, bounded by courts-with-balance; (5) election discovery (ElectionOpen/Windows/Candidates on /needs + curate; NEVER ElectionTally mid-vote — copy-the-whale, same seal rationale) — S-M; (6) open-claim answerability honesty (TrailingOI maturity → the realm's own refusal words; floor/timeout NOT readable → wishlist OpenedAt/EffMinAnswerX/AnswerableNow) — S; (7) /me spendable vs locked (LockedOf/SpendableOf split exists) — S, +2/court; (8) folder rows open-now counts from loaded rows, zero reads. RULING: D6-1 implemented THIS FIRE directly from the reviewer's spec — it is chain-render passthrough (route regex + two links + demo mirror), no design freedom to converge over; D6-2+ get the full converge treatment. | WAIT→WORK |
| 84w-1 | 2026-08-17 | WAIT — D5 critic + D6 gap reviewer out, both post-ping (duplicates at ~7 from launch: D5 critic next fire, gap reviewer +2). | WAIT |
| 84 | 2026-08-17 | D4 CLOSED by self-verification (the logged fallback: the original critic died on an API error mid-run — its unofficial last note was "Everything passes now" — and the duplicate went silent through ping+deadline; stopped). Evidence of record: d4_probe.py 22/22 + NEW d4_extra.py 6/6 covering the critic-only angles (keyboard chip activation via Enter + aria-pressed sync; data-yes attributes clean floats, no NaN/undefined leaks on series-less rows; caption byte-restored through sort→search→clear→default cycles — the cap0 stash holds; back/forward coherent after replaceState chip clicks — no phantom history entries; zero page errors); page_test 13 D4 assertions incl. at/focus-never-carried + escaped-ampersand sort-carry + bottom-pager-nav-only; d2 15/15 + d3 27/27 regression-clean at ship time and re-run green after D5; all 13 harnesses green. AMENDMENT same fire: the stopped duplicate's dying note named ONE real finding — the fill-completion applySort re-apply (and any chip click) BLURRED a keyboard-focused docket row to body (appendChild detaches-and-reinserts; reproduced in Chromium: afterSort activeElement=BODY) → applySort now captures a focused row inside its boxes and re-focuses it preventScroll after the reorder (re-verified: focus survives sort and restore); d5_test gained the assertion; d4_probe + d4_extra re-run green. | NOT-CLEAN (self-verified close + 1 dup finding applied) |
| 83w-6 | 2026-08-17 | WAIT — gap reviewer pinged; D4 dup fire ~7 post-ping (self-verify next fire if still silent); D5 critic post-ping. | WAIT |
| 83w-5 | 2026-08-17 | WAIT — D4 dup post-ping fire ~6 (duplicate-of-dup NOT planned: it is already the redundancy; if silent 2 more fires, self-verify D4 from probe evidence per the D2 precedent); D5 critic post-ping; gap reviewer fire 4 (ping next fire). | WAIT |
| 83w-4 | 2026-08-17 | WAIT — D4 dup post-ping; D5 critic fire 4 (ping if silent next fire); gap reviewer fire 3. | WAIT |
| 83w-3 | 2026-08-17 | WAIT — D4 dup pinged (~fire 4; told the D5 batch landed since, scope stays D4); D5 critic fire 3; gap reviewer fire 2. | WAIT |
| 83w-2 | 2026-08-17 | WAIT — three agents out: D4 dup critic, D5 critic, D6 gap reviewer. | WAIT |
| 83w-1 | 2026-08-17 | WAIT — D4 dup critic + D5 critic out. Backlog empty per shipped D3-D5, so a D6 discovery-gap reviewer launched (three-persona browser walk: shared-link first-timer, returning staker, moderator; ships-refusals lists included so it cannot re-propose settled matters). | WAIT |
| 83 | 2026-08-17 | D5 IMPLEMENTED (disclosure + clocks). CONVERGENCE: impl lens delivered first; the design lens landed LATE (late originals count) and OVERTURNED two in-flight judge calls with better rulings, both adopted: (r1) the hidden-court count IS honestly readable — ListByTier(0) length-only, slugs never rendered (precedent: allCourtSlugs already fetches tier-0 for the /me money scan; the render's omission is about LISTING, not about the page knowing a number) — replacing the no-number sentence; zero reads as a CERTIFICATE ("no courts are hidden by moderation — this register is complete"), read-failure and zero-but-total-unread get their own honest lines; CountByTier stays wishlisted as the cleaner surface. (r2) disputed docket rows RENDER the sealed-vote line in both modes (zero reads, states the realm's own rule on the row) instead of showing nothing. Also per design lens: (r3) full .grid.stats office-door strip (courts listed / featured / claims filed over loaded rows — money sums excluded by name) instead of my interim one-liner; (r4) DEMO gained the hidden exemplar court ANNEX (tier 0, one open claim, curve-consistent stats) so the ruling is demonstrable offline — demo listCourts + demoRender now mirror the render's tier-0 omission (demoRender's Courts loop was tier!==2 and WOULD have leaked it — caught), allCourtSlugs still returns all keys; #/c/annex shows the Hidden pill + "unlisted — reachable by direct link only"; (r5) opened-by chip SHIPPED over the impl lean-to-cut ("a file that does not say who opened it is not a record") — +1 qeval claim-route-only, never in claimDetail (would tax every /me scan), all 14 demo claims backfilled with authors; (r6) /me answer record appended to court-block headers ONLY when nonzero (the read cannot distinguish never-answered from overturned — printing 0 would imply it can) + one footnote; demo abstains; (r7) rail gains the one exit it lacked — "this realm on gnoweb →", live-only, toggled by the existing mode-dot updater. Clocks per the merged spec: clockLine() one vocabulary for both modes; answered→settle block, settling→reopen block, disputed→sealed always, open/settled→none; live +1 qeval only on frozen rows inside the existing spark fill + one /status; demo at build vs NOW (exemplars: orem/2 ahead, orem/6 passed, orem/3 sealed, orem/7 reopenable, ledger/2 finalizable — all browser-verified). Court docket short-window disclosure (live arithmetic, count-only): "K claims are de-listed by moderation in this window — their pages still answer by id". CUTS logged: countdown color-coding (a registry, not a trading screen), authors on docket rows, seeded annotation, close-height estimates for disputed, /me bounded-scan history (holes presented as a record — refused), height stamp in the rail. EVIDENCE: d5_probe.py 19/19 (annex absent from the register + counted + direct-link opens with pill/line; all five clock exemplars byte-checked; opener chip; no fabricated /me record in demo; gnoweb exit hidden in demo); d2 15/15, d3 27/27, d4 22/22 regression-clean; 13 harnesses PASS (new d5_test: clockLine table, annex integrity, every-demo-claim-has-an-opener, §7.4 sweep); Screen 3 regenerated (rows now carry clock lines), divs 343/343; D5 wireframes anno added; Owner notes gained the D5 wishlist (CountByTier, AnsweredBy/history, ClaimDeadline). D4 critic original DIED on an API error mid-run (its last note: "Everything passes now" — not counted as a verdict); its duplicate still out and remains the D4 verdict of record. D5 critic launched. | NOT-CLEAN (batch shipped) |
| 82w-7 | 2026-08-17 | WAIT — D4 critic duplicate launched at fire 7 (late original counts). D5: impl spec in hand; design lens fire 6 post-ping (duplicate next fire if silent). | WAIT |
| 82w-6b | 2026-08-17 | D5 IMPL LENS delivered (evidence-dense). (1) STRIP: featured total = parsed + moreFeatured footer (exact, free); listed total ALREADY read via ListByTier(1) w/ dirTotFailed hedge; NO CountByTier exists — the only hidden-count source is ListByTier(0) which would pull hidden slugs into the listing surface the render deliberately omits them from (allCourtSlugs() already calls it but that is the personal money-scan, a different justification) → disclosure sentence WITHOUT a number ("Courts hidden by moderation are not listed and not counted here") + CountByTier(tier) realm wishlist. (2) DEMO has NO tier-0 court (orem=2, ledger=1; orem/4 tier:2 is claim QUALITY, unrelated) and a hidden one would be invisible by definition → don't add; docket short-page disclosure sentence at the pager ("Moderators can de-list claims; hidden claims are omitted and a page may run short"), demo hidden claim CUT (would desync every sample count); folder page already discloses ("N hidden or unreadable rows are omitted") = house style. (3) CLOCKS cost table: claimDetail = 13-25 qevals/claim → 325-625/page REJECT; demo-only REJECT (violates "Both branches return the SAME shapes"); DISPUTED rows have NO readable clock anywhere (close height unexposed — the file already says so twice); RECOMMENDED: extend the spark fill +1 qeval on answered→SettleDeadline / provisional→EscrowUntil rows only (typically 0-5/page) + one /status for chainHeight (court route lacks it); dispute rows never render a clock in EITHER mode so shapes stay identical (precedent: demo carries voteEndsAt live cannot read, disputeTicket already splits honestly); skip = defensible fallback (/needs already serves the personal case). (4) OPENED-BY: claimDetail does NOT return author; ClaimAuthor read exists (2 sites); +1 qeval claim-page-only; shortAddr confirmed hardened (esc inside, L808); lens leans CUT as pure decoration — judge call. (5) /me ANSWER RECORD: AnswerRecord(slug,addr) is an address-keyed per-court scalar ALREADY read at one site ("contested-and-upheld record") → ship +1 qeval/court on /me; the LIST of answered claims has no realm surface → wishlist AnsweredBy(slug,addr)→[]uint64. (6) CHAINFOOT does not exist in the file (grep 0); rail nav already carries every route, sticky on desktop → CUT (a provenance line would duplicate the rail's node block + add a /status poll for decoration). Await design lens (pinged) → judge. | WAIT |
| 82w-6 | 2026-08-17 | WAIT — D4 critic fire 6 (duplicate next fire); D5 lenses fire 5 post-ping. | WAIT |
| 82w-5 | 2026-08-17 | WAIT — D5 lenses pinged at their fire 4; D4 critic fire 5 post-ping (duplicate at 7). | WAIT |
| 82w-4 | 2026-08-17 | WAIT — D4 critic pinged at its fire 4; D5 lenses fire 3. | WAIT |
| 82w-3 | 2026-08-17 | WAIT — D4 critic fire 3; D5 lenses fire 2. | WAIT |
| 82w-2 | 2026-08-17 | WAIT — D4 critic fire 2; D5 lenses fire 1. | WAIT |
| 82w-1 | 2026-08-17 | WAIT — D4 critic out (fire 1). D5 converge panel launched (2 lenses: disclosure/clocks design; reads-cost impl — key questions: an honest hidden-court COUNT source without exposing hidden slugs, docket clocks vs the demo-mirrors-live principle, whether /me answer-record has any read at all or goes to the wishlist). | WAIT |
| 82 | 2026-08-17 | D4 IMPLEMENTED (sort chips + always-caption pager). CONVERGENCE: both lens originals delivered (impl v2 confirmed the shipped core post-hoc); dups stopped unread. Judge rulings: chips live INSIDE the pager row (one order-statement per listing — design lens); ?sort QP via replaceState + applySort DOM appendChild reorder scoped to [data-sortable] opt-in (impl lens — the fills live only in the DOM, so re-render-with-comparator would repaint empty spark/price cells; the opt-in keeps the specimen tour/argument/needs/folder order untouchable); docket chips {newest·default, oldest, %YES now} on EVERY window (impl's "most staked" CUT — sorting by an invisible number; design's "oldest-open" cut stands but plain oldest kept — no false discovery promise); directory chips {GNOT burned·default, busiest} PAGE-1-ONLY, nav pills hidden while busiest (a busiest page-2 does not exist on chain); "name A-Z" CUT (D2 search covers it); grouping CUT (per both lenses: /needs owns urgency, it is personal); one settle-re-apply when the spark fill completes (impl: rows must not jump under the cursor per chunk); sort restore runs BEFORE the ?q re-apply (the search caption reads QCTX.orderTail). PAGER FIX: caption always renders ("all 11 claims · newest first", singular at 1, hedge "— the full count did not read" when ListByTier failed), pnav pills only when actionable, empty ledgers render nothing, links carry {sort,p} ONLY (q mutates live; at/focus never belong on a listing), bottom docket pager nav-only-when-actionable (no double captions). Keys: data-id/data-yes stamped at build (demo series) or by the spark fill per chunk (live), data-rank/data-claims on directory cards; keyless rows sink stable; caption tails swapped via cap0 stash ("stakes, never ballots" walls off sealed-tally misreading — no tally read EXISTS in the file). EVIDENCE: d4_probe.py 22/22 in Chromium (chips render/sort/press; %YES desc with keyless sink; caption patch+restore; ?sort URL round-trip incl. cold-load restore + unknown-key fallback; search caption carries the active order; tour/folder/directory-demo correctly chip-free; folder finally states "curated order"; ?at coexists; zero page errors) — two initial probe FAILs were probe bugs (selectors concatenated both sortable sections); d2_probe 15/15 + d3_probe 27/27 regression-clean; all 12 harnesses PASS (page_test synced to the new pager contract incl. escaped-ampersand sort-carry, empty-ledger, singular, at/focus-never-carried). Wireframes: Screen 3 regenerated+spliced (rows now carry data-id/data-yes, dockets data-sortable), divs 343/343, D4 order-and-sorting anno added. D4 critic launched. | NOT-CLEAN (batch shipped) |
| 81 | 2026-08-17 | D3 CLOSED by critic verdict: "the batch holds under attack — nothing invents, leaks a tally, or breaks escaping" (full read + 52 Chromium checks + realm sources cross-checked for the tour captions: claim.gno:327,350 dead→deposit refunds/fee burns; dispute.gno:14,383 provClose→deposit AND fee refund). Zero HIGH. All 8 findings addressed: F1 404s now carry a focusable .page-h (notFound gains the heading; the GLOBAL 404 now renders crumbs so the orientation line + focus contract hold on every route class) APPLIED; F2 malformed ?focus (0/007/1e3/XSS-shaped) no longer silently swallowed — zero-pads NORMALIZE (007→7), everything else gets an honest note that echoes nothing APPLIED; F3 >2^53 ids can no longer print a wrong number (15-digit cap, no echo) APPLIED; F4 specimen #7 says "two failed dispute rounds" (round:2 in the dataset) APPLIED; F5 the chip's curation source is visible text "· sample"/"· local", not hover-only title APPLIED; F6 dismiss aria-label no longer promises permanence blocked storage cannot keep ("Dismiss this note") APPLIED; F7 live HiddenFromListing claim inside the id-window gets a map link that lands on the miss note — ACCEPTED as residual (the gate cannot know hiddenness without the sweep the chip ruling already cut; the miss note is honest) — logged here as the D3 ruling of record; F8 chart_test now prints ALL PASS/FAILURES truthfully (BAD-pattern detector + exit code) APPLIED. d3_test extended to 6 new assertions covering the fixes; late dup critic + late D4 merged dup stopped unread (originals delivered first — first-delivery-wins). | NOT-CLEAN (7 applied, 1 residual) |
| 80w-10 | 2026-08-17 | WAIT — D4 design spec in hand; still out: D4 impl orig 9 / merged dup 3, D3 critic orig 10 / dup 3. | WAIT |
| 80w-9b | 2026-08-17 | D4 DESIGN LENS delivered (full read, 3247 lines). Core frame: ONE order-statement per listing — sort chips live INSIDE the pager row (right chips group, before newer/older), caption always reflects the active sort; state = ?sort= QP via replaceState, DOM reorder keyed on stamped data-* attrs, stable ties, keyless rows last, chips only when ≥2 rows. Search interplay: .qactive hides the bar; QCTX.orderTail must carry the active sort into the search caption. FOLDER PAGES: never chips (curated order is the realm's). DIRECTORY: chips page-1-only (deep windows are bare slugs until the stats fill; "busiest of ranks 26-50" is a trivium) = [GNOT burned (default) | busiest (claims count, sync-loaded)]; nav pills HIDE while busiest active (a busiest page-2 does not exist on chain); CUT largest (holders unreadable; supply≈burn duplicate) + newest (no age read exists — belongs server-side if ever). DOCKET: chips on EVERY page (windows are coherent id-ranges, fill runs on all) = [newest (default) | most staked | %YES staked] keyed off the spark fill's pools (demo: stamp at render; live: stamp per chunk + re-apply active sort; "still reading N" caption tail while unkeyed; failed reads keyless-last); §7.4: keys are the same staking signal rows already display, no tally read EXISTS to misuse, caption closes "stakes, never ballots"; CUT oldest-open (window holds newest ids — the chip could not honor its label; older› already walks there). GROUPING: CUT (pills already mark states; a needs-attention section urges rather than records, reshuffles between visits, duplicates /needs which is correctly personal). PAGER FIX (exact code in spec): always emit caption ("all N claims · newest first" when one page), nav pills only when actionable, empty-ledger returns "", singular label at total==1, ?sort carried in links; bottom courtBody pager only when nav exists (no double caption); dirPager total-read-failed hedge caption. Flagged adjacent defect (not this batch): live directory p1 renders up to 50 while p2 fetches offset 25 — ranks 26-50 can appear on both. Await impl lens/dup + judge. | WAIT |
| 80w-9 | 2026-08-17 | WAIT — five agents out: D3 critic orig 9 / dup 2; D4 lenses 8 / merged dup 2. | WAIT |
| 80w-8 | 2026-08-17 | WAIT — D4 duplicate launched at lens fire 7 (ruling: ONE merged design+impl dup, D3 precedent; late originals count). Out: D3 critic orig fire 8 + dup fire 1; D4 lens pair fire 7 + merged dup fire 1. | WAIT |
| 80w-7 | 2026-08-17 | WAIT — D3 critic duplicate launched at fire 7 (late original counts); D4 lenses fire 6 (duplicates next fire). | WAIT |
| 80w-6 | 2026-08-17 | WAIT — D3 critic fire 6 (duplicate next fire); D4 lenses fire 5 post-ping. | WAIT |
| 80w-5 | 2026-08-17 | WAIT — D4 lenses pinged at their fire 4; D3 critic fire 5 (pinged last fire, duplicate at 7). | WAIT |
| 80w-4 | 2026-08-17 | WAIT — D3 critic pinged at its fire 4; D4 lenses fire 3. | WAIT |
| 80w-3 | 2026-08-17 | WAIT — D3 critic fire 3; D4 lenses fire 2. Pings at critic-4 / lens-4 respectively. | WAIT |
| 80w-2 | 2026-08-17 | WAIT — D3 critic fire 2; D4 lens pair fire 1. | WAIT |
| 80w-1 | 2026-08-17 | WAIT — D3 critic out (fire 1). D4 converge panel launched in parallel (2 lenses: sorting/grouping design incl. the honest page-window captioning question + §7.4 sealed-tally guard; impl-integrity incl. QP-vs-ephemeral sort state, .offp/search interplay, pagerHtml always-caption fix, D3-param interactions). Judge on delivery. | WAIT |
| 80 | 2026-08-17 | D3 IMPLEMENTED (cross-links + orientation). CONVERGENCE: all three lens ORIGINALS delivered (two double-delivered refined v2s after the ping; both dups stopped unread per first-delivery-wins). Judge rulings (inline, logged): (r1) claim folder chip reads local/sample curation ONLY — the live chain sweep (1+2F ≤201 qevals on the most-visited route) is CUT per impl lens; silent absence, never "not filed" (§7.4: a chip must assert nothing unread); realm wishlist ClaimFolders(slug,claimID)→Owner notes. (r2) argument→map link CUT per nav-v2: byte-duplicate of the tagrow link on the same page. (r3) "on the map →" gated live to the drawn window (id>ClaimCount−50) per impl-v2 — no link that dead-ends; the ?focus miss note still covers shared stale URLs. (r4) ?at=resolution (nav's param name) with impl-v2 mechanics: AT_OK allowlist + atTarget() getElementById (QP never a selector), scroll/focus lives in the NAVIGATION handlers so the 7s post-tx re-render can never yank scroll; both resolutionSection returns carry id="resolution" tabindex="-1". (r5) map focus ring = .focused class re-applied inside put() (survives mode toggles; no SVG geometry change) + camera center + zoom from inverting the LOD line (z=min(8,max(1,9·fitW/(fs·clientW)))) + a.focus(). (r6) tour = orientation-v2: h2 "Specimen files — one claim in each state" inside .prose after the overlay section, 7 docket rows (open=1, answered=2, disputed=3, provisional=7, provClose=5, settled=4, dead=8; titles+pills from DEMO at render = drift-proof; no finalize-ready row — only ledger/2 has it; no flag row — quality lane, not a lifecycle state); LIVE replaces the whole section with a switch-to-Demo line (sample ids must never point at a real chain). (r7) orientation = impl-v2 crumbs-level line (ONE function, crumbs is already flex) + a dismiss × the impl version lacked; skips directory (full strip) + about (destination); one delegated [data-introdismiss] handler retires both surfaces via the single cc.intro key; per-route wiring deleted. BUG FOUND BY OWN PROBE: in-app hashchange stole the map node's landed focus via the .page-h fallback → handler now yields when the route already placed focus inside #main (innerHTML swaps blur to body, so ordinary navigation still gets page-h). Also folderMeta now returns {label,path} (chain fid wins over index; docketRow renders .label — Screen 3 gen output byte-identical, diff-verified). EVIDENCE: d3_probe.py 26/26 in Chromium (chip renders/absents/navigates; ?at scrolls+focuses on cold load AND hashchange; needs rows carry it; ring+camera+focus land, ring survives ids-toggle, miss notes both modes, no ring on miss; tour 7 rows ladder-ordered with real pills; orientation line on deep links only, dismissal kills both surfaces once, one key); d3_test.js NEW harness ALL PASS (tour ids truth-checked against DEMO phases, §7.4 sweep, all plumbing asserts); all 12 harnesses PASS (folders_test updated for {label,path} + nested dot-path + chain-fid tests); node --check clean; both pages 200. Wireframes: map anno gains ?focus behavior; D3 cross-links paragraph added; Screen 3 regen diffed byte-identical (no drift); divs 343/343. Fresh D3 critic launched. | NOT-CLEAN (batch shipped) |
| 79w-7b | 2026-08-17 | Nav-scent ORIGINAL also delivered (line-anchored). Items 1-5 mechanics: (1) folder chip in claim tagrow between verdictBanner and raw-link, label = folderMeta's "parent · child" + source suffix (sample/local/chain-title), first-wins on chain multi-membership + unlinked "also filed in N", needs path-aware folderPathOf sibling; LIVE: claim route does NOT fetch folders — fill post-paint via empty span + chainFolders (1+2F, cap 100) in the existing fillCourtRecord idiom; no-folder/failed-read = render NOTHING (chip is enhancement, never "read failed"). (2) claim→map link "map →" href #/c/slug/map?focus=id, same label as court tagrow. (3) argument section closing p gains "drawn on the map →" (section only exists when relations exist, so never edgeless). (4) #resolution deep link: literal hash fragment BREAKS the router — use QP: ?at=resolution; id="resolution" tabindex="-1" on BOTH resolution returns (closed-phase early return AND general), focus() post-paint + hashchange handler prefers QP.at over .page-h (else scroll yanks to top); needs rows get "resolution →" appended to meta (title link stays claim-top — CUT ruling), urgentBox title becomes the link; FREE reuse on /me pull-now + since-last-here rows. (5) map QP.focus (\d+ else silent): after put(), center cx,cy on node, z=min(4,max(1.5,fit.w/960)), then .mnode-a focus() — programmatic focus reuses dim() + :focus stroke, NO new highlight state; miss copy live "claim #N is not drawn on this map — it shows the newest 50." + open-claim link, demo "no claim #99 on this map." (6/7) mostly converges with orientation lens. JUDGE DOCKET (disagreements): tour placement (orientation: BETWEEN answers-h2 and overlay-h2; nav: AFTER overlay para + hr), tour size (orientation: 7 rows incl dead=8 + flag pointer line; nav: 6 rows, dead/flag CUT with orem/8 as candidate 7th), heading ("Specimen claims — one in every state" vs sec-h "See each state"), live-mode tour (orientation: replace section w/ one line; nav: unlinked rows + line + "Browse the courts →" CTA), slim-bar insertion (orientation: after .crumbs w/ data-orientdismiss; nav: main afterbegin BEFORE crumbs, "Got it" button), slim copy ("First time here? How this office works →" vs "New here? Four things before your first button — how it works →"). Nav CUTS also: no ?at on title links, no second map link on rows, no link on "also filed in N". Realm wishlist: ClaimFolders(slug,claimID) reverse read → 1-2 qevals vs 1+2F. Impl lens still out (orig pinged + dup fresh) — judge next fire regardless, two lenses suffice if it stays silent. | WAIT |
| 79w-7 | 2026-08-17 | Fire 7: duplicates launched (ruling: TWO not three — one merged design lens nav-scent+orientation, one impl-integrity; late originals count). Then the ORIGINAL orientation lens delivered a full spec (verified against DEMO): state→exemplar enumeration open=1, answered-in-window=2, flag-vote-sealed=6, disputed-sealed=3, provisional/escrow=7, provClose-no-decision=5, settled-by-vote=4, dead-unanswered=8, NO terminal junk exemplar exists (pointer line covers #6 instead); about-page "Specimen claims — one in every state" h2 between the answers section and "The overlay is optional", docket-pattern rows w/ statusPill, titles read from DEMO at render (drift-proof), DEMO-ONLY (live mode renders a switch-to-demo line; modesel already re-renders); CTA plain .btn not .primary (primary = money only); orientation option 2 chosen: slim one-liner "First time here? How this office works →" + dismiss, injected ONCE in render() after .crumbs on all non-directory non-about routes, same cc.intro key both surfaces, delegated data-orientdismiss handler folding in the existing #introdismiss. §7.4-audited captions incl. "withdraws 1×"/"by vote" route naming. Await nav-scent + impl lenses, then judge. | WAIT |
| 79w-6 | 2026-08-17 | WAIT — D3 lens panel still out. Fires: 6 (duplicate next fire). | WAIT |
| 79w-5 | 2026-08-17 | WAIT — D3 lens panel pinged last fire, still out. Fires: 5 (duplicate at 7). | WAIT |
| 79w-4 | 2026-08-17 | WAIT — D3 lens panel silent at fire 4; all three pinged (deliver-partial instruction). Duplicate at 7. | WAIT |
| 79w-3 | 2026-08-17 | WAIT — D3 lens panel out. Fires: 3 (ping at 4). | WAIT |
| 79w-2 | 2026-08-17 | WAIT — D3 lens panel out (nav-scent, impl-integrity, orientation). Fires: 2. | WAIT |
| 79w-1 | 2026-08-17 | WAIT — D3 converge panel out (3 lenses: navigation-scent, implementation-integrity vs the router/qeval costs, first-visit orientation + per-state tour ids). Judge on delivery, then implement. Fires: 1. | WAIT |
| 79 | 2026-08-17 | D2 CLOSED. The original critic delivered late (browser-verified, 7 findings) and its finding 2 solved my own open probe failure: the qbar lived INSIDE a `data-qsec` section, so any zero-match query collapsed the box itself — focus fell to `<body>`, which is exactly why the Escape probe saw blur-without-clear (the handler's `t.id==="q"` guard never matched; the handler was always correct). All 7 APPLIED: F1 HIGH `.offp{display:none}` (0,1,0) lost to `.docket a{display:grid}` (0,1,1) — off-page rows always visible; `#/c/orem?p=2` showed 11 phantom rows under "Open docket 0" → scoped to `.docket .offp` (0,2,0), reveal rescoped `.qactive .docket .offp:not(.qhide)` (0,4,0). F2 HIGH → qbar moved OUTSIDE the collapsible sections on both pages (court: above Open docket; directory: above Featured — it filters both sections, so it sits above both). F3 Enter now `clearTimeout(QDEB)` so hint captions ("name a court — orem/9", "no claim #99") survive the 120ms debounce that was overwriting them. F4 QCTX.totalClaims now uses the route's own totalClaims (counts parsed.more) — no more false bare-id refusals on live deep dockets. F5 captions speak in the page's noun — "no loaded name matches … more courts are not loaded; page older ›" on the directory; PLUS self-found: applyQ dropped `kind` in its zero/active branches (browser probe caught it; pure-fn test alone missed it — kind is now threaded through all three). F6 parseJump lowercases ("OREM/7" jumps) and rejects zero-padded ids in the slug form ("orem/007" = null, same as bare "007"). F7 stats-hidden tier-2 rows also `removeAttribute("data-q")` so they can't inflate match counts. EVIDENCE: d2_probe.py 15/15 (Escape now passes); new d2_fixes_probe.py 10/10 — every critic repro re-run in Chromium (p=2 zero phantoms + query reveals; zero-match keeps box alive+focused; Escape recovers from the exact wedge state; settled-only match keeps the box; both Enter hints persist; OREM/7 jumps; directory zero caption says "name"; no page errors); all 11 node harnesses PASS with 13 new regression assertions in search_test.js. Wireframes: Screen 3 figure REGENERATED from the live courtBody (gen_court.js repaired: block-aware listener strip — the old line-strip regex orphaned a `});` once the debounce listener went multi-line — plus store stub for the D1 foldersFor path), divs 343/343, filter-row anno now states the box never collapses with the sections it filters. Duplicate critic stopped (its root-cause matched before termination). | NOT-CLEAN (8 applied) |
| 78w-12 | 2026-08-17 | WAIT — D2 critics 12/5; one grace fire before self-verify. | WAIT |
| 78w-11 | 2026-08-17 | WAIT — D2 duplicate critic pinged at its fire 4 (orig 11 silent). If both stay silent 2 more fires, self-verify D2 with the browser scripts + close from harness evidence. | WAIT |
| 78w-10 | 2026-08-17 | WAIT — D2 critics 10/3. | WAIT |
| 78w-9 | 2026-08-17 | WAIT — D2 critics 9/2. | WAIT |
| 78w-8 | 2026-08-17 | WAIT — D2 critics 8/1. | WAIT |
| 78w-7 | 2026-08-17 | WAIT — D2 critic silent through 7 → DUPLICATE launched. | WAIT |
| 78w-6 | 2026-08-17 | WAIT — D2 critic fires: 6 (duplicate next). | WAIT |
| 78w-5 | 2026-08-17 | WAIT — D2 critic fires: 5 post-ping. | WAIT |
| 78w-4 | 2026-08-17 | WAIT — D2 critic PINGED at fire 4. | WAIT |
| 78w-3 | 2026-08-17 | WAIT — D2 critic fires: 3 (ping next). | WAIT |
| 78w-2 | 2026-08-17 | WAIT — D2 critic fires: 2. | WAIT |
| 78w | 2026-08-17 | WAIT — D2 critic out (fires: 1). | WAIT |
| 78 | 2026-08-17 | D2 SEARCH + #ID JUMP IMPLEMENTED per the judge's canonical spec (three lens conflicts ruled: caption family = the impl lens's with honesty in idle/zero lines; q KEEPS ?p — dropping it would shrink a shared ?q link's search set and lose the user's place on clear; address-parse beats exactly-one-match on Enter). Shipped: qPredicate/parseJump/qCaption as PURE functions (harness-factored); one .addrbar.qbar row under the section head on the directory ("court name or orem/7") and the court docket ("title or #id") — NO left-rail global box ("a records office searches the drawer you're standing at"; a global box would promise an index the chain doesn't hold); rows render the FULL loaded set (parsed ∪ current window, deduped id-desc) with off-window rows class-hidden (offp) so a query sweeps everything this render loaded; filtering toggles classes ONLY — sparkline fills survive, revealed off-page rows keep the shipped empty-px state; one delegated input listener, 120ms debounce; the pager and Folders section hide while a query is active and qcap becomes the caption ("17 of 50 loaded titles match \"snow\" · newest first"; zero-match live: "no loaded title matches — 164 older claims were never loaded; browse the docket pages"; demo: "the sample is complete"); ?q rides the hash via replaceState (no refetch storm; shareable; routes re-apply QP.q after paint); Enter jumps — bare ids scope to the current court with FREE in-scope validation ("no claim #999 — this court has 214"), slug/id navigates into the route's honest 404, directory bare numbers get "name a court — orem/7", Enter on a non-address query with exactly one visible match opens it; "/" focuses (never from an input), Escape clears-then-blurs; role=status caption, searchbox via type=search. REFUSED: cross-court title search (unbounded; demo-only would break mode symmetry), fuzzy/ranking, statusText matching. Zero new reads. search_test.js (TENTH harness) 31/31 (predicate table, full jump grammar incl. leading-zero/decimal/junk rejections, every caption verbatim, structural stamps, replaceState, §7.4); ALL TEN harnesses pass; node --check clean (one dropped backtick caught and fixed in-flight); both pages 200; doc Screen 3 gains the filter row + anno ("searches only what the page loaded; the pager returns when the box clears"). Critic launched. | IMPLEMENTED (critic out) |
| 77w-14 | 2026-08-17 | WAIT — D2 judge fires: 6 (duplicate next). | WAIT |
| 77w-13 | 2026-08-17 | WAIT — D2 judge fires: 5 post-ping. | WAIT |
| 77w-12 | 2026-08-17 | WAIT — D2 judge PINGED at fire 4. | WAIT |
| 77w-11 | 2026-08-17 | WAIT — D2 judge fires: 3 (ping next). | WAIT |
| 77w-10 | 2026-08-17 | WAIT — D2 judge fires: 2. | WAIT |
| 77w-9 | 2026-08-17 | WAIT — D2 judge fires: 1. | WAIT |
| 77w-8 | 2026-08-17 | WAIT — D2 impl lens LANDED (code-verified: hide/show NEVER rebuild — spk fills survive as #spk- ids; render all loaded rows up front with off-?p-window rows class-hidden; ONE delegated input listener toggling qhide via .t textContent + #id — textContent catches late-filled directory names; ?q via replaceState — no hashchange refetch storm; exact caption family incl. zero-match live/demo variants; jump parser table with free pre-validation only where the count is in hand; cross-court title search REFUSED — unbounded + would break mode symmetry; search_test scope incl. statusText-never-matches). D2 JUDGE launched to canonicalize captions + resolve the deep-page ?q interplay. | WAIT |
| 77w-7 | 2026-08-17 | WAIT — D2 impl lens fires: 6 (duplicate next). | WAIT |
| 77w-6 | 2026-08-17 | WAIT — D2 impl lens fires: 5 post-ping. | WAIT |
| 77w-5 | 2026-08-17 | WAIT — D2 search-UX lens LANDED ("a records office searches the drawer you're standing at": one .addrbar-token row under the section head, per-page only, NO left-rail global box — it would promise an index the chain doesn't hold; case-insensitive substring over "#id title"; filter ignores pagination over the FULL loaded set, the pager HIDES while a query is active — "showing 26–50 of N" would lie — and its row becomes "N of the 50 loaded claims match — older pages are not searched"; ?q= rides the hash via replaceState, typing drops ?p; Enter navigates when the query parses as an address ("7"/"#7"/"orem/7"; bare 7 on the directory → "name a court — orem/7"), out-of-range ids navigate into the route's honest 404, Enter on exactly-one-match opens it; cross-court search DEFERRED — simulates an index the realm doesn't expose; "/" focuses, Escape clears; zero-match .empty state). Honesty/impl lens PINGED at fire 4. | WAIT |
| 77w-4 | 2026-08-17 | WAIT — D2 lenses fires: 3 (ping next). | WAIT |
| 77w-3 | 2026-08-17 | WAIT — D2 lenses fires: 2. | WAIT |
| 77w-2 | 2026-08-17 | WAIT — D2 lenses fires: 1. | WAIT |
| 77w | 2026-08-17 | WAIT — D2 (search + #id jump) panel launched: search-UX lens (placement idiom, filter semantics vs pagination, ?q vs ephemeral, jump grammar, cross-court defer ruling) + honesty/impl lens (per-mode searchable sets with exact captions, hide/show vs rebuild for sparkline survival, jump validation via honest 404, harness scope). Judge when both land. fires: 0 | WAIT |
| 77 | 2026-08-17 | D1 CRITIC APPLIED (duplicate stopped). The clean list settled the deep questions with vm-source evidence: the []uint64 qeval shape is exactly the per-element token form our parser prefers (gnovm values_string_stream.go — empty renders slice[]/nil-slice, all parse; hex impossible for []uint64); bare-JS-number uint64 args match every existing claim read; NO phantom ids (AddToFolder requires mustClaim, no claim delete) so skipping curFilterFolders for chain is SOUND; contiguity holds (no folder delete; PurgeFolder keeps the row); purge tombstones escaped everywhere incl. crumbs; precedence airtight; fail-closed verified. Findings, ALL FIXED (+5 harness assertions): F1 HIGH the MAP drew duplicate nodes when chain folders share a claim (the docket build got first-wins, the map didn't — the critic reproduced harness failures B/F on a constructed share) → the map's filterFolders now dedupes first-wins across folders ("one node per claim; containment is drawn as a partition"); F2 MED-HIGH the folder route resolved EVERY out-of-window member before pagination (a 1,000-member folder ≈ 2,900 queries per view) → it now pages over the folder's ids FIRST in curated order and resolves only the current page's missing ids (≤25×3 reads, bounded); F3 MED the re-sort destroyed the realm's curated order exactly when the "curated order" caption rendered → no re-sort, curated order preserved; F4 stale "demo-only route" comment rewritten; F5 /f/01 and /f/1.2 no longer alias /f/1 (exact fid match). INFO ruled no-change: court-page folder counts are the chain's true member counts even when the folder page omits hidden rows. ALL NINE harnesses pass (+chart_test = ten); node --check clean; both pages 200. D1 CLOSED. Next fire: D2 (title search + go-to-#id jump) panel. | NOT-CLEAN (5 fixed; D1 closed) |
| 76w-10 | 2026-08-17 | WAIT — D1 critics 10/3. | WAIT |
| 76w-9 | 2026-08-17 | WAIT — D1 critics 9/2. | WAIT |
| 76w-8 | 2026-08-17 | WAIT — D1 critics 8/1. | WAIT |
| 76w-7 | 2026-08-17 | WAIT — D1 critic silent through 7 → DUPLICATE launched (map multi-membership dedup prioritized). | WAIT |
| 76w-6 | 2026-08-17 | WAIT — D1 critic fires: 6 (duplicate next). | WAIT |
| 76w-5 | 2026-08-17 | WAIT — D1 critic fires: 5 post-ping. | WAIT |
| 76w-4 | 2026-08-17 | WAIT — D1 critic PINGED at fire 4. | WAIT |
| 76w-3 | 2026-08-17 | WAIT — D1 critic fires: 3 (ping next). | WAIT |
| 76w-2 | 2026-08-17 | WAIT — D1 critic fires: 2. | WAIT |
| 76w | 2026-08-17 | WAIT — D1 critic out (fires: 1). D2 (search + #id jump) panel queues behind its verdict. | WAIT |
| 76 | 2026-08-17 | D1 LIVE ON-CHAIN FOLDERS IMPLEMENTED (the D-judge's synthesis: an unkept caption outranks a missing feature — the site promised "does not read them live yet" in three places; folders decide what the rows ARE, search only filters them. D2 search+#id-jump, D3 cross-links+orientation, D4 sorts+urgency-grouping+the pager-caption bug, D5 directory stats/hidden-count+docket clocks+author chip queue next). Shipped: chainFolders(slug) — 1 FolderCount read (courts without folders cost exactly one), then name+items per folder chunked (1+2F, F capped at 100 with caption), ids parsed defensively from BOTH qeval slice shapes (per-element "(N uint64)" tokens preferred so their 64s never leak, bare-bracket fallback with type tokens stripped — harness-proven), purge tombstones rendered escaped verbatim (never styled from the spoofable prefix), per-folder failure → "read failed" row; foldersFor() composes ONE labeled source — local curation ?? chain (live) ?? sample (demo) ?? none, NEVER merged (local enforces nesting/one-folder-per-claim, the chain is flat with multi-membership — a merged tree has no honest caption); folderMeta first-wins so shared claims get one meta line and one map node; chain folder pages at #/c/:slug/f/<fid> (the folder's own stable id, single segment; dot-paths remain for nested local/demo), members beyond the parsed docket window resolved by real per-id reads gated through HiddenFromListing FAIL CLOSED with "N hidden or unreadable rows omitted"; ALL THREE apology captions replaced (docket sec-h "read live from the chain — moderator curation, zero economic weight", map "folders read from the chain…", empty "this court's moderators have filed no folders") — the string "does not read them live" is EXTINCT (grep 0); curate page notes descs are stored-but-unreadable; map/docket/folder-page all consume the same array unchanged. folders_test.js (NINTH harness) 21/21: both parse shapes, no-64-leakage, read counts (F=0 → exactly 1), cap+flag, purge-name XSS, fid hrefs, the precedence triple live/local/demo, first-wins meta, every caption, apology-extinct, §7.4; two stale caption assertions in map/curation harnesses synced; ALL NINE harnesses pass; node --check clean; both pages 200; doc anno rewritten (chain folders read live, the one-source rule, fid paths). qeval bound: +1 read for folderless courts, 1+2F otherwise, on court/folder/map routes. FolderInfo + id-contiguity → Owner notes. Critic launched. | IMPLEMENTED (critic out) |
| 75w-16 | 2026-08-17 | WAIT — D-judge silent through 7 → DUPLICATE launched with a tightened D1-focused brief. | WAIT |
| 75w-15 | 2026-08-17 | WAIT — D-judge fires: 6 (duplicate next). | WAIT |
| 75w-14 | 2026-08-17 | WAIT — D-judge fires: 5 post-ping. | WAIT |
| 75w-13 | 2026-08-17 | WAIT — D-judge PINGED at fire 4. | WAIT |
| 75w-12 | 2026-08-17 | WAIT — sweeper re-delivered post-ping (consistent: live folders THE pick; "show the list anyway" stays REFUSED — would resurrect moderation-hidden slugs, the legal analog is a count-only disclosure; docket clocks = the needs logic made public; per-claim curation notes carry a heavy label burden; refusals reaffirmed). D-judge fires: 3 (ping next). | WAIT |
| 75w-11 | 2026-08-17 | WAIT — D-backlog judge fires: 2. | WAIT |
| 75w-10 | 2026-08-17 | WAIT — D-backlog judge fires: 1. | WAIT |
| 75w-9 | 2026-08-17 | WAIT — conformance sweeper LANDED (screen-by-screen leftovers: directory counts-only stats + the LEGAL hidden-courts analog "N courts hidden by moderation; direct links still work" via reads allCourtSlugs already makes; docket per-row operative clocks; claim "opened by" chip; own AnswerRecord on /me; chainfoot nav; refusals stand. ITS ONE RECOMMENDATION: LIVE ON-CHAIN FOLDER READS — FolderCount/FolderName/FolderItems exist purge-gated, every surface already receives folders, the captions literally promise it, zero §7.4 exposure, "the last reference idea that's true only in demo"). D-backlog synthesis judge LAUNCHED (sequence D1-D5; full detail for D1). | WAIT |
| 75w-8 | 2026-08-17 | WAIT — discovery scout re-delivered post-ping (consistent; adds effort tags: search S pure-visual with the honest "filters the newest 50 loaded" caption, cross-links XS one-liners incl. needs→#resolution anchor + map ?focus=id, status-urgency docket grouping S, sorts S-M sorting the loaded window only with caption, deep-link orientation XS-S — introStrip only renders on #/ so shared claim URLs strand strangers; recommended order 1→4→2a→3→5). Conformance sweeper fires: 7 → DUPLICATE next round if silent. | WAIT |
| 75w-7 | 2026-08-17 | WAIT — conformance sweeper fires: 6 (duplicate next). | WAIT |
| 75w-6 | 2026-08-17 | WAIT — discovery-walk scout LANDED (verdict: "a credible records office, but discovery is one-directional — every page assumes you already know which claim you want". Ranked: 1 SEARCH — title filter over already-parsed rows (directory + docket pure-visual zero reads; cross-court = 1 qrender/court chunked, labeled "searches the newest 50 of N per court"; go-to-#id jump free); 2 IN-MOTION surface — per-court re-sort of ticking phases zero reads, directory-level "N in dispute · closing soonest" bounded; 3 CROSS-LINKS all pure-visual — claim page lacks its folder chip, claim→map, argument→map, needs' URGENT NOTICE DOESN'T LINK ITS CLAIM, raw pages render the realm's own links as dead text, folder pages lack tagrow; 4 SORT CHIPS — "most claims"/"largest" computable now, "newest" blocked on wishlist read; PLUS A REAL BUG: page 1 never states its order because pagerHtml hides the caption when ≤25 rows; 5 ORIENTATION — claim page has no route to about, about is a dead-end that never links the demo's one-claim-per-state tour, dismissing the intro removes the only inline about link). Conformance sweeper fires: 5 post-ping. | WAIT |
| 75w-5 | 2026-08-17 | WAIT — both discovery scouts PINGED at fire 4. | WAIT |
| 75w-4 | 2026-08-17 | WAIT — discovery scouts fires: 3 (ping next). | WAIT |
| 75w-3 | 2026-08-17 | WAIT — discovery scouts fires: 2. | WAIT |
| 75w-2 | 2026-08-17 | WAIT — discovery scouts fires: 1. | WAIT |
| 75w | 2026-08-17 | WAIT — discovery-gap review launched (named backlog complete): first-visitor discovery-walk auditor (search? sort chips? what's-happening surface? missing cross-links? orientation) + reference-conformance sweeper (final screen-by-screen diff of unadopted §7.4-legal richness, ends with ONE recommended next item). Judge/synthesis when both land. fires: 0 | WAIT |
| 75 | 2026-08-17 | C3 CRITIC APPLIED (duplicate stopped). The report was decisive — CRITICAL: PullState takes THREE args (courtSlug, claimID, who); our two-arg call errored silently, .catch nulled the flags, and every reward-pull row was MISSING ON LIVE (demo masked it — its pulls come from data, which is why me_test passed) → PullState now carries gstr(addr) and the detail cache is keyed slug/id/ADDR (also killing the cross-address poisoning race the critic warned about); HIGH: /me's Unstake omitted the required amount arg (realm panics on amount≤0) → the row now carries the side's exact stake; MED: an address holding BOTH sides got one withdraw row (and both-sides-provisional suppressed even the losing-side exit) → one row PER HELD SIDE with side-named labels; MED-LOW: needsList never cleared the detail cache (re-Check reused stale clocks) → clears like meHoldings; LOW verdict-read-failure could build a panic button (fallback to the possibly-overturned answer) → vote-route claims with a failed Verdict read get NO winner row (never guess a side); LOW the court-level carrot line now requires an actual carrot pool (turnout-withheld pools no longer prompt); LOW senior tickets now scan EVERY court (owed regardless of holdings) and the stat tile counts them ("newest 25 per court checked" hedge). Ruled no-change: reopen-in-flight losing-side withdraw (consistent with the claim page — logged as a known miss). Critic's clean list: PullState/SeniorEntitlement tuple orders verified against the realm, all button arg shapes correct elsewhere, provisional early-exit rule exact, needs gating matches the claim page, mefail guard ordering right, demo consistency incl. the canned-prior design. me_test extended to 44/44 (three-arg PullState, addr-keyed cache, Unstake amount, both-sides rows, verdict-fail suppression both routes, carrot-pool gate, senior-independent scan); ALL EIGHT harnesses pass; node --check clean; both pages 200. C3 CLOSED — the phase-4 named backlog (C1 map, C2 curation, C3 personal richness) is now COMPLETE; the loop continues per the owner's "do not stop": next fire opens a discovery-gap review to pick the next-most-valuable improvement. | NOT-CLEAN (7 fixed, 1 ruled; C3 closed) |
| 74w-7 | 2026-08-17 | WAIT — C3 critic silent through 7 → DUPLICATE launched. | WAIT |
| 74w-6 | 2026-08-17 | WAIT — C3 critic fires: 6 (duplicate next). | WAIT |
| 74w-5 | 2026-08-17 | WAIT — C3 critic fires: 5 post-ping. | WAIT |
| 74w-4 | 2026-08-17 | WAIT — C3 critic PINGED at fire 4. | WAIT |
| 74w-3 | 2026-08-17 | WAIT — C3 critic fires: 3 (ping next). | WAIT |
| 74w-2 | 2026-08-17 | WAIT — C3 critic fires: 2. | WAIT |
| 74w | 2026-08-17 | WAIT — C3 critic out (fires: 1). | WAIT |
| 74 | 2026-08-17 | C3 IMPLEMENTED (judge double-delivered; v1 base with v2's decisive correction ADOPTED: every position-scanned claim is a PARTICIPANT claim — author/answerer/staker are all carrot-excluded — so per-claim carrot hedge rows were decidably wrong; the hedge is one court-level line, and claimables() now excludes participants outright; also adopted v2's senior-demo consistency, entries sum to seniorOwed 640,000; v2's orem/12 REFUSED — v1's ledger/2 already implemented). Shipped: claimables(slug,id,d,p,addr,nowH) PURE decision function (stake withdrawals by phase incl. provisional-losing-side "leave with 1× now" and closed-unstake; rewardsOpened draws gated by the realm's OWN PullState paid flags — never guessed, null flags drop rows; overturned answerer renders the decisive "draws nothing"; Finalize when escrow passed + participant); meDetail() render-scoped cache sharing ONE claimDetail per claim across /me and /needs, live adding ClaimAuthor+PullState only on rewardsOpened hits (≤2/staked claim); seniorFor() — demo from the new seniorQueues sample, live QueueLen + newest-25 SeniorEntitlement scan filtered to the address; /me rebuilt: stats strip (counts ONLY — "coins are per-court, never summed"), "You can pull these now" ticket with §7.4 labels + action buttons, court-level carrot maybe-line, senior tickets ("X CC payable now of Y still owed to you — paid as budget accrues, the court's pace, not a promise", btn sub "owed, not earned"), positions table + state pill + personal meta ("runs against your side", "reopenable until ≈block N"), "Since you were last here" ladder = THIS BROWSER's memory (diffSeen pure fn; snapshot written only when mefail===0 — a partial view is never memorized; demo teaches with a canned prior look labeled sample); /needs rebuilt: header counts, "Closing soonest" warn notice, Against/With sections deadline-ascending, per-row operative clock ("you may dispute until ≈block N — in about T" / "reopenable until" / disputed's honest "a sealed vote is deciding — no close height is published"), against-rows carry bond facts + state-correct Dispute/Settle/Reopen buttons. DEMO: ledger/2 (Finalize-ready provisional, DEMO_ME winning-side stake, ledger balance 4.2 CC), orem/4 author=DEMO_ME + pulls flags, orem seniorQueues (Σ owed = seniorOwed ✓). me_test.js 34/34 (decision table per phase×flags×role incl. paid-suppression, never-guess on null flags, participant carrot exclusion three ways, finalize gates, diffSeen cases, copy strings, §7.4 sweep with a false-positive regex fix — "again" is not "gain"); arg/record synced for ledger/2 (live 2, 1 failed round); ALL EIGHT harnesses pass; node --check clean; both pages 200. Doc Screens 11/12 rebuilt (pull ticket, senior ticket, stats strip, needs sections with per-row clocks) + the never-guessed/owed-not-earned/browser-memory annos; divs 340/340. Critic launched. | IMPLEMENTED (critic out) |
| 73w-2 | 2026-08-17 | WAIT — C3 judge fires: 5 post-ping. | WAIT |
| 73w | 2026-08-17 | WAIT — C3 judge fires: 4 → PINGING. | WAIT |
| 73 | 2026-08-17 | COMBINED CRITIC APPLIED (duplicate stopped). The headline question came back CLEAN with browser proof: a drag released over a node does NOT navigate (capture at the 4px threshold retargets the compat click to the box), plain clicks and 3px wiggles navigate, drags pan — no suppress flag needed; XSS probes (hostile folder names/desc through court page, folder page, map SVG header, curate source line, argument labels) all render as literal text; prototype pollution impossible (fresh output object, __proto__ stripped+warned); depth/cycle/64KB/dup rules exact; curationFor corruption-safe with chain-scoped keys; Apply/Clear lifecycle correct with no double-apply window. Five findings, ALL FIXED (+5 harness assertions): F1 LOW-MED stuck drag — a press escaping the box before the 4px threshold left drag armed so a later buttonless hover panned (browser-verified) → pointermove now bails when (ev.buttons&1)===0; F2 LOW cleanCuration silently coerced non-array folders/relations to [] (contradicting fail-hard) → explicit "must be an array" errors; F3 LOW held off-chain ids inflated folder COUNTS on court/folder rows while pages dropped the rows (count/content disagreement) → shared curFilterFolders + curValidSet now filter curation at courtBody (via s.claims) and the folder route (live fetches ClaimCount, +1 qeval) exactly as the map already did; F4 COSMETIC same-folder duplicate now says "listed twice in this folder" and a part-cycle reports ONCE; F5 COSMETIC ids capped at 1e12 ("sane positive integer"). All SEVEN harnesses pass; node --check clean; both pages 200. C2 CLOSED; C1 map CLOSED (critic verdicts + fixes all applied). C3 judge fires: 3. | NOT-CLEAN (5 fixed; C1+C2 closed) |
| 72w-11 | 2026-08-17 | WAIT — combined critics 10/3; C3 judge fires: 2. | WAIT |
| 72w-10 | 2026-08-17 | WAIT — combined critics 9/2; C3 judge fires: 1. | WAIT |
| 72w-9 | 2026-08-17 | WAIT — C3 data lens LANDED (PullState READS the paid flags — winner/author/answerer draws fully decidable; WithdrawStake/Finalize decidable at ~0 marginal; carrot UNDECIDABLE per address → "May be drawable — the realm knows" hedge; SeniorEntitlement(seq) scannable 1..QueueLen with a K-cap pager, "when" unanswerable; bonus amounts only estimable (F9 cap unexposed); DEMO orem/4 already exhibits both decidable pulls, additions listed (pulls flags, seniorQueue, Finalize-ready provisional, DEMO_ME-as-author, carrot hedge case); last-visit state-diff ruled HONEST with exact copy; §7.4 row labels drafted; budget +≤2 qevals/staked claim with claimDetail memoized, per-court bound 2S+K+1). C3 JUDGE LAUNCHED. Combined critics 8/1. | WAIT |
| 72w-8 | 2026-08-17 | WAIT — combined critic silent through 7 → DUPLICATE launched (with the drag-release-over-node click question sharpened). C3 data lens fires: 6. | WAIT |
| 72w-7 | 2026-08-17 | WAIT — C3 data lens fires: 5 post-ping; combined critic fires: 6 (duplicate next). | WAIT |
| 72w-6 | 2026-08-17 | WAIT — C3 data lens PINGED at fire 4; combined critic fires: 5 post-ping. | WAIT |
| 72w-5 | 2026-08-17 | WAIT — C3 anatomist LANDED (ranked gaps: 1 per-row dispute deadline PURE-VISUAL — settleAt already fetched unused; 2 header summary + urgent worst-item notice PURE-VISUAL; 3 positions status chips via existing claimDetail; 4 stats strip — coin/in-positions visual, paid-in/backing/claimable NEW reads; 5 options panel from existing bond reads minus §7.4 lines; 6 closing-soonest assembly-only; 7 against-your-side section headers; 8 claimable panel partially derivable; 9 since-you-were-last-here NEEDS state-diff approach (no event log on chain); 10 reputation block new reads. §7.4 kills named: "You take, if right", "your NO are worth X", P&L px columns, "won" chip, "policing pays", "gain of X"). Honest-data lens fires: 3. Combined critic fires: 4 pinged. | WAIT |
| 72w-4 | 2026-08-17 | WAIT — combined critic PINGED at fire 4; C3 lenses fires: 3. | WAIT |
| 72w-3 | 2026-08-17 | WAIT — C2+C1fix critic fires: 3; C3 lenses fires: 2. | WAIT |
| 72w-2 | 2026-08-17 | WAIT — C2+C1fix critic fires: 2; C3 lenses fires: 1. | WAIT |
| 72w | 2026-08-17 | WAIT — C2+C1fix combined critic out (fires: 1). C3 panel LAUNCHED in parallel (me/needs richness, ref Screens 7/9): anatomist (claimable strip anatomy, since-you-were-last-here ladder, §7.4 flags) + honest-data (per-claimable reads + undecidable-paid-flags hedges, DEMO_ME states, client-side last-visit diff honesty, §7.4 row labels, marginal query bound). Judge when both land. | WAIT |
| 72 | 2026-08-17 | C1 CRITIC APPLIED + C2 IMPLEMENTED. C1 critic verified IN A REAL BROWSER (Playwright): its HIGH finding — pointer capture on pointerdown retargeted the compat click to #mapbox so map node clicks NEVER navigated (6/6 Chromium trials; causation proven by no-op'ing setPointerCapture) → FIXED: capture is now taken only after pointermove crosses a 4px drag threshold (clicks stay clicks, drags still capture); MED empty-court viewBox -Infinity (Math.max over []) → mapLayout guards + the route renders an honest empty state ("the map draws the docket, and this docket is empty"); LOW LOD flicker → hysteresis (enter <7px, exit >8.5px). Critic's clean list: the multi-column-edge limitation is real but caught by assertion G in both directions and no current data hits it (comment scopes it honestly); nested-claim relations route cleanly; phaseClass classifies the realm's EXACT live strings correctly ("…settles undisputed" → answered); XSS-escaped everywhere; no listener leaks; slider can't loop; §7.4 clean. Harness nits fixed: map_test tautology assertion replaced, buy_test brute-force walk bounded (minutes → 0.12s). C2 SHIPPED per the judge: #/c/:slug/curate — on-chain section FIRST (real bond-free mod calls as standard three-path rows: Create/Rename/Add/Remove folder, OpenClaimSeeded "deposit and fee waived", Hide/Unhide "m of n", AppointMods "creator only, free"; copy: "the realm refuses non-moderators at signing"), local section (Export download+copy with dated filename, Import paste/upload → cleanCuration validation → diff-preview ticket with held-ids note → Apply, Clear-with-confirm); cleanCuration enforces the full table (version/court/desc 240-1para/names 60/one-folder-per-claim/depth 4/counts/types/stances/dup/one-parent/no-cycles/64KB, unknown keys stripped+reported, off-chain ids warned+held); curationFor precedence local??sample(demo)/none(live) under cc.cur.<chain>.<slug>; FIVE gate flips — court folders sec-h, court desc line, folder route, map data+captions+legend+header-links, associationSection (now live-capable via a lookup; live titles reuse the docket's own qrender ONCE, curation-gated; ids beyond the window render bare "not in the rendered docket window") — every local surface labeled "your local curation — held in this browser, not on any chain"; refused: silent pre-fill of on-chain rows from local JSON, bulk sync-to-chain. curation_test.js 30/30 (every validation rule accept+reject, precedence both modes, idempotence, label strings, §7.4); ALL SEVEN harnesses pass; node --check clean; both pages 200. Doc: Screen 8b GENERATED-copy curate frame (section copy extracted verbatim from the route) + diff ticket + anno; Screens 3/3b/4b annos gain the local-curation caveat; divs 299/299. C2 wishlist → Owner notes. Combined critic launched. | IMPLEMENTED (critic out) |
| 71w-14 | 2026-08-17 | WAIT — C1 critics 13/6; one grace fire left before C2 implements regardless. | WAIT |
| 71w-13 | 2026-08-17 | WAIT — C2 judge re-delivered post-ping (v2 consistent; v1 base per precedent: keep v1's ruled exception — live argument rows get titles via ONE curation-gated reuse of the docket's own qrender, ids beyond its window untitled; adopt v2's explicit refusals: on-chain rows never silently pre-fill from local JSON, NO bulk sync-to-chain button — it would forge a write path the realm lacks). C2 implementation still HELD for a C1 critic verdict (editing the file mid-critique scrambles their anchors); implement anyway in ~2 fires if both critics stay silent. C1 critics fires: 12/5. | WAIT |
| 71w-12 | 2026-08-17 | WAIT — C1 duplicate critic PINGED at its fire 4 (orig 11 fires silent). | WAIT |
| 71w-11 | 2026-08-17 | WAIT — C2 JUDGE DELIVERED (D1-D7: on-chain mod actions surface ABOVE the local tool with verbatim section copy — "no bonds, gas only… the realm refuses non-moderators at signing", EditClaimTitle excluded as a trap button; curate page layout with export/copybtn/import-textarea-on-cliblock-tokens/clear+confirm/diff-preview ticket + refuse-notice errors; curationFor() precedence demo local??sample, live local??none, with the exact five gate-flip call sites and label strings; cleanCuration full table; doc Screen 8 generated in diff-preview state + three anno caveats; curation_test.js scope; 0 new reads EXCEPT one ruled exception — live claim pages with local relations reuse the docket's own qrender for titles, curation-gated, out-of-window ids render untitled). C2 implementation HELD until a C1 critic verdict lands (C2's gate-flips touch the map captions under review). C1 critics fires: 10/3. | WAIT |
| 71w-10 | 2026-08-17 | WAIT — C2 judge PINGED at fire 4; C1 critics 9/2. | WAIT |
| 71w-9 | 2026-08-17 | WAIT — C1 critics 8/1; C2 judge fires: 3 (ping next). | WAIT |
| 71w-8 | 2026-08-17 | WAIT — C1 critic silent through 7 → DUPLICATE launched. C2 judge fires: 2. | WAIT |
| 71w-7 | 2026-08-17 | WAIT — C1 critic fires: 6 (duplicate next); C2 judge fires: 1. | WAIT |
| 71w-6 | 2026-08-17 | WAIT — C2 tooling designer LANDED (curation JSON v1 in exactly the consumer shape; cleanCuration validation table incl. one-folder-per-claim partition rule + one part-parent + no cycles + ids-only/title-free with unknown keys stripped; #/c/:slug/curate with export/import/diff/apply/clear; localStorage cc.cur.<chain>.<slug>; curationFor() flips the three demo-only gates into curation-presence gates — local curation lights folders/relations/desc LIVE over the real parse, out-of-range ids held-not-rendered; labels "your local curation — held in this browser, not on any chain"; NO claim annotations in v1 — editorial voice §7.4 risk; drag-drop AND relation forms deferred — export→edit→import is the write path). C2 JUDGE LAUNCHED (must rule the tension: also surface the REAL bond-free mod calls as standard action rows, sectioned on-chain vs local). C1 critic fires: 5 post-ping. | WAIT |
| 71w-5 | 2026-08-17 | WAIT — C2 capability scout LANDED (bond-free mod powers exist NOW: CreateFolder/RenameFolder/AddToFolder/RemoveFromFolder single-active-mod gas-only, OpenClaimSeeded waives deposit+fee, Hide/Unhide m-of-n, AppointMods creator-free, EditClaimTitle author-only-in-window; chain folders are FLAT append-order — no nesting/reorder (demo's nested tree exceeds chain); court name immutable 100ch, NO charter/scope/claim-body/relations on chain (CS §5 future: containment=author-at-creation, argument edges=anyone-no-bond); overlay gap list = charter/scope, bodies/notes, relations, nesting/order, declared-verdict scoreboard; CLI/Adena-able bond-free call list delivered; realm wishlist: SetCourtCharter, MoveInFolder+nesting, body/hash anchor, SetParent/AddArgumentEdge, mod-gated rename). Tooling designer fires: 3; C1 critic pinged fire 4. | WAIT |
| 71w-4 | 2026-08-17 | WAIT — C1 critic PINGED at fire 4; C2 lenses fires: 3. | WAIT |
| 71w-3 | 2026-08-17 | WAIT — C1 critic fires: 3; C2 lenses fires: 2. | WAIT |
| 71w-2 | 2026-08-17 | WAIT — C1 critic fires: 2; C2 lenses fires: 1. | WAIT |
| 71w | 2026-08-17 | WAIT — C1 critic out (fires: 1). C2 panel LAUNCHED in parallel: realm-capability scout (bond-free creator/mod powers table from folders.gno/moderation/StartCourt/ClaimSeeded; gap list the overlay must cover; wishlist) + curation-tooling designer (per-court curation JSON exactly in the DEMO shape so map/argument/folders consume it unchanged; export/import/clear UX with validation+diff preview; localStorage precedence + "your local curation — held in this browser, not on any chain" labeling; never-publishes honesty; v1 scope ruling). Judge when both land. | WAIT |
| 71 | 2026-08-17 | C1 THE MAP IMPLEMENTED (judge double-delivered post-ping; v1 base + v2's refinements adopted: richer 7-dot grammar, dim .35 for text legibility, CSS-only LOD that never moves geometry; the LATE original layout engineer also delivered an independently-verified convergent design — its textLength attribute-cap adopted so label containment is provable from the SVG itself). Shipped: route #/c/:slug/map + "map →" tagrow entry; mapLayout() — grid-clustered folder columns, folders take POSITION (disjoint boxes, nested indented, folderless in a dashed pseudo-box), part-of keeps PRIMACY (solid 2px arrows child→parent + orders claims within folders), bears dashed by stance / supersedes dotted, orthogonal channel+bus routing with per-edge lanes, wide folders wrap into columns of 8 (the live 50-box goes wide not 3,800px tall), pure cumulative sums — deterministic and overlap-free BY CONSTRUCTION; mapSvg() — records-office tokens only, 7-phase dot grammar agreeing with statusPill families (harness-checked per claim), textLength-capped labels, per-node <title> "#id · phase — sentence", every node a real <a>, folder headers link to folder pages (demo); chrome per spec — honest count line ("11 claims, 11 shown" / live "newest N of M shown"), honesty caption (demo sample-curation / live "the chain stores folders, but this overlay does not read them live yet; no relations are recorded on chain"), controls [titles][ids only]·[−][+][fit]·log2 slider, fineprint, HTML legend; mountMap() — container-level listeners surviving innerHTML swaps, wheel zoom cursor-anchored via getScreenCTM (letterbox-exact), pointer-capture pan with down-frame CTM deltas, z∈[0.5,8] rAF-throttled viewBox writes, text toggle relayouts preserving relative center, CSS-only label LOD (<7px effective), hover/focus neighbor-highlight dim .35, keyboard arrows/±/0; CLICK NAVIGATES (click-to-frame REFUSED — zooming at a node IS framing); §7.4 kills honored (no stake sizing, no amounts, no verdict-side legend). VERIFIED PER THE OWNER'S MANDATE: map_test.js renders and geometrically measures the generated SVG — assertions A-I (node pairs ≥4u, text-in-owner, text pairs, folder nesting, claims-in-folders, dots clear, edges cross zero non-endpoint nodes/headers, distinct lanes/tracks, viewBox containment) PASS on demo orem both modes (minGap 12, 1376×524 / 672×380) AND the live 50-claim box both modes (1832×704 wide-wrap), deterministic bytes, NEGATIVE CONTROL fails correctly, §7.4 sweep clean, dot↔pill agreement 11/11, all nodes linked. Live qeval bound: 0 new reads. Doc: new Screen 3b GENERATED from the live functions (gen_map.js — the verified SVG embedded) + full anno; divs 247/247. Two harness slice anchors fixed post-refactor (phaseClass moved above statusPill); ALL SIX harnesses pass; node --check clean; both pages 200. Critic launched. | IMPLEMENTED (critic out) |
| 70w-9 | 2026-08-17 | WAIT — C1 judge fires: 5 post-ping. | WAIT |
| 70w-8 | 2026-08-17 | WAIT — C1 judge PINGED at fire 4. | WAIT |
| 70w-7 | 2026-08-17 | WAIT — C1 judge out (fires: 3; ping next). | WAIT |
| 70w-6 | 2026-08-17 | WAIT — C1 judge out (fires: 2). | WAIT |
| 70w-5 | 2026-08-17 | WAIT — C1 judge out (fires: 1). | WAIT |
| 70w-4 | 2026-08-17 | WAIT — C1 layout engineer (duplicate) LANDED WITH PROTOTYPE: grid-clustered folder columns, overlap-free BY CONSTRUCTION (coordinates = cumulative sums, deterministic same-input-same-bytes); folders = disjoint boxes (nested indented, folderless in a dashed "(no folder)" box); part-of = heaviest edge (solid 2px child→parent) AND drives within-folder order; bears dashed by stance, supersedes dotted; orthogonal channel/bus routing with per-edge lanes; titles-node 240×64 with clipPath char budgets, ids-node 64×28; 9-assertion collision harness (node pairs/text-in-rect/text pairs/folder nesting/edge-crossings/lane distinctness/viewBox) PASSES orem + 50-claim synthetic in both text modes (0.4-5.8ms), NEGATIVE CONTROL fails correctly; interaction math complete (viewBox pan/zoom z∈[0.5,8], wheel cursor-anchored, ± toggle, log2 slider two-way, text toggle recomputes <6ms preserving center, nodes are real <a> links, keyboard); prototype at scratchpad/c1proto_b/layout.js + 4 SVGs. C1 JUDGE LAUNCHED (merge with reference chrome, 7-phase grammar, §7.4 kills stake-sized dots + amount tooltips, demo/live honesty, click-model conflict to resolve, map_test harness + doc screen from generated SVG). Original layout agent 13 fires silent (counts if late). | WAIT |
| 70w-3 | 2026-08-17 | WAIT — layout engineers still out (orig 12, dup 5 post-ping). | WAIT |
| 70w-2 | 2026-08-17 | WAIT — layout duplicate PINGED at its fire 4 (orig 11 fires, presumed dead but counts if late). | WAIT |
| 70w | 2026-08-17 | WAIT — C1 layout engineers still out (orig 10 fires, dup 3). | WAIT |
| 70 | 2026-08-17 | B8+B9 critic delivered (duplicate stopped; clean list included: docket footer regex verified char-for-char vs render.gno incl. U+2026, routes with ?p=, all 6 relation triples direction-correct on all 11 pages, statusPill divergences from old docket logic confirmed as deliberate FIXES — old code misclassified closed-never-answered/provisional/answered via substring traps). Eight findings, ALL APPLIED: F1 MED-HIGH directory deep pages used lexicographic ListByTier vs p1's burn-ranked render (courts skipped AND duplicated) → deep pages now walk ListedCourtsBy("burn",offset,25) — the same index the render walks — with featured-in-window rows hidden by the stats fill exactly as the render skips them (no backfill); F2 MED docket p1 windowed by row position vs p2 by id (overlap whenever a hidden claim sat in the newest 25) → p1 now windows by ID (parsed.filter(id≥total−24)), short pages mirror the chain; F3 MED listed total was silently wrong (the burn section emits NO footer; the Featured footer was misattributed into the listed total — and page_test had FIXTURED the misattribution as correct) → footer attribution is now per-section (moreFeatured vs more), live directory reads ListByTier(1) for the true listed count (1 qeval, falls back to shown-count + hidden pager), harness fixture corrected; F4 directory caption now says "ranked by GNOT burned" (was "newest first" — false); F5 LOW live vote-settled claims rendered a stale "settle deadline — an undisputed answer settles here" rung (SettleDeadline persists after freeze) → the rung shows only while phase==="answered" i.e. while it is the operative clock; F6 LOW HiddenFromListing catch failed OPEN → fails closed ("an unreadable moderation state never lists"); F7 LOW past-the-end pages produced "showing 51–50" + "Open the first claim" → pager caption "page N is past the end — X in all", docket page>1 empty state is "Nothing on this page."; F8 LOW malformed ?scan=%zz threw URIError killing render → per-param try/catch. page_test extended (+6 assertions incl. the corrected footer-attribution fixture); all five harnesses pass; node --check clean; both pages 200. B8+B9 CLOSED. C1 layout engineer (orig 9 fires + dup 2) still out; judge when it lands. | NOT-CLEAN (8 applied; B8+B9 closed) |
| 69w-9 | 2026-08-17 | WAIT — all four still out: B8+B9 critic 9/2, layout engineer 8/1. | WAIT |
| 69w-8 | 2026-08-17 | WAIT — layout engineer silent through 7 → DUPLICATE launched. Out now: B8+B9 critic (orig 8 + dup 1), layout engineer (orig 7 + dup 0). First deliveries win. | WAIT |
| 69w-7 | 2026-08-17 | WAIT — B8+B9 critic silent through 7 → DUPLICATE launched (first delivery wins). Layout engineer fires: 6 (duplicate next fire). | WAIT |
| 69w-6 | 2026-08-17 | WAIT — B8+B9 critic fires: 6 (duplicate next fire); layout engineer fires: 5 post-ping. | WAIT |
| 69w-5 | 2026-08-17 | WAIT — B8+B9 critic silent post-ping (fires: 5); layout engineer fires: 4 → PINGING it. | WAIT |
| 69w-4 | 2026-08-17 | WAIT — C1 anatomist LANDED (reference map fully quoted: 620px .graphwrap viewport, mapctl labels:[titles|ids only|fit] + fineprint "scroll to zoom, drag to pan, click a node to frame it"; nodes root-rect/folder-circle-r13-always-slugged/claim circles r=4.5+sqrt(stake)/11 — stake sizing + ORM tooltip lines are §7.4-OUT for us; 3 states via shape AND fill (CVD note), we need 7; containment = quiet quadratic curves no arrows, folder trunks 1.6px; pro/con = solid/dashed bowed quads with target arrowheads trimmed to radii; LOD: class far when screenW/viewBox.w<0.62 hides long claim labels, folder slugs always drawn "the map's legend"; wheel zoom cursor-anchored clamp 0.06–1.6× fit; click-to-frame 430ms + dim non-neighbors to .12 + background-click reset + Enter/Space; legend + "214 claims, 23 shown"; part/supersedes edges have NO reference grammar — ours to design; reference's own non-overlap is soft: label reach a fixed guess, chords cross nodes — the owner's complaint). B8+B9 critic fires: 4 → PINGING. Layout engineer fires: 3. | WAIT |
| 69w-3 | 2026-08-17 | WAIT — B8+B9 critic (fires: 3) + C1 lenses (fires: 2) still out. | WAIT |
| 69w-2 | 2026-08-17 | WAIT — B8+B9 critic (fires: 2) + both C1 lenses (fires: 1) still out. | WAIT |
| 69w | 2026-08-17 | WAIT — B8+B9 combined critic out (fires: 1). C1 map panel LAUNCHED in parallel (different aspect, no edits until judge): reference-Screen-3 anatomist (viewport/node/edge/zoom-label grammar, folder-as-claim treatment, §7.4-suspect prices) + layout engineer (think-hard: deterministic overlap-free layout for the dual hierarchy folder+part-of, geometry constants that guarantee non-collision incl. LABELS, node-side SVG verification harness, pan/zoom/toggle/slider interaction math; may prototype in scratchpad/c1proto). Judge when both land. | WAIT |

STATUS AT PHASE-4 START: B1-B4 closed; B8 (argument) + B9 (pagination) implemented, combined critic OUT (apply its findings when it lands — that is the first loop item before C1's panel).

## OWNER DIRECTIVE (2026-08-17, post-loop-stop): B8 related claims + B9 pagination

Verbatim: "in the wireframes i saw related claims... see screen 5, 'the argument' and 'resolutions so far'... i'm not sure what the categories should be, but i think immediate children and non-children related should be shown. populate the court with rich relations so that related claims can be shown. but use subagents and think hard what the example should be and what the categories should be as well. also figure out pagination in all the pages and sections btw."

Process: same converge-before-implement discipline, driven inline (no cron — loop remains stopped). Panel launched: reference anatomist (Screen 5/6 relation rows + reference pagination treatment), taxonomy+example architect (categories argued from first principles, realm relation-storage check, the rich orem story incl. parent with 2+ children and cross-cutting non-child relations), pagination scout (full unbounded-list inventory + realm render paging check + one consistent pattern). Judge merges when all land.

Anatomist LANDED: reference has ONE typed relation — stance pro/con (supports/counters), directed edges (from arguing claim TO target), triples like ['C-153','C-152','pro']; nesting = the same relation one level down (indented .nest), NOT a separate category; folders are the decomposition axis; NO parent/child type; prose cross-refs untyped. Row = stance mark · mono id · title · state chip · yes-price (grid 18px 60px 1fr auto auto); stance and state independent (a CON row can be settled yes). Fineprints verbatim captured ("navigation and evidence only — nothing here moves the price or the verdict"; "you should not have to leave the page you vote on"). "The argument" = claim-graph neighborhood; "Resolution so far" = THIS claim's event ladder (when/what rungs + a future rung) — PURE-VISUAL for us (chart already reads every event). Reference has NO pagination — strategies: curated tiers + "N not shown … show the list anyway", sort choice (newest·busiest·largest), honest counts ("214 claims, 23 shown"), state sectioning, scroll only for width. Site gaps ranked: argument block (DATA — overlay triples + per-claim stake), unsettled-underneath banner (derived), resolution ladder (visual), list-bounds honesty (visual: docket "newest 50 of ClaimCount", directory hidden-courts link), directory sort (visual for parsed fields), docket map (defer — largest build).

Pagination scout LANDED: realm renderPageSize=50 (render.gno:82, gas-bound rationale), NO Render paging path (:slug/page/2 parses as claim id → Not found), BUT offset/limit qeval reads EXIST — ListedCourtsBy(order,offset,limit), StripPage, PendingPage — and ids are contiguous 1..ClaimCount, so honest paging is chain-served. Inventory bugs: overlay regex parses DROP the realm's "…and N more" footers (directory 568, docket 596) so subset counts label as totals; directory per-court stats fill is UNCHUNKED (~900 queries at 100 courts); demoRender omits the 50-cap footer (raw-view fidelity gap); me/needs full-scans are ~4,000 queries at scale; court record (K=20) is the model citizen. Proposal: ONE pattern — newest-first id-windows of 25, hash state (?p=2), mono caption "showing 26–50 of 214 · newest first", square chips [‹ newer][older ›]; LIVE page 1 = free render parse captioned "newest 50 of ClaimCount"; deeper pages = ClaimTitle+ClaimStatus 2 qevals/row chunked + sparklines visible-window only (~150/page, cheaper than today); failed row says "read failed", never guesses; directory deeper pages via ListedCourtsBy; me/needs paginate SCAN DEPTH ("scanned newest 100 of 214 — scan older ›"); demoRender gains cap+footer; DO NOT paginate asides/ladders/raw (the realm footer is raw's pager). fires: taxonomy lens still out.

Taxonomy lens LANDED (the think-hard lens): claimState has NO relation field (ClaimSeeded = fee-waiver bool); FOLDERS ARE ON CHAIN (folders.gno: name/desc/ordered ids, zero economic weight) — the overlay's "not on-chain" copy is STALE (predates folders.gno); docs/COURTS_STRUCTURE.md §5 already designs future relations (containment tree + argument edges, "views may aggregate; the chain does not infer") — the demo should teach that model. TAXONOMY (3): Part of (one parent), Bears on (supports|contradicts), Supersedes (re-filing after no-decision — kourt mechanically produces no-decisions); same-subject REJECTED (folders own it). Rows: title + relation tag + status pill ONLY — no yes%, no sparkline ("supports + 90%" is a trading hint). Fineprint: "Curation, not mechanics: relations move no stake, no bond, no bar, no verdict — each claim is decided alone." EXAMPLE: 3 new open claims — #9 annual-report-accurate (PARENT of #3,#4,#7 — a case half-won), #11 inspection-log-load-restricted (CONTRADICTS #3 — explains the live dispute), #10 mayor-email-documented (SUPERSEDES #5 — the unfalsifiable phrasing failed 3 rounds); #6 SUPPORTS #9 (outside evidence teaches child vs non-child); owner-floated #4↔#5 REJECTED (fails the reviewer read); #1,#2,#8 stay relationless (honest empty state). Placement: ONE section "The argument" on the claim page (Part-of line, "Rests on" children, "Related"); docket/folders unchanged.

B9 IMPLEMENTED (round 68) per the judge's three chunks, ALL SHIPPED: (chunk 1 honesty/bugs) listCourts/listClaims now parse the realm's overflow footers ("…and N more"/"…and N older") into a .more count so a subset is never labeled a total; demoRender mirrors the realm exactly (newest-50 by descending id + footer); the directory stats fill is chunked (6 at a time) instead of an unbounded burst; (chunk 2 pagers) hash query params (#/c/orem?p=2) parsed by the router into QP; PAGE_N=25 newest-first windows with mono captions ("showing 26–50 of 214 claims · newest first") and square [‹ newer][older ›] chips (disabled ends), pager hidden when one window holds all; court docket windowed (folders head page 1 only; sparkline fill covers only the visible window), folder pages windowed with "curated order" caption, demo docket now newest-first matching the chain's own order; (chunk 3 live depth) listClaimsPage materializes deeper docket pages by real reads (ClaimTitle+ClaimStatus chunked 6) and — critically — checks HiddenFromListing per id so pagination cannot resurrect moderator-de-listed claims (the render skips them; so do we), failed rows say "read failed — reload to retry"; directory deep pages read ListByTier(1) ONLY (tier-0 hidden courts stay hidden — allCourtSlugs unions tier 0 and must never feed the directory), rows fill name/claims from the chunked stats pass; me/needs scans are depth-bounded live (SCAN_DEPTH=100 newest ids per court, ?scan=K extends by 100) with the caption "live scan: the newest K of N claims in {court} — older positions are not shown / older stakes are not checked" and a [scan older ›] chip. Harness page_test.js 17/17 (pager math incl. clamps and disabled chips, pageSlice windows, demo newest-first, BOTH footer regexes against synthetic realm markdown, demoRender cap+footer at 60 claims); all five harnesses pass; node --check clean; both pages 200. Doc: Screen 3's ENTIRE court-page cols now GENERATED from the live courtBody (gen_court.js — the doc can no longer drift from the docket/aside at all), .cols/.pager/.assocrow parity CSS added; divs 242/242. Combined B8+B9 critic launched.

B8 IMPLEMENTED (round 67): judge's spec followed — statusPill extracted (docket + argument rows share the classifier); DEMO gains orem/9 (composite annual-report claim, PARENT of 3/4/7), orem/10 (documentary re-filing SUPERSEDING the unfalsifiable #5), orem/11 (load-restricted log CONTRADICTING #3), claims [1..11], folders 4/3/3(+1 nested Elections), DEMO.relations.orem = 6 typed triples with the honesty comment; associationSection on the claim page ONLY (Part-of line, Rests on with "1 of 3 parts settled" + "2 of 3 parts are still undecided — any verdict here is reached without them", Related with direction-resolved chips supports/contradicts/supersedes + passive "…by this" forms, title+chip+status-pill rows — NO yes%, .assocrow grid without the sparkline slot), header labeled "sample curation — the chain stores no relations", live absent, relationless claims omit the section; resolutionLadder ("Resolution so far") in BOTH resolutionSection returns — real fields only (derived answered rung LABELED "derived: settle deadline − 72h", settle/vote/reopen/now rungs, phase-appropriate future rung; openedAt fabrication REFUSED per judge 9), ladder above the notice so buttons stay; folders-on-chain copy corrected in 4 index sites + doc anno (folders.gno reads exist; overlay reads them from sample data for now — live-folder reads to wishlist). Harness assoc_test.js 35/35 (ripples: census live 5→8 sums 11, folders 4/3/3, one-parent invariant, every chip direction, live/relationless absence, ladder per phase incl. no-fabricated-heights and unexposed-close); record_test synced to 11 claims; dispute/buy harnesses still pass; node --check clean. Doc: Screen 4 + Screen 7 gain GENERATED ladders, new Screen 4b "The argument (#/c/orem/9)" GENERATED from associationSection with taxonomy anno; divs 275/275; both pages 200.

ALL THREE LENSES IN → B8/B9 judge LAUNCHED (merged brief incl. the folders-on-chain copy correction and the resolution-so-far ladder; sequenced B8 then B9).

## PHASE 3 — wireframe-richness reconciliation (owner, 2026-08-17) — cron 8cd9c260, 1m

Goal: make every screen look and feel like
gno_grc20vote/wireframes/courts-wireframe.html — strictly better or richer,
never a step back in cohesion/UX. The owner explicitly wants a DETAILED graph
per claim ("the information isn't all that important" — the richness is).
§7.4 still binds all copy. web/** + these state files writable; never commit;
realm files untouched (realm-side wants go to Owner notes).

Process per item (MANDATORY): (1) launch 2-3 DESIGN subagents with distinct
lenses (wireframe anatomy · honest-data design · then a UX-cohesion judge fed
both outputs) and CONVERGE on a written spec BEFORE any implementation;
(2) implement the converged spec; (3) smoke (node --check + serve);
(4) verify with a fresh critic; (5) log the round here.

Data honesty rule for richness: demo mode MAY synthesize dense series if
deterministic, anchored to the three true ratios, and labeled as sample data;
live mode shows only real reads (3-point series + real event heights) and must
never invent history. Event markers may use real heights (settleAt,
escrowUntil, phase transitions) in both modes.

Reconciliation backlog:
- B1 claim chart richness — CLOSED (rounds 55-57)
- B2 buy/join panel → rich receipt (you send / receive / price after — curve-computable, honest)
- B3 court identity richness (court-id sub description; demo descriptions)
- B4 dispute/vote screen richness — CLOSED (rounds 64-66)
- B5 needs view richness (reference Screen 7 "Watching")
- B6 me view richness (reference Screen 9 "You": claimable, what changed)
- B7 wireframes.html kept reconciled after each item

## Round log

| round | date | what | verdict |
|---|---|---|---|
| 66 | 2026-08-17 | B4 critic delivered post-stop (duplicate stopped); THE KEY VERDICT: "stands provisionally" is REALM-EXACT — an upheld round sets provisional, bond escrowed until Finalize, reopenable until escrowUntil; the site's phase model agrees. Clean list: overturn/quorum-fail/make-good rows all verified to the line, abstain semantics exact (quorum counts it, threshold excludes it), 5,925 CC quorum recomputed exact (implied x̄≈160 CC ≪ 5% arm — bond arms cross-check 2×40%×80M=64M ✓), tie/all-abstain uphold verified, both honesty lines true (no close-height/tally reads exported), carrot sentence matches PullCarrot, doc byte-identical, harnesses pass. Five findings, four APPLIED (+harness 33/33): F1 MED "vote closes" chart mark pinned near now — voteEndsAt was missing from the domain candidates → added; F2 MED the who-pays-whom inset omitted the UPHOLD make-good (answerer also gets min(2×bond, 80%·burn) minted+queued) → Upheld row completed; F3 LOW eligibility affirmative could bless an address the realm refuses (records persist after full withdrawal; ConvictionOf floors to 0) → hedged: "the realm has the final say at signing; staker records persist even after full withdrawal"; F4 LOW reopen-after-overturn renders answerBond=0 → hint and overturn row are now bond-aware ("its bond already burned in an earlier overturned round" / "no bond is left to burn"). F5 NIT ladder-restarts: self-disclosed in copy, no change. Doc Screen 7 regenerated from the live function; node --check clean; both pages 200. B4 CLOSED. LOOP REMAINS STOPPED (owner). | NOT-CLEAN (4 applied; B4 closed) |
| 65w-9 | 2026-08-17 | OWNER STOP — loop halted by owner mid-B4-verification ("once ur done with your current work stop the loop and show me the website"). Cron 8cd9c260 DELETED. B4 critics (original + duplicate) still out; their findings will be applied as a final wrap when they land, then this table gets a closing row. Backlog state: B1 chart, B2 buy receipt, B3 court identity, B4 dispute ballot all IMPLEMENTED (B1-B3 critic-verified + closed; B4 awaiting verdict); B5 needs-view and B6 me-view richness NOT started; B7 (doc reconciliation) done continuously per item. Site served for the owner at http://127.0.0.1:8399/. | STOPPED BY OWNER |
| 65w-8 | 2026-08-17 | WAIT — B4 critics (original + duplicate) both out. fires: 8/1 | WAIT |
| 65w-7 | 2026-08-17 | WAIT — B4 critic silent through fire 7 → DUPLICATE launched (identical brief; first delivery wins, late original counts). fires: 7/0 | WAIT |
| 65w-6 | 2026-08-17 | WAIT — B4 critic still silent. fires: 6 (duplicate next fire) | WAIT |
| 65w-5 | 2026-08-17 | WAIT — B4 critic still silent post-ping. fires: 5 | WAIT |
| 65w-4 | 2026-08-17 | WAIT — B4 critic silent at fire 4 → PINGED (uphold-vs-provisional flagged as the key question). fires: 4 | WAIT |
| 65w-3 | 2026-08-17 | WAIT — B4 critic still out. fires: 3 (ping next fire if silent) | WAIT |
| 65w-2 | 2026-08-17 | WAIT — B4 critic still out. fires: 2 | WAIT |
| 65w | 2026-08-17 | WAIT — B4 critic out (realm fidelity of every ticket sentence, esp. the uphold-provisional question; quorum recompute; ladder/clock; eligibility; §7.4; regression harnesses; doc parity). fires: 1 | WAIT |
| 65 | 2026-08-17 | B4 IMPLEMENTED (judge double-delivered post-ping; v1 base per precedent, v2's "sealed means un-summed, not secret" clause merged; v2's demo-gate-the-vote-buttons REFUSED — demo buttons are already inert with the demo note, and data-needack's message is ack-specific; BOTH judges' demo quorumFloor values REJECTED against the realm source — dispute.gno's quorumFloor lands on the 5% arm for orem: max(5%·118,500 CC, min(x̄, votable/3)) = 5,925 CC = 5_925_000_000 units, comment in the dataset shows the arithmetic). Shipped: disputed branch = slim notice + disputeTicket() — ballot hint restating answer/answerer/bond ("whose 80.0 CC bond rides on it?"), uphold/overturn rows stating BOTH outcomes concretely, round ladder from the failed-rounds count ("round 1 — failed quorum: half that round's disputer bond burned, half returned; round 2 — voting now at 64.0 CC — doubles each failure, resets when a round decides"), clock row (demo voteEndsAt NOW+86,400 blocks ≈ 5 days + a "vote closes" chart event; LIVE: "the chain exposes no close height to read; Resolve simply refuses until it has passed" — never invented), turnout bar (QuorumFloorOf read added live-only when disputeOpen, labeled "a new dispute today would face…; the running round's own bar was fixed when it opened and is not republished"), threshold ">50%… stated here, not read; ties and all-abstain rounds uphold", sealed-tally paragraph ("un-summed, not secret… copying the first big voter"), eligibility paragraph (epoch-checkpointed balance, exclusions incl. persistent staker records, demo-exclusion teaching shown only when the sample address actually holds a position — orem/3 does), ABSTAIN button added (choice:"abstain", "counts to turnout only, never to a side"), who-pays-whom .never inset (quorum-fails/overturned/upheld in burn/mint/escrow language, "nothing ever moves between adversaries… no pot to steer"); fillVoteEligibility() async live+wallet (ClaimAuthor + positionOf → "You may not vote here — you authored/answered/staked this claim"); settled-by-vote rewardsOpened claims gain the carrot sentence (route-gated; placed as a line, not a button sub — layout deviation logged); qeval bound: +1 anonymous (QuorumFloorOf), +6 with wallet. Harness dispute_test.js 26/26 (6 initial failures were my expectations, not the site: orem/3's answer is YES, cc renders 80.0, close is 5 days by design); record 21/21, buy 34/34, chart baseline OK; node --check clean; both pages 200. Doc: Screen 7 ticket GENERATED from the live disputeTicket (gen_dispute.js), anno rewritten with the honesty story; divs 259/259. Wishlist → Owner notes (VoteCloseAt, RoundOrdinal, FinalTally post-close, DisputeThresholdBps, QuorumFloorAt). Critic launched. | IMPLEMENTED (critic out) |
| 64w-13 | 2026-08-17 | WAIT — B4 judge still silent post-ping. fires: 5 | WAIT |
| 64w-12 | 2026-08-17 | WAIT — B4 judge silent at fire 4 → PINGED. fires: 4 | WAIT |
| 64w-11 | 2026-08-17 | WAIT — B4 judge still out. fires: 3 (ping next fire if silent) | WAIT |
| 64w-10 | 2026-08-17 | WAIT — B4 judge still out. fires: 2 | WAIT |
| 64w-9 | 2026-08-17 | WAIT — B4 judge still out. fires: 1 | WAIT |
| 64w-8 | 2026-08-17 | WAIT — B4 honest-data lens LANDED (DisputeBondNext == posted bond while open, verified on orem/3: 64M=32M·2¹ → failedRounds=1; vote-window end NOT readable (open+120,960 blocks, event-only); QuorumFloorOf recomputes at now → only labelable "bar a new dispute faces"; threshold 5001bps unreadable → ">50%" hardcoded with disclosure; weight = epoch-checkpointed coin balance, staked CC votes; eligibility excludes author/answerer/stakers; votes CHAIN-PUBLIC → "sealed tally" never "secret ballot"; outcomes: failed → half disputer bond burns, overturn → answer bond burns whole + comp MINTED senior-queued, uphold → disputer bond burns + answer bond escrowed, nothing moves between adversaries; §7.4 never-surface list incl. voter counts; BUG: DEMO_ME stakes orem/3 → the sample address is an EXCLUDED participant, vote buttons teach a trap; PullCarrot compliant phrasing supplied). Judge LAUNCHED with both lenses. fires: 0 | WAIT |
| 64w-7 | 2026-08-17 | WAIT — B4 honest-data lens still silent. fires: 6 (duplicate next fire) | WAIT |
| 64w-6 | 2026-08-17 | WAIT — B4 honest-data lens still silent post-ping. fires: 5 | WAIT |
| 64w-5 | 2026-08-17 | WAIT — B4 anatomist LANDED (reference Screen 6 anatomy + explicit BANNED list: running "7,240 ORM has voted" turnout meters while sealed, "your share if right" profit talk, loser-bond-paid-to-winner — ours burn; ranked surviving gaps: 1 ballot block stating both outcomes concretely, 2 clock panel (needs vote-close height read), 3 round-history ladder from FailedRounds, 4 quorum rule + failure consequence as prose (meter itself banned), 5 who-pays-whom ladder in burn/carrot semantics, 6 personal eligibility, 7 post-close margin on settled votes (legal — only LIVE tallies banned), 8 voter record (likely unexposed); N/A abstain + weight epochs — mechanics our realm lacks). Honest-data lens still out. fires: 4 → PINGING it. | WAIT |
| 64w-4 | 2026-08-17 | WAIT — B4 lenses both still out. fires: 3 (ping next fire if silent) | WAIT |
| 64w-3 | 2026-08-17 | WAIT — B4 lenses both still out. fires: 2 | WAIT |
| 64w-2 | 2026-08-17 | WAIT — B4 lenses both still out. fires: 1 | WAIT |
| 64w | 2026-08-17 | WAIT — B4 (dispute/vote richness, ref Screen 6) two-lens panel launched: anatomist (ballot presentation, round history, countdown/quorum grammar, banned partial-tally items) + honest-data designer (dispute.gno reads: round/window/bond/threshold/eligibility; sealed-tally compliance — what MUST NOT surface even if exposed; orem/3 demo fields; PullCarrot §7.4 framing). Judge follows. fires: 0 | WAIT |
| 63 | 2026-08-17 | B3 critic delivered (duplicate stopped; clean list: census hand-recount matches, all seven realm status strings route correctly with never-answered-before-^closed required, parseTyped makes the verdict comparisons sound, FailedRounds is a plain field read, ClaimCount=last-id confirmed, burns recomputed exact, "≥" fallback honest because the realm counter is Buy-fed, live never tallies sealed votes, every orem/4 consumer renders correctly — banner/bonus use verdict, chart's "answered no" is historically true, draw shows answerer 0.00 CC, raw view unaffected; harnesses re-run clean). Five findings, ALL RESOLVED: F1 zero-claim live court left the panel at "…" forever (s.claims falsy guard) → guard is now s.claims!=null and fillCourtRecord(0) hides the panel; F2 partial read failures undercounted silently under a full scope label → cr-scope now appends "— N chain reads failed; counts are incomplete" (me/needs precedent); F3 a settled claim with a failed VerdictRoute read was guessed into "closed without a decision" → route "closed" is handled explicitly and a failed route now increments bad instead of polluting a bucket ("a bucket is never guessed"); F4 the new generic .sub rule leaked max-width:54ch into the stat tiles' div.sub at ≤820px → rule scoped to p.sub (doc already was); F5 "· 1 decided" implied the realm's round counter (decidedRounds can exceed 1/claim; failedRounds resets on decide — demo/live/realm mutually consistent) → reworded "· 1 ended in a verdict" (claims-level, exact) in site+doc+harness. INFO ruled no-change: orem/4 record:3 is the answerer's record AT ANSWER TIME (unrendered for settled claims; the overturn resets the answerer's global record, not this historical field). record_test 21/21, buy_test 34/34, node --check clean, both pages 200. B3 CLOSED. Next fire: B4 (dispute/vote screen richness, reference Screen 6) panel. | NOT-CLEAN (5 resolved; B3 closed) |
| 62w-11 | 2026-08-17 | WAIT — duplicate B3 critic pinged at its fire 4; original 11 fires silent. fires: 11/4 | WAIT |
| 62w-10 | 2026-08-17 | WAIT — B3 critics both still out. fires: 10/3 | WAIT |
| 62w-9 | 2026-08-17 | WAIT — B3 critics both still out. fires: 9/2 | WAIT |
| 62w-8 | 2026-08-17 | WAIT — B3 critics (original + duplicate) both out. fires: 8/1 | WAIT |
| 62w-7 | 2026-08-17 | WAIT — B3 critic silent through fire 7 → DUPLICATE launched (identical brief; first delivery wins, late original counts). fires: 7/0 | WAIT |
| 62w-6 | 2026-08-17 | WAIT — B3 critic still silent. fires: 6 (duplicate next fire) | WAIT |
| 62w-5 | 2026-08-17 | WAIT — B3 critic still silent post-ping. fires: 5 | WAIT |
| 62w-4 | 2026-08-17 | WAIT — B3 critic silent at fire 4 → PINGED. fires: 4 | WAIT |
| 62w-3 | 2026-08-17 | WAIT — B3 critic still out. fires: 3 (ping next fire if silent) | WAIT |
| 62w-2 | 2026-08-17 | WAIT — B3 critic still out. fires: 2 | WAIT |
| 62w | 2026-08-17 | WAIT — B3 critic out (census vs dataset, live bucketing vs realm status strings, orem/4 edit consumers, §7.4, doc parity, regression harnesses). fires: 1 | WAIT |
| 62 | 2026-08-17 | B3 IMPLEMENTED per the judge's spec (judge delivered post-ping with orem census recounted from the dataset; two judge slips corrected against ground truth and logged: ledger burned is 2,760,140,980,980 µGNOT — ceil(m²/2e9) computed, judge was +1; my own initial worry that #3's round:1 shouldn't count as failed was WRONG — index.html:1398 already renders round as "after N failed rounds", so the judge's 6 stands). Shipped: (1) scope line — .sub under the court h1, institutional first-person demo copy for orem + ledger, inline label "· demo copy — the chain stores only a name", ABSENT live (folder precedent), refused for directory rows; (2) Court record panel (aside: join → record → senior → four things) — .kv census rows settled undisputed 0 / settled by vote 1 (1 overturned the answer) / closed without a decision 1 / expired unanswered 1 / live now 5 / dispute rounds failed 6 (· 1 decided) / GNOT burned here 7,020,070 GNOT with the keyless-address sub, scope header "of this court's 8 claims", adverse-disclosure fineprint; (3) demo data: orem+ledger desc/burned (burned = exact curve cost of minted), orem/4 now an overturned-by-vote settle (answer:1, draw.ans:0 — openrewards.gno:92 "overturned answer draws NO answerer slice" verified before touching); (4) demoCensus counts the dataset by the same rules the live fill reads; (5) fillCourtRecord live: last-K=20 ids via inChunks(6): ClaimStatus+FailedRounds per claim, VerdictRoute+Verdict+AnswerVerdict on vote-settled (overturn = verdicts differ), never-answered/closed-without bucketing by status string, ≤5K+1=101 qevals bound, CourtBurnedGNOT 1 qeval with ceil(minted²/2e9) "≥ … derived from curve position" fallback, row-hides-on-fail, whole-panel hides if every claim read fails; (6) ClaimCount=nextID VERIFIED to equal the last id (claim.gno:260 increments first) so ids 1..count exactly. Refused (judge + confirmed): post-verdict opposition + median settle days (not derivable — fabrication), "What the coin is worth" (§7.4), directory motto/age/settled chips (no reads). Realm wishlist → Owner notes (CourtCreatedAt/CourtAdmin, CourtCensus, VerdictAt, CourtScope). Harness record_test.js 21/21 (census matches judge exactly, sums to 8, panel rows in order, banned-word sweep) + buy_test.js still 34/34. Doc: Screen 3 scope line + record panel GENERATED from the live functions (gen_record.js), CSS parity, anno extended; divs 249/249; both pages 200; node --check clean. Critic launched. | IMPLEMENTED (critic out) |
| 61w-13 | 2026-08-17 | WAIT — B3 judge still silent post-ping. fires: 5 | WAIT |
| 61w-12 | 2026-08-17 | WAIT — B3 judge silent at fire 4 → PINGED. fires: 4 | WAIT |
| 61w-11 | 2026-08-17 | WAIT — B3 judge still out. fires: 3 (ping next fire if silent) | WAIT |
| 61w-10 | 2026-08-17 | WAIT — B3 judge still out. fires: 2 | WAIT |
| 61w-9 | 2026-08-17 | WAIT — B3 judge still out. fires: 1 | WAIT |
| 61w-8 | 2026-08-17 | WAIT — B3 honest-data lens LANDED (realm-verified: CourtBurnedGNOT(s) EXISTS at modrender.gno:116 — exact per-court burn counter, curve buys only, 1 qeval; census = ClaimStatus + VerdictRoute over ids, N-2N qevals, overturned ⟺ Verdict≠AnswerVerdict; FailedRounds exposed; median settle NOT derivable; identity prose NOT stored — demo labeled sample, live hides; createdAt/admin stored without reads → Owner-notes wishlist; §7.4 sheet: "destroyed at a keyless address, not held; nothing redeems it", outcomes census wording; demo orem burn ≈7.02e12 µGNOT consistent; orem/4 overturn needs demo answer field). UX-cohesion judge LAUNCHED with both lenses. fires: 0 | WAIT |
| 61w-7 | 2026-08-17 | WAIT — B3 honest-data lens still silent. fires: 6 (duplicate next fire) | WAIT |
| 61w-6 | 2026-08-17 | WAIT — B3 honest-data lens still silent post-ping. fires: 5 | WAIT |
| 61w-5 | 2026-08-17 | WAIT — B3 honest-data lens silent at fire 4 → PINGED. fires: 4 | WAIT |
| 61w-4 | 2026-08-17 | WAIT — B3 anatomist LANDED (ranked gaps: 1. scope line under court name — institutional first-person prose, chain stores only name/tier/admin → overlay-metadata honesty like folders; 2. court-health adverse-disclosure panel — turnout failures derivable per-claim (FailedRounds), overturns roughly (Provisional/ProvClose vs verdict), median settle days NOT derivable (no settle timestamp exposed), post-verdict opposition impossible; 3. directory-row richness — settled counts parseable from the Render status text already fetched, motto/age blocked; 4. charter block partially (ClaimSeeded exists, prose doesn't); 5. createdAt/admin stored but NO public accessor → realm-side one-liner for Owner notes; 6. pure-visual .sub style/chips/age-column. §7.4-banned reference panels ignored). Honest-data lens still out. fires: 3 | WAIT |
| 61w-3 | 2026-08-17 | WAIT — B3 lenses both still out. fires: 2 | WAIT |
| 61w-2 | 2026-08-17 | WAIT — B3 lenses both still out. fires: 1 | WAIT |
| 61w | 2026-08-17 | WAIT — B3 (court identity richness) two-lens panel launched: anatomist (reference Screen 2 header/description/Court-health disclosure ethic + directory-row identity vs current site) + honest-data designer (realm identity fields; integrity numbers the chain can answer — terminal-state counts, dispute outcomes, median settle, BurnedGNOT scope, and the derivable per-court burned-by-buys = ceil(minted²/2D) to verify; demo consistency; §7.4 phrasing; live degradation per item). Judge follows. fires: 0 | WAIT |
| 60 | 2026-08-17 | B2 critic delivered (duplicate stopped; original's math pass: curveQuote == an independent int64 oracle of p/curve Minted/Cost on 4000 fuzz cases + X==cost/X±1 boundaries at 9 seed positions incl. cap−1; isqrt exhaustive 0-3000 + b²±1 at 2^50/2^63/2^100; wiring/staleness/non-regression/a11y all VERIFIED CLEAN — focus survives retype, BUYCTX nulled per render, btn/cliCmd byte-identical elsewhere). Five findings, ALL RESOLVED: F1 doc Screen 3 stale outside the regenerated aside — dropped the old top-level Buy/Open/Pull actions row and moved the Four-things primer into the aside matching the live layout (each action now appears exactly once; divs 227/227); F2 ack gate was click-only (middle-click/open-in-new-tab followed the href) — a GATED anchor now renders with NO href (role=link kept for a11y); ticking re-renders with the href; F3 the live no-quote fallback silently lost the acknowledgement — fallback now sets BUYCTX.noquote, keeps the ack checkbox, and gates on ack alone (buying stays possible; the chain computes the true amount); F4 parseGnot unbounded — now rejects > int64 µGNOT ("a send no transaction can carry is not an amount"), and the µGNOT echo + Returned-to-you row print exact BigInt digits via toLocaleString (no >2^53 rounding); F5 "Returned to you" contains "return" — RULED CLEAN, it names the realm's refund and matches the shipped "returns 1×" house copy. Harness extended to 34/34 assertions incl. all four fixes (scratchpad/buy_test.js). Smoke: node --check clean, both pages 200. B2 CLOSED. Next fire: B3 (court identity richness) two-lens panel. | NOT-CLEAN (5 resolved; B2 closed) |
| 59w-11 | 2026-08-17 | WAIT — duplicate critic pinged at its fire 4; original critic 11 fires silent (dead, but still counts if it lands). fires: 11/4 | WAIT |
| 59w-10 | 2026-08-17 | WAIT — B2 critics both still out. fires: 10/3 | WAIT |
| 59w-9 | 2026-08-17 | WAIT — B2 critics both still out. fires: 9/2 | WAIT |
| 59w-8 | 2026-08-17 | WAIT — B2 critics (original + duplicate) both out. fires: 8/1 | WAIT |
| 59w-7 | 2026-08-17 | WAIT — B2 critic silent through fire 7 → DUPLICATE critic launched (identical brief; first to deliver wins, late original still counts). fires: 7/0 | WAIT |
| 59w-6 | 2026-08-17 | WAIT — B2 critic still silent. fires: 6 (duplicate next fire) | WAIT |
| 59w-5 | 2026-08-17 | WAIT — B2 critic still silent post-ping. fires: 5 | WAIT |
| 59w-4 | 2026-08-17 | WAIT — B2 critic silent at fire 4 → PINGED. Duplicate at ~7. fires: 4 | WAIT |
| 59w-3 | 2026-08-17 | WAIT — B2 critic still out. fires: 3 (ping next fire if silent) | WAIT |
| 59w-2 | 2026-08-17 | WAIT — B2 critic still out. fires: 2 | WAIT |
| 59w | 2026-08-17 | WAIT — B2 implementation critic out (math-vs-realm boundaries, DOM wiring/staleness, byte-identity of other call sites, §7.4, doc parity, a11y). fires: 1 | WAIT |
| 59 | 2026-08-17 | B2 IMPLEMENTED per the judge's spec (judge double-delivered post-ping; v1 D1-D20 taken as base per B1 precedent, with two v2 adoptions logged: Adena keeps its confirm prompt PREFILLED from the receipt — preserves the shipped "every argument confirmed" invariant — and the Buy CLI gains a "# one-way: the GNOT is burned; no sell-back" comment; duplicate judge stopped). Shipped: joinPanel receipt — "You send" IS the input (GNOT ≤6dp, string-parsed to µGNOT BigInt, exact echo), rows in .ticket .line vocabulary: You receive (cc + exact units) · Average price you pay · Returned to you (only when >0) · Price after your buy (small "now N") · Your voice share (<0.1% floor); Δ≤0 reads "too small to mint — the realm refuses it and your GNOT never leaves"; mode note (demo sample / live fewer-never-more staleness); ack checkbox gating the one-click paths in live (data-needack preventDefault + txnote, signbtn disabled; CLI never gated); button restates output ("Buy 0.84 CC"); worst-case fineprint. Curve math mirrors p/curve in BigInt (D=1e9, CAP=461,168,601,842,738, ceil-cost, isqrt ±correction) — HARNESS-PROVEN identical to a brute-force largest-Δ reference on 35 position×send cases + realm invariants (no overspend, no underfill, refund exact) + orem 100 GNOT ≈ 0.84 CC + dust-refusal + cap clamp + parseGnot exactness (27/27 assertions, scratchpad/buy_test.js). courtStats: demo minted=supply−emitted (commented exact-only-because-no-burns), live 9th parallel read CurvePosition(slug).catch(null); null → honest fallback (price/supply kv, "No quote — the node did not answer the curve read", shipped prompt-flow paths). btn()/cliCmd() gained optional send/gate params — other call sites byte-identical; adenaSign Buy prompt prefills from data-send. Relocations: "Open a claim" → actions row under Open docket; "Pull senior payout" → Senior queue mini-panel (§7.4-safe copy mirroring the stats-grid sub). Provenance panel DEFERRED to B3 (both judge versions concur). Doc: Screen 3 aside REGENERATED from the live joinPanel via node (scratchpad/gen_join.js; shows the real 0.84 CC quote incl. the conditional Returned-to-you 27 µGNOT row), + senior panel + relocated action row + CSS parity block + anno; balance 228/228. Smoke: node --check clean, both pages 200. Critic launched. | IMPLEMENTED (critic out) |
| 58w-10 | 2026-08-17 | WAIT — B2 judge silent through fire 7 → DUPLICATE judge launched (identical brief; first to deliver wins, late original still counts). fires: 7/0 | WAIT |
| 58w-9 | 2026-08-17 | WAIT — B2 judge still silent. fires: 6 (duplicate next fire) | WAIT |
| 58w-8 | 2026-08-17 | WAIT — B2 judge still silent post-ping. fires: 5 | WAIT |
| 58w-7 | 2026-08-17 | WAIT — B2 judge silent at fire 4 → PINGED (deliver numbered decisions, ≤600w). Duplicate at ~7 if still silent. fires: 4 | WAIT |
| 58w-6 | 2026-08-17 | WAIT — B2 judge still out. fires: 3 (ping next fire if silent) | WAIT |
| 58w-5 | 2026-08-17 | WAIT — B2 judge still out. fires: 2 | WAIT |
| 58w-4 | 2026-08-17 | WAIT — B2 judge still out (both lens outputs in its brief). fires: 1 | WAIT |
| 58w-3 | 2026-08-17 | WAIT — B2 panel complete: honest-curve lens LANDED (realm-verified: d=1e9, Cost=ceil((s1²−s0²)/2d), Buy takes largest Δ with Cost≤sent, refunds remainder, no fee, panics Δ≤0; receipt needs BigInt isqrt; LIVE exact s0 via CurvePosition(slug) qeval — supply−emitted is WRONG live because CC burns shrink supply, but exact in demo (no burns in sample); staleness is monotone-safe "fewer, never more"; §7.4 label sheet). UX-cohesion judge launched with both lens outputs — resolves panel anatomy, three-action-path integration, CurvePosition wiring+fallback, provenance-panel defer, degradation. fires: 0 | WAIT |
| 58w-2 | 2026-08-17 | WAIT — B2 panel: anatomist LANDED (5-row causal receipt You-send/You-receive/avg-price/price-after/share; button restates computed output "Buy 236.6 ORM"; single-irreversibility ack checkbox gating the button; fineprint names worst case; reference middle panel "What the coin is worth" is a §7.4 VIOLATION — backing/treasury language; replace with burned-to-date provenance or integrity numbers since our GNOT is burned; drop crammed-in extra buttons from panel). Honest-curve designer still out. fires: 2 | WAIT |
| 58w | 2026-08-17 | WAIT — B2 (buy receipt) design panel out: join-panel anatomist (reference Screen 2 aside vs current panel) + honest-data designer (realm curve math: exact units/avg-price/price-after/voice-share formulas, s0 recoverability per mode, staleness caveat, §7.4 row labels). Judge runs when both land. fires: 1 | WAIT |
| 57 | 2026-08-17 | B1 critic delivered (math replicated numerically: y() exact, pad kills zero-span, anchors exact after snap for all 9 claims with snap indices provably interior, flat orem/4 renders in [88.3,91.1], ansH≥d0 by construction, future settleAt inside plot, marker-collision estimate corrected — 37px apart not 4, escaping complete, deterministic, orem/8+ledger fine). Four findings, all APPLIED: F1 retirement completed — orphaned .ratio/.poolrow CSS stripped from the site (0 refs), and the DOC's Screen 4 chart is now GENERATED FROM THE REAL signalChart in node (figure+chips pasted from actual output — the doc can no longer drift) with the doc's old chart CSS replaced by the site's current block; F2 right ticks now suppress themselves within 11px of the end label (6 of 9 demo claims had endL/tick collisions at inst≈50/100); F3 mobile SVG text raised to the legibility floor (20/18/26px → ≈9.4/8.5/12.2px at the 0.47 scale); F4 demo endS now follows phase ("frozen" when not open) like live — no more "YES now" under a frozen header. Smoke: node --check clean, both pages 200, doc divs balanced. B1 CLOSED. Next fire: B2 (buy panel receipt) design panel. | NOT-CLEAN (4 applied; B1 closed) |
| 56w | 2026-08-17 | (hold) B1 critic out (geometry math, anchor exactness, flat-series, event clustering, escaping, retired-code sweep, 375px scale, orem/8/ledger render). Fires: 6, pinged. | WAIT |
| 56 | 2026-08-17 | B1 CONVERGED and IMPLEMENTED. Judge's spec (first completed delivery; a re-generated variant differed on two minor points — pools/fallback retention — resolved in favor of the first: ratioBlock retired, chips replace pools, chart handles immaturity inline): one .bigchart component, 640×150 wireframe frame, axis restraint 0/50/100 + right ticks, event anatomy (dashed vertical + hit r=10 with <title> + dot + word label, alternating), zone strip, CVD ink-not-color. DEMO: mulberry32(fnv1a(slug/id)) 49-pt synthesis, anchors EXACT, line + 10% area + end halo + srcnote honesty line under the SVG. LIVE: NO line/fill (different-window aggregates); dashed lifetime ref + trailing-week band (or "not enough history") + now dot; real event heights (answered=SettleDeadline−51840, settle, escrow, now) with washes (72h, reopenable, scheduled-future); block-height zone labels with ≈. chartChips row (stakes, conviction, failed rounds, next bond, verdict·route, rewardsOpened). HEADLESS-TESTED in node: 4 demo phases OK/deterministic/sample-labeled with correct marker counts; live = no-line + ref + band OK; immature → note; missing height → empty. Dead ternary self-caught. OWNER RULING recorded: the left rail stays. Smoke: node --check clean, both pages 200. NEXT: critic verify + wireframes.html Screen-4/5 reconciliation. | NOT-CLEAN (B1 implemented) |
| 55w | 2026-08-17 | (hold) B1: anatomist DELIVERED (10 ranked items: density, 4-sibling event-marker anatomy w/ hover+title, area, end dot halo, two-ended figcaption, zone seams, axis restraint 0/50/100-only, CVD ink-not-color events, wide aspect + narrated aria). Honest-data designer DELIVERED (live: aggregate BANDS not a connected line — the current polyline is dishonest live; lifetime dashed reference + trailing-week band + now dot + REAL event rules/zones from SettleDeadline/EscrowUntil/status height, no opened marker live; demo: mulberry32(fnv1a(slug/id)) N=49 anchored synthesis, exact anchors, §7.4-safe sample label). JUDGE launched with both outputs. Fires: 5, pinged. | WAIT |
| 55 | 2026-08-17 | PHASE 3 opened per owner directive (reconcile all screens to the wireframes' richness, strictly-better rule; detailed graph per claim is the priority; converge-before-implement via subagents is MANDATORY). Cron 8cd9c260 (1m) created; backlog B1-B7 seeded; honesty rule written (demo may synthesize dense series iff deterministic + anchored to the three true ratios + labeled sample; live only real reads/heights). B1 design panel LAUNCHED: wireframe-chart anatomist + honest-data designer in parallel; UX-cohesion judge synthesizes both into the spec next. No implementation until the spec converges. | PANEL OUT (B1) |
| 54 | 2026-08-17 | Verifier agent stopped by owner mid-run; self-verified the sharp edges instead: isDone tested against all 7 realm status strings (5 open / 3 done, "settles undisputed" stays open, provisional stays contestable — node run in log); crumbs() escapes every trail label via esc() (folder names safe), crumb hrefs code-constructed from route-constrained charsets; folder meta nesting by construction; JS parsed + both pages 200 (round 53). Owner Q&A: folder moves/counts are website-only — the realm stores no folder field, counts are render-time derived, no transaction exists. | verified (self) |
| 53 | 2026-08-17 | OWNER: court page restructured to the reference's SCREEN 2 layout (owner's stated preference; also the scale answer — a long docket never nests). Folders are now a compact CLICKABLE list at top (accent-wash rows, folder icon, name + claim/subfolder counts, count in the px column) linking to NEW folder pages (route #/c/:slug/f/:path, demo-only with an honest live/absent message); folder pages show subfolder rows + member claims; the docket is SECTIONED — "Open docket" and "Recently settled" (isDone on the status classifier; "settles" never matches /settled/) — with each claim row carrying its folder path as meta ("Municipal record · Elections"); two-column .cols layout with a right aside holding the "Join this court" panel (burn copy §7.4-safe — the reference's "backing per coin" language deliberately NOT adopted, banned) + price/supply kv + the three money actions (moved off the header) + the four-things primer; inline nesting (frow/docketGroups/ghost Unfiled) retired — unfiled claims simply carry no folder meta; depth param dropped from docketRow in favor of the meta line; .cols collapses at 820px; live spark fill untouched (ids stable across sections). Charter block: JUSTIFIED-DIFFERENCE (no realm field). Smoke: node --check clean, served 200. | NOT-CLEAN (owner-directed) |
| 52 | 2026-08-17 | OWNER: subfolders + unfiled claims made visible. Renderer is now recursive (folderBlock: nested folders indent 18px/level on a lighter wash, counts are recursive totals; member rows share their folder's indent); claims in NO folder trail under a transparent "ghost" Unfiled header (muted, faded icon) so not-under-folders reads explicitly. Demo restructured to show all three shapes: Municipal record (3) → #4 #5 + subfolder Elections (1) → #1 indented; Infrastructure → #3 #7; Filings & audits → #6 #8; #2 deliberately unfiled. Live stays flat (no on-chain folders). Doc Screen 3 rebuilt to match (line-count balanced), anno updated. Smoke: node --check clean, both pages 200. | NOT-CLEAN (owner-directed) |
| 51 | 2026-08-17 | Hero-chart verifier (edges valid incl. p=100 ascender under overflow:visible, "100%" end label fits R=70, all 9 demo claims chart, header ternary coherent for every phase, live-null fallback list confirmed, pools fields unconditional, XSS clean — numbers/fixed strings only, doc Screen 4 exact to the decimal) | Three findings, all APPLIED: F1 the panel printed staked amounts twice (my .pools + the pre-existing .poolrow kv pair) → poolrow now renders only in the chartless fallback; conviction format aligned to the doc ("9.40 / 2.60 CC"); F2 at 375px the ×0.54 viewBox scale rendered 10px SVG text at ~5.4px → mobile media bump (tick/xl/pv 15px, endS 14, endL 22 → ~8-12px rendered); F3 claimSeries's null guard passed NaN (malformed qeval string → y="NaN" coords) → Number.isFinite guard on all three (node-tested: NaN week → null, good series unchanged). Smoke: node --check clean, served 200. | NOT-CLEAN (3 applied) |
| 50 | 2026-08-17 | OWNER DIRECTIVE: the claim page's hero is now a BIG signal chart (the Polymarket treatment) — signalChart(d) draws the same three honest series the docket sparkline plots (lifetime conviction → trailing week → instantaneous YES%) at 560×210: 0-100% gridlines with a dashed 50% midline, accent line + 10% area fill, labeled mid points, end dot with the large % + "YES now", x-station labels, role=img aria-label carrying all three values; a mono pools line beneath (staked YES/NO + conviction split); the two small ratio bars remain ONLY as the fallback when a live series is immature (ratioBlock unchanged). Doc Screen 4 mirrored with orem/1's exact coordinates (y: 78.3→50.7, 74→57.6, 77.4→52.2 — node-verified). Smoke: node --check clean, both pages 200. | NOT-CLEAN (owner-directed) |
| 49 | 2026-08-17 | Folders/alignment verifier (both files; INDEX FULLY CLEAN — spark x pinned across all 8 rows, widest chip fits, 820px placements correct for both row types, folder ids cover 1-8 exactly once, frow border logic single-line, live path flat, ledger flat, icons valid/decorative, non-crow surfaces untouched) | Two findings, BOTH in my own doc regroup and BOTH from the same regex-over-HTML mistake (non-greedy .*?</div> truncated 7 of 8 rows mid-cell and spliced #2's title onto #8's data) → Screen 3's docket rebuilt wholesale from the demo data table: 4 folder headers + 8 complete rows, correct pill/series pairs (#2 = answered 93.5%, #8 = never answered 57.1%); file div count now balanced 210/210. Site untouched this round. | NOT-CLEAN (doc repair) |
| 48 | 2026-08-17 | OWNER DIRECTIVE: (a) ALIGNMENT — each docket row is its own grid, so auto-width chips shifted the spark column per row; the px and chip tracks are now fixed (64px / 124px, chips+px right-justified) so sparklines and chips align down the whole docket (courtrows share the 124px chip track); (b) FOLDERS — overlay-level grouping (the realm stores no folder field): DEMO orem gains four folders (Municipal record 1/4/5 · Infrastructure 3/7 · Filings & audits 6/8 · Aerospace 2), docketGroups() renders blueish accent-wash folder header rows (serif name + mono count) with member claims beneath, an Unfiled group catches strays, LIVE renders flat until the realm carries folders — stated honestly in code comment and doc; (c) ICONS — two inline SVGs: folders get the folder glyph in accent-indigo on the blue wash (per the original wireframe), courts get a classical-building court glyph in escrow-gold on plain surface, on directory rows and distinct from folders. wireframes.html mirrored (Screen 3 regrouped, Screen 1 court icons, same tracks/CSS, anno updated). Smoke: node --check clean, both pages 200. | NOT-CLEAN (owner-directed) |
| 47 | 2026-08-17 | Reskin SIGN-OFF sweep (both files in full; mobile courtrow/crow interaction proven conflict-free by source-order trace, the wrap fix shown to also cover the long fallback copy, page-h anywhere-breaking proven cosmetically inert, doc/site agreement verified to the spark y-coordinates; two nits challenged out) | ZERO findings. RESKIN SETTLED: sparklines per claim (the owner's selling point) + the wireframe's records-office look landed across rounds 43-47 — one owner-directed build round, then fidelity, regression, final-verify, and sign-off critics, 14 verified findings fixed en route, zero logic changes, AA held throughout. courtv2-moderation may commit per the handoff directive. | CLEAN — reskin batch closed |
| 46 | 2026-08-17 | Reskin final verifier (contrast recomputed both themes — all pill pairs pass, tightest 4.96; shadow-hairlines proven 1px at every wrap; async-fill shape, seal line-box, token agreement CONFIRMED) | Four findings, all APPLIED: V1 the mobile two-line crow's row 2 could overpaint the widest pill at ≤375px (spark is unshrinkable 60px) → flex-wrap on the mobile px; V2 my overflow-wrap fix was scoped to crow titles only — needs rows and the claim .page-h stayed crushable by one long word (the exact targeted failure) → .docket .t + .page-h now both break-anywhere; V3 the wireframes doc missed the round-2 redraw → Screen 1 rows redrawn as courtrows (/slug id, meta, px price, chip), court h1 gains the seal, stats switched to the shadow-hairline treatment (doc now agrees with site); V4 the 820px crow rule clipped courtrow ids at 44px ("/led…") → courtrow keeps minmax(44px,auto) with the 110px cap on mobile. OPTIONAL (blank live spark conflates error with no-history) DECLINED — blank asserts nothing false; the row's pill/title still render. Smoke: node --check clean, both pages 200. | NOT-CLEAN (4 applied) |
| 45 | 2026-08-17 | Reskin regression critic (13 tool uses; re-read after the mid-review fidelity edits — verdict against current bytes; all 28 tokens present in all four blocks, needs/empty rows unbroken, sparkSvg valid + ids match, zero stray hex CONFIRMED) | Four findings, all VERIFIED and APPLIED: R2 the .crow 4-column row crushed titles at 375px (title track ~36-46px; long words overpainted the spark since minmax(0,1fr) allows sub-min-content) → overflow-wrap:anywhere + a two-line phone restack inside the 820px block (id spans rows; title takes the full width; spark+% and chip sit beneath) + live-slug id capped at 110px ellipsis in courtrows; R3 AA — light escrow text on its wash was 4.28:1 (the reference's own pairing!) → #7d5c0f (4.96 on wash, 5.28 on paper) and light muted dipped to 4.45 on hover → #56646f (4.93 hovered), both node-computed, doc tokens mirrored; R4 the fused hairline stat grid leaked solid gray filler cells when 4 tiles wrapped onto 3 columns → per-tile 1px outline shadows (hairlines between tiles coincide; empty wrap cells stay pure paper); R1 meta scope-note acknowledged — the directory rebuild was the fidelity critic's owner-driven F2 landing mid-review, and --shadow tokens are now defined-but-unused (kept: harmless, four-block removal is churn). Smoke: node --check clean, both pages 200. | NOT-CLEAN (4 applied) |
| 44 | 2026-08-17 | Wireframe-fidelity critic (read all three files; classified SHOULD-ADOPT vs JUSTIFIED) | All six items APPLIED: F1 section heads are now the reference's small-caps ruled labels (19px serif → 600 11px uppercase + hairline, mono counts); F2 the directory dropped lifted cards for docket rows (/slug id column, name + claims meta, right-aligned mono price px, tier chip; async fill + failure copy reshaped to match; wider id column via .courtrow); F3 flattened — hover lift/shadow gone, ratio meter squared (2px + hairline border), the three 6px radii → 3px; F4 notices carry the reference's semantic 1px border + 4px left stamp (warn/good/void; refuse normalized); F5 records density — body 15px, mono crumbs, and a .seal chip with the court slug beside the court h1; F6 the DOC's two disagreements fixed (stats fused to the hairline grid; all 8 claim rows gained the #id column, "claim #N" meta dropped). JUSTIFIED differences confirmed and kept: functional left rail, no per-court sparklines (no stored series), no folders/argument map (title-only claims), no order book (§7.4). Regression critic still out — its survivors apply next. Smoke: node --check clean, both pages 200. | NOT-CLEAN (6 applied) |
| 43 | 2026-08-17 | OWNER DIRECTIVE (post-convergence): (a) commit request relayed to courtv2-moderation via BOTH handoff files; (b) SPARKLINES restored — the owner's stated selling point: each docket claim row now carries a 60×16 spark of the three ratio series the realm actually keeps (lifetime conviction → trailing week → instantaneous YES%; no invented history) + the instantaneous % in mono, demo synchronous, live filled async per row (4 tuples, chunked 6, failures stay blank — never lie); (c) RESKIN to the grc20vote wireframe's records-office language: cold archive paper + fountain-pen indigo tokens across all four theme blocks (ALL AA-checked in node: muted 4.71/6.17, accent-on-ink 10.4/7.9 — better than amber), 3px radii, square bordered uppercase chips replacing rounded pills, hairline 1px-gap stat grid with mono figures, wireframe claim-row anatomy (#id | serif title | spark+pct | chip) as a new .crow class so needs/directory rows are untouched, masthead-style 2px ink rule under the directory lead, wireframe yes-green/no-red semantic inks; (d) wireframes.html reconciled — same tokens, chips, and all 8 docket rows sparked with the computed series. Justified differences kept: the left rail (config/wallet/nav is functional; the wireframe had none), no court-row sparklines (the realm keeps no per-court price history — an honest series does not exist), colored yes/no BUTTON variants (transaction semantics). Zero logic changes; classifier/escaping/routes untouched. Smoke: node --check clean, both pages 200, series math node-verified (orem/1 78.3→74→77.4). | NOT-CLEAN (owner-directed reskin) |
| 42 | 2026-08-17 | OWNER REQUEST (post-convergence): built web/wireframes.html — the shipped site's 13 screens drawn as static annotated frames in the courts-wireframe.html idiom (masthead, chrome bars, figcaptions, footer), using the site's own tokens and the exact demo data (all realm-producible figures). Includes the three-path action anatomy, every claim state incl. the two no-verdict endings, me/needs, and the raw chain render; footer records provenance + the §7.4 never-shown list + why the grc20vote argument-graph/folder screens have no analog (title-only claims). Zero JS, works from file://. Served 200. | NOT-CLEAN (new file, owner-directed) |
| 41 | 2026-08-17 | FINAL verification sweep #11 = sweep #27 (24 tool uses; full contract-fidelity re-verification: enumeration, all 25 write functions by name+order, read tuple orders, all 7 status strings through the classifier, FlagState/route tokens, strip-gate, purge gates both paths, constants, README claims incl. §7.4 — ALL RESOLVED CORRECT; "ClaimTitle purge-gated was my last open thread; every candidate resolved") | ZERO findings. OPTIONAL ("backed by a bond"→"secured" for grep-clean §7.4) DECLINED — the reviewer itself judged the actual rule targets investment framing, which an answer bond is not. Duplicate final sweep stopped. No file touched. | CLEAN (streak 4/4) — **CONVERGED** |
| 40w | 2026-08-17 | (hold) FINAL verification sweep #11 out (4/4 — convergence round if clean). Fires: 8; original silent past ping, DUPLICATE launched. Fires: 11. | WAIT |
| 40 | 2026-08-17 | Streak sweep #10 = sweep #26 (15 tool uses; novel attack: READ-PATH contract fidelity — all ~35 live qeval expressions vs realm BODIES: tuple orders, Provisional int -1 sentinel (would have broken a bool assumption — it is not), FlagState/VoteDispute enum strings, 1..ClaimCount id-space, block-height units, BLOCK_SECS=5, trailing numerator/denominator pairing, AnswerRecord/StakeOf arg orders — ALL FAITHFUL) | ZERO findings. One OPTIONAL (gate OpenFlag during rare cooldown/consumed FlagStates) DECLINED as consistent with the established button philosophy — conditions in subs, realm enforces (same pattern deliberately kept for the counter-window trio and pull buttons; no adjacent copy contradicts). No file touched. | CLEAN (streak 3/4) |
| 39w | 2026-08-17 | (hold) Streak sweep #10 out (verification 3/4, untaken-attack brief). Fires: 6, pinged. | WAIT |
| 39 | 2026-08-17 | Streak sweep #9 = sweep #25 (14 tool uses; chosen attack: TRANSACTION-ARGUMENT FIDELITY — what the three signing paths actually transmit vs realm exports, the sign-a-valid-but-WRONG-tx class; all 26 buttons + ~35 reads cross-checked: names, param names, positional order, yes=overturn, sideYES=0, sentinel -1, inclusive scan, strip-gate leak re-test, purge routing, constants, tuple orders — ALL EXACT) | ZERO findings. Optionals are prior-declined cosmetic repeats (demo statusText wording; errorView title on realm-level panics). No file touched. | CLEAN (streak 2/4) |
| 38w | 2026-08-17 | (hold) Streak sweep #9 out (verification 2/4, pick-your-own-attack). Fires: 6, pinged. | WAIT |
| 38 | 2026-08-17 | Streak sweep #8 = sweep #24 (17 tool uses; all five round-36/37 changes PASS with realm citations — carrot sub matches openrewards.gno:313's primary path, pull buttons correct through gnoweb/CLI/Adena incl. insertion-order args, cc(0) safe, snapshot sentence exact per governor.gno:697 epoch pinning ("if anything conservative"), GNOT/unit glosses accurate vs coinDecimals=6; free pass: 23 buttons, VoteDispute semantics, FlagState, ClaimCount, Render regexes incl. un-linked overflow lines, isParticipant set equality — ALL CONFIRMED) | ZERO findings. One OPTIONAL (mark the voters figure as the remaining pool with one word "left") DECLINED per taste-decline precedent — the pools line is informational and the realm's own render shows the same asymmetry. No file touched. | CLEAN (streak 1/4) |
| 37w | 2026-08-17 | (hold) Streak sweep #8 out (pull-button CLI/Adena paths, carrot-zero render, snapshot-sentence accuracy + free pass). Fires: 7, pinged. | WAIT |
| 37 | 2026-08-17 | Sweep-23 duplicate delivered on fire 3 (19 tool uses; angles: how-it-works vs whitepaper + first-timer jargon) | Four findings: D1 my round-36 "quality voters" sub on PullCarrot was WRONG — the realm pays with-verdict voters of the DECIDING ROUND first (openrewards.gno:313 panic string verbatim), quality voters only fallback; demo orem/4 is vote-decided so the label misled in the exercised case → "deciding-round voters", and the fetched-but-never-rendered carrot pool now shows in the draw line; D2 WHITEPAPER ERRATUM (code sides with the page): the hedging analysis presumes unstaked coin still votes but isParticipant excludes the address wholesale → owner-noted (whitepaper not in this loop's writable set); D3 GNOT never glossed (now "(the chain's native coin)" at the intro's first use) and "unit" undefined (price sub now says 1 CC = 1,000,000 units); D4 the whitepaper's central seal — epoch-snapshot voting weight — appeared nowhere on the page → how-it-works dispute ¶ gains "voice bought mid-vote counts for nothing." Smoke: node --check clean, served 200. | NOT-CLEAN (4 applied + erratum noted) |
| 36w | 2026-08-17 | (hold) Sweep-23 duplicate still out on non-overlapping angles (how-it-works vs whitepaper, primer, theme, raw) — kept running, drives round 37. Fires: 3, pinged. | WAIT |
| 36 | 2026-08-17 | Streak sweep #7 = sweep #23 delivered on fire 8 (28 tool uses; ~40 qeval reads + 22 tx functions re-verified by name/args/order, VoteDispute yes=overturn per dispute.gno:225, constants, Render parsing incl. the KRT:SYM tail, §7.4 zero-hit audit, esc/safeInline all-sites audit, theme override both directions, "immutable title" verified — EditClaimTitle barred once staked, demo no-network confirmed) | STREAK BROKEN at 1 — one LOW finding + one accepted optional APPLIED: BB1 earned money was rendered but not pullable through the overlay (AuthorBonus/AnswererBonus/PullCarrot/PullSenior are explicit pull txs with no button anywhere, while the positions view promises "actions match its state" and the owner directive wants a CLI helper on EVERY action) → rewardsOpened claims now offer Pull author bonus / Pull answerer bonus / Pull voter carrot (subs name who), the court page offers Pull senior payout (seq = queue ticket placeholder); OPT-accepted: cleanCfg now admits shell/href-safe PATH segments so reverse-proxied nodes (https://host/rpc) are not silently snapped to default (node-tested: paths pass, quotes/semicolons still rejected); OPT-declined: three-state theme toggle (scope creep). Streak resets 0/4. Smoke: node --check clean, URL charset tested, served 200. | NOT-CLEAN (2 applied) |
| 35w | 2026-08-17 | (hold) Streak sweep #7 out (verification 2/4; free lens with under-weighted-angle pointers). Fires: 8; original silent past ping, DUPLICATE launched. Fires: 10. | WAIT |
| 35 | 2026-08-17 | Streak sweep #6 = sweep #22 (17 tool uses; the round-34 economics independently re-derived and CONFIRMED EXACT — bonds = 50%·X̄, dispute bonds = 40%×2^r incl. the maxFailedRounds−1 shift cap, convictions under the 19.27% derivation from maxMidGrossBps, flag bond = 2%·X̄ above flagMinCC, positions ≤ pools everywhere, lifetime bars coherent with trailYes, no other reader of old values; free pass re-confirmED 21 buttons, scan, FlagState, status classifiers, tuple orders, SettleUndisputed-during-flag legality) | ZERO findings. One OPTIONAL (orem/2 lacks a flagBondNext so OpenFlag's bond sub-label shows only live; orem/6's never renders) DECLINED as cosmetic demo enrichment — the button renders and works offline, only the sub-label amount is absent; consistent with prior taste declines. No file touched. Duplicate stopped. | CLEAN (streak 1/4) |
| 34w | 2026-08-17 | (hold) Streak sweep #6 out (independent re-verify of recomputed economics + fully free final pass). Fires: 8; original silent past ping, DUPLICATE launched. Fires: 9. | WAIT |
| 34 | 2026-08-17 | Streak sweep #5 = sweep #21, the FULL demo-economics audit (18 tool uses; round-33 supplies/prices CLEAN — floors verified robust to ~100 CC of burns, stakes 0.65% of supply sane, demoRender wording verbatim vs render.gno:114, answerability floors respected; orem/4 draw re-verified realm-exact; settleAt/escrow offsets, F9 zero-stake-conviction position, provClose at maxFailedRounds all CONFIRMED) | Three findings, all APPLIED with node-verified recomputation: E1 HIGH every bonded claim's conviction exceeded the realm's hard ceiling (~19.25% of X̄ from the deploy invariant; demo ran ~53%) and two answer bonds sat below the 1.6×conviction floor (quality.gno:525) → convictions recomputed under cap for orem/2/3/6/7 (all pass cap, 50%-X̄ floor, AND 1.6×conv floor with existing bonds kept; positions still ≤ pool convictions incl. orem/6 cn); E2 HIGH all four disputeBondNext values contradicted disputeBond0 = 40%·bond₀×2^min(failed,2) (dispute.gno:353) → 28/64/23.6/102.4 CC exact; E3 LOW orem/6 flagBondNext implied a different X̄ than its own bond → 2%·X̄ = 2.36 CC. OPT demoRender court page gained the realm's "- coin: SYMBOL" line. orem/5 supply-growth tightness noted possible, left. Duplicate audit stopped (redundant). Streak stays 0/4 — the demo-economics class should now be drained wholesale rather than one-per-sweep. Smoke: node --check clean, all constraints re-verified in node, served 200. | NOT-CLEAN (4 applied) |
| 33w | 2026-08-17 | (hold) Streak sweep #5 out (FULL demo-economics audit — every DEMO number vs "could a real court produce this?"). Fires: 8; original silent past ping, DUPLICATE launched. Fires: 9. | WAIT |
| 33 | 2026-08-17 | Streak sweep #4 = sweep #20 (12 tool uses; round-32 fixes all HOLD — min-width:0 breaks no child, ×160 slices realm-exact vs open the rewards splits/tier caps/reservoir, dead query gone; 40+ qeval/tx signatures and tuple shapes re-CONFIRMED; layout fits at 375/768/1280) | ONE finding + one optional, both APPLIED: AA1 MED my round-32 supply raise left demo prices stranded off the realm's curve — CoinPrice = floor(curve-sold/1e9) µGNOT (court.gno:538, curveDenom 1e9), so orem's honest price at 1,450 CC was 1 µGNOT (not 118) and ledger's 74 was impossible at any burn history → supplies scaled onto the curve (orem 118,500 CC; ledger 74,300 CC — node-verified both floor to exactly 118/74 after emitted; escrow ~1,082 CC ≪ supply ✓); OPT price labels now say "µGNOT/unit" at both display sites (whole-CC vs base-unit ambiguity). Streak stays 0/4 — but note the finding was a knock-on of MY OWN prior fix, not new page rot; the underlying sweep verified everything else clean. Smoke: node --check clean, curve math verified, served 200. | NOT-CLEAN (2 applied) |
| 32w | 2026-08-17 | (hold) Streak sweep #4 out (min-width:0 blast radius, 1450 CC consistency, full layout+arithmetic at 3 widths). Fires: 7, pinged. | WAIT |
| 32 | 2026-08-17 | Streak sweep #3 = sweep #19 (9 tool uses; the round-31 grid fix verified correct through the whole cascade — grid-row only at 3 sites, sole sticky unset at 820, zero z-index, tables wrapped; ClaimCount/status-classifier/trailing-tuples/20 tx signatures/DrawSlices order re-CONFIRMED) | Two findings + one precision note + one dead rule, all APPLIED: Z1 MED the CLI block forced page-wide horizontal scroll at 375px — .cliblock had overflow-x:auto but the flex min-content chain (pre's ~450px longest line) refused to shrink → .act{max-width:100%;min-width:0} + .cliblock{min-width:0;max-width:min(640px,100%)} so the block scrolls INSIDE itself; Z2 LOW demo orem's coin arithmetic was impossible (staked ~770 CC + bonds ~273 CC vs supply 642 CC) → supply 1,450 CC; Z3 my ×160 answerer slice (102,720) was unproducible by the realm's floor(D·5/93) — exact value for D=1,912,080 is 102,800 (node-verified: 1,644,800/164,480/102,800 all exact) → fixed; dead 760px stakewrap query removed (820 covers it). OPTIONAL demo closed-statusText wording kept — it is TRUER than the realm string (fee does burn; realm wording already in Owner notes). Streak stays 0/4. Smoke: node --check clean, slice math verified, served 200. | NOT-CLEAN (4 applied) |
| 31w | 2026-08-17 | (hold) Streak sweep #3 out (rendered-layout focus: all media queries/overflow/sticky/z-index at 375/768/1280; ×160 draw consistency). Fires: 7, pinged. | WAIT |
| 31 | 2026-08-17 | Streak sweep #2 = sweep #18 (11 tool uses; every tx button's names AND positional order re-verified across all 21 functions; id-scan, sentinels, docket classifier incl. the settles/settled near-miss, dead-claim copy, constants, purge gates — all CONFIRMED) | STREAK BROKEN at 1 — two findings, both VERIFIED and APPLIED: Z1 MED the ≤820px breakpoint collapsed the grid to one column but .rail and .main stayed pinned grid-row:1, so CSS auto-placement packed them side-by-side into an implicit second column — the stack the breakpoint was written for never rendered on any phone; caught only by reasoning about a RENDERED viewport (17 code-path sweeps missed it) → .rail,.main{grid-row:auto} inside the query; Z2 LOW draw slices were the one coin figure in raw base units beside cc()-formatted stakes (1e6 scale confusion) → cc()-formatted, and demo draws scaled ×160 (exact 80:8:5 ratio preserved, sum within the 2.1 CC demo reservoir; cc() renders 1.64/0.16/0.10 CC — the old values would have shown 0.00). OPTIONAL (Adena DoContract gasFee/gasWanted — unverifiable offline, browser test) added to the owner manual-pass list. Streak resets: 0/4. Smoke: node --check clean, served 200. | NOT-CLEAN (2 applied) |
| 30w | 2026-08-17 | (hold) Streak sweep #2 out (under-weighted angles: Adena end-to-end, CSS state combos, copy vs whitepaper, demo arithmetic). Fires: 7, pinged. | WAIT |
| 30 | 2026-08-17 | Sweep #17 (12 tool uses; {slugs,degraded} consumer audit PASS — both consumers incl. go()/Enter re-query paths handle the shape, no residual array use, tier probe 0/1/2 spans the realm's exact domain, zero-slug edge unreachable since init always creates meta; meSeq/needsSeq leak-free in both completion orders; free lens chased ClaimCount, status classification, strip headings, directory format, sentinel, purge gates, tuple orders, AnswerRecord arg order — ALL COLLAPSED) | ZERO findings. One OPTIONAL (degraded-warning says "reload" though a capped fallback may persist) DECLINED consistent with prior optionals — "may be incomplete" is the load-bearing clause and the transient-failure case IS reload-curable. No file touched this round. | CLEAN (streak 1/4) |
| 29w | 2026-08-17 | (hold) Sweep #17 out ({slugs,degraded} consumer audit + free lens). Fires: 6, pinged. | WAIT |
| 29 | 2026-08-17 | Sweep #16 delivered on fire 9 (32 tool uses; orem/7 conviction-only trace PASS end-to-end incl. against-first sort; slug-regex proven SAFE — ListByTier returns realm-enforced [a-z0-9-] slugs, type suffixes unquoted; total-failure path proven honest via route-level errorView; 21 arg orders, docket keywords incl. "settles"≠"settled", unesc↔inlineEscapeSet exact match re-CONFIRMED; duplicate stopped mid-read as redundant) | Two findings, both APPLIED: Y1 MED the ListByTier→directory fallback silently degraded to the capped/moderated universe with NO failure counted — the views then printed the definitive "no positions found" while omitting hidden/51st+ court money, breaching their own no-conclusions rule → allCourtSlugs returns {slugs,degraded}; a degraded enumeration seeds the failure counter so the "may be incomplete" warning shows; Y2 LOW cross-address paint race (query addr A, re-query addr B mid-flight; last finisher wins under the wrong input) → per-view meSeq/needsSeq tokens; a superseded query abandons the box before final paint. OPTIONAL demo statusText wording drift noted as cosmetic, declined. Smoke: node --check clean, served 200. | NOT-CLEAN (2 applied) |
| 28w | 2026-08-17 | (hold) Sweep #16 out (orem/7 conviction-only trace, slug-regex stress, allCourtSlugs failure modes, free lens). Fires: 8; original silent past ping, DUPLICATE launched. Fires: 9. | WAIT |
| 28 | 2026-08-17 | Sweep #15 (19 tool uses; both round-27 changes PASS — conviction survives full unstake per stake.gno:186/openrewards.gno:186, units consistent, demo membership unchanged, against-first sort stable; ClaimTitle purge-gate, status regexes, ClaimCount, sentinel re-CONFIRMED) | Two findings + optional, all APPLIED: X1 the needs flag judged against the ANSWER, but a successful dispute flips the standing verdict (dispute.gno:292) — during a reopen window the winners saw green "matches you" and the newly-losing side (who may withdraw 1× now) saw ⚑ → against now compares the standing verdict (provisional>=0? provisional : answer) and the row label says "provisional NO" during escrow; X2 the money views' COURT universe was the capped (50/section), moderation-filtered directory render — the confident-false-negative class the by-id claim scan fixed one level down → allCourtSlugs() enumerates via qeval ListByTier(0..2) (directory.gno:74, unbounded, includes hidden) with permissive quoted-token extraction and graceful fallback to the directory when the slice syntax is opaque; me/needs iterate slugs with purge-gated names fetched lazily only for courts that contribute rows; OPT demo gains a conviction-only position (orem/7) exercising the round-27 branch. Self-caught: a dead d.standing reference in the new label (harmless fallback) cleaned before any critic saw it. Smoke: node --check clean, served 200. | NOT-CLEAN (3 applied) |
| 27w | 2026-08-17 | (hold) Sweep #15 out (cy/cn re-verify + free-lens finale). Fires: 7, pinged. | WAIT |
| 27 | 2026-08-17 | Sweep #14 (11 tool uses; BOTH round-26 changes PASS with careful traces — "## Not found" provably cannot match the # heading regex so bad slugs fall back to the esc'd slug; purge-gating switch confirmed closing a real leak; all six hasOwn sites verified, composite keys proven prototype-safe; 21 buttons, FlagState, tiers, phase precedence, regexes, no-network claim all re-CONFIRMED) | ONE LOW finding + one optional, both APPLIED: W1 the needs view skipped conviction-only holders — but conviction survives an honest unstake and the realm pays on stake+conviction>0 (render.gno:343), so the exact person the view exists for ("stakers whose reward it would deny") saw "Nothing needs you" after honestly unstaking → filter and side-weighting now include cy/cn; W2 directory price cells stayed "reading…" forever on a failed read (empty catch) → "unavailable — the node did not answer". Needs-view stake tie reading as YES left as taste (second confirmation, still declined). Smoke: node --check clean, served 200. | NOT-CLEAN (2 applied) |
| 26w | 2026-08-17 | (hold) Sweep #14 out (qrender-name edge cases + residual prototype hazards + free lens). Duplicate-12 delivered (32 tool uses, pre-round-25 snapshot): independently re-derived BOTH already-fixed findings — Go-unquote-before-unesc (= round 25 fix verbatim) and prefer-Render-name-over-CourtName (= round 26 fix verbatim) — plus re-verified the round-24 trio correct. Convergent confirmation, nothing new. Fires: 7, pinged 14. | WAIT |
| 26 | 2026-08-17 | Sweep #13 (16 tool uses; the round-25 quote-layer fix VERIFIED PASS with a four-read end-to-end trace — InlineText's escape set byte-identical to unesc's class, titles single-line by fold, addresses/status/enum strings unaffected, tuples string-free, Go-only escapes fall back to slice = prior behavior) | Two findings APPLIED, one optional declined: V1 MED CourtName purge-bypass (third confirmation) now gets the overlay-side mitigation the critic proposed — courtStats live name comes from the court Render's own "# heading" (courtNameFor: purge-gated, tombstoned, sanitized → single unesc), the raw CourtName qeval dropped; realm-side gating stays in Owner notes; V2 LOW prototype-key slugs (#/c/constructor) resolved through Object.prototype and defeated every demo lookup (throw → fake node-error screen) → demoCourt() helper with Object.hasOwn at all six bare-slug sites incl. balances (unit-tested: constructor→null, orem→hit); DECLINED (taste/perf): dropping claimDetail's display-unused ClaimStatus qeval — one read per claim page vs late churn in a load-bearing Promise.all. Duplicate-12 still out (will fold or stop). Smoke: node --check clean, hasOwn unit test green, served 200. | NOT-CLEAN (2 applied) |
| 25w | 2026-08-17 | (hold) Sweep #13 out (JSON.parse-unquote blast-radius across string reads + final free lens). Duplicate-12 also still out. Fires: 7, pinged 13. | WAIT |
| 25 | 2026-08-17 | Sweep #12 delivered on fire 8 (26 tool uses; verified the round-24 changes' ordering/scope correct as written — but see finding; 21 tx names, ClaimCount semantics, sentinel, FlagState re-CONFIRMED) | U1 HIGH: my round-24 .then(unesc) decoded the WRONG LAYER — qeval strings arrive strconv.Quote'd (gnovm WriteQuote), so sanitizer backslashes are DOUBLED on the wire; parseTyped only sliced the quotes, so unesc consumed the quote-layer doubling and stopped ("Version 2\.0 \[beta\]" still rendered escaped, " undecoded) → parseTyped's string branch now Go-unquotes via JSON.parse (slice fallback), THEN unesc strips the sanitizer layer — unit-tested end-to-end on the critic's exact example ("Version 2.0 [beta] \"quoted\"" ✓, plain "voting" ✓). U2 MED CourtName purge-bypass re-confirmed with the full render-site list — already in Owner notes (realm-side), no page action. OPT: demo #/c/unknown/5 now says "No court by that slug" rather than blaming the claim id. Equal-stakes-tie-counts-as-YES left as taste. Duplicate-12 launched pre-delivery still out; will fold in or stop. Smoke: node --check clean, layering unit test green, served 200. | NOT-CLEAN (2 applied) |
| 24w | 2026-08-17 | (hold) Sweep #12 out (unesc/catch ordering re-verify + free lens). Duplicate-11's CLEAN noted (round-23 state). Fires: 8; sweep-12 original silent past ping, DUPLICATE launched. | WAIT |
| 24 | 2026-08-17 | Sweep #11 delivered on fire 10 (22 tool uses; lens: qeval↔Render data-path parity; all four round-23 changes re-verified — store helper total, bounds check exact vs nextID, CloseDeadClaim guards match the sub, single setNav; 21 tx funcs, FlagState, VoteQuality-during-dispute, XSS surfaces, README claims all CONFIRMED; duplicate sweep still out, will be stopped) | Three findings + one optional: T1 HIGH live qeval titles arrive SANITIZER-ESCAPED (claim.gno:370 routes ClaimTitle through sanitize.InlineText; EscapeInline backslash-escapes the markdown set) and only the Render-scrape path unesc()d — a live claim H1 showed "2025\." while the docket showed it clean, breaking "titles render verbatim" → .then(unesc) on both qeval title sites (claimDetail + positions; needs inherits via claimDetail; unesc regex verified to cover EscapeInline's full set); T2 MED is REALM-side (CourtName raw/ungated defeats the purge tombstone with one qeval — the exact hazard ClaimTitle was gated against) → logged in Owner notes, realm file untouched (other session owns it); T3 LOW live #/c/typo hit the node-error screen (courtStats throws on mustCourt panic; the notFound branch was demo-only dead code) → try/catch with the honest message, same pattern as the claim route; OPT trailing ratio now requires BOTH series mature (trailYesMature && trailOImature), mirroring render.gno:182. Smoke: node --check clean, served 200. | NOT-CLEAN (3 applied + owner note) |
| 23w | 2026-08-17 | (hold) Sweep #11 out (four-change re-verify + free-lens pass). Fires: 8; original silent past ping, DUPLICATE launched (late original still counts). Fires: 10. | WAIT |
| 23 | 2026-08-17 | Sweep #10 (13 tool uses; all six round-22 changes re-verified sound; docket regexes vs every realm status string, ratio math, nextID scan, renderSeq, about-copy vs CloseDeadClaim — all CONFIRMED) | Two findings + two optionals, all APPLIED: S1 MED unguarded localStorage killed first paint with a FALSE node error in storage-blocked contexts (introStrip getItem bare inside the / route; saveCfg and the intro dismiss unguarded — while boot's own guard proves storage is treated as fallible) → store.get/set helper wraps every access (localStorage now appears only inside the helper); S2 LOW live #/c/slug/999 painted "Could not read the node… check it is reachable" though the node answered — live claim route now bounds-checks ClaimCount first (bad id → notFound like demo; unreadable court → its own message); OPT the closed lifecycle gained its permissionless closer (CloseDeadClaim button, sub "if its timeout passed", + one copy line) so the state the overlay renders is reachable from the overlay; OPT duplicate setNav("/about") no-op removed. Smoke: node --check clean, localStorage grep = helper-only (3), served 200. | NOT-CLEAN (4 applied) |
| 22w | 2026-08-17 | (hold) Sweep #10 out (six-change re-verify + fresh pass on boot/hash-edges/localStorage/long copy). Fires: 6, pinged. | WAIT |
| 22 | 2026-08-17 | Sweep #9 (13 tool uses; closed-phase realm reads re-verified — ClaimClosed exists, Unstake unblocked on closed, CloseDeadClaim dispositions match copy; 18 tx names; parsers vs realm markdown; settled withdraw pair coherent; CLI guard comment survives esc/copy — all CONFIRMED) | Three findings + two optionals APPLIED: K1 MED the new closed phase missed two branches — verdictBanner fell through to "open · no answer yet" and the signal header said "frozen at the answer" beside a ticket saying never-frozen → closed pill ("closed · never answered") + "never frozen — claim died unanswered" header; K2 positions sub promised "what's withdrawable" after the column was removed → copy now points to each claim's own page; K3 README demo-state list gained closed-never-answered; OPT the NO-side withdraw now carries the no color (was unstyled beside a yes-colored YES) and a no-op .replace removed. One realm-side note logged for owners, not the page: render.gno:388's closed string says "every stake exited 1×" past-tense though stakes sit until Unstake. Smoke: node --check clean, served 200. | NOT-CLEAN (5 applied) |
| 21w | 2026-08-17 | (hold) Sweep #9 out (closed-phase trace, withdraw pair, CLI guard comment, fresh general pass). Fires: 6, pinged. | WAIT |
| 21 | 2026-08-17 | Sweep #8 (three-journey open brief, 11 tool uses; parsers, ClaimStatus regexes, FlagState strings, deadline boundaries, VoteDispute choices, ratio math, id-scan, 20 tx args, raw-view fidelity — all CONFIRMED) | Three findings + one optional, all VERIFIED and APPLIED: J1 HIGH never-answered-CLOSED claims rendered as fully OPEN (claimDetail never read ClaimClosed; claim.gno:377 exports it, CloseDeadClaim leaves frozenAt=0) — docket said "never answered" but the claim page offered Stake/Answer buttons that all panic (stake.gno: "this claim is closed") → closed phase end-to-end: live read (+catch false), demo synthesis, statusText, unstake-only ticket ("stakes were never frozen — unstake freely" — verified CloseDeadClaim refunds deposit/burns fee but does NOT touch stakes), resolution notice, quality lane silent, demo orem/8 added; J2 HIGH the settled page's only withdraw button hardcoded the WINNING side under copy promising both sides 1× (losing-side staker signs a guaranteed panic) → YES-side/NO-side pair like provClose; J3 MED positions rows offered phase-blind "withdraw ↗" (panics pre-verdict — 3 of 4 demo positions are such states; conviction-only rows panic even after) → column dropped, the claim page (one click via title) carries the correctly-gated actions; OPT demo CLI commands now open with "# demo data — these entities exist on no chain" so a blind paste cannot broadcast at nothing. Early-return wrapper verified identical to the fall-through shape. Smoke: node --check clean, served 200. | NOT-CLEAN (4 applied) |
| 20w | 2026-08-17 | (hold) Sweep #8 out (open brief: three journeys — first-timer, returning staker, raw-view developer). Fires: 6, pinged. | WAIT |
| 20 | 2026-08-17 | Sweep #7 (20 tool uses; inChunks/counters, all new copy vs realm incl. finalizeGraceBlocks=WEEK and priority arming, staker-can-OpenDispute CONFIRMED (dispute.gno:80 bars only the answerer; credEligible gates the record not the opener), docket parser overflow lines, DrawSlices order, button boundaries vs realm >= guards — all CONFIRMED) | ONE finding, VERIFIED and APPLIED: my round-19 claimIdsFor dropped the NEWEST claim in every court — claim.gno:260 increments nextID BEFORE assigning, so ids run 1..ClaimCount inclusive, and the newest claim (likeliest to carry a fresh answer needing a dispute) vanished from positions/needs with no failure counted; a one-claim court scanned as empty → i<n → i<=n, comment corrected (verified against claim.gno:260-261; empty court = ClaimCount 0 → [] still right). Smoke: node --check clean, served 200. | NOT-CLEAN (1 applied) |
| 19w | 2026-08-17 | (hold) Sweep #7 out (claimIdsFor/inChunks re-execution, new-copy coherence, can-a-staker-OpenDispute realm check). Fires: 6, pinged. | WAIT |
| 19 | 2026-08-17 | Sweep #6 delivered on fire 8 (18 tool uses; section gate, quality-lane phases, failed-rounds label, courtStats reads, WEEK/settleDelay constants, StartCourt-is-free, slug rules, Adena int coercion — all CONFIRMED; duplicate sweep stopped as redundant, its partial concurred on the section gate) | Four findings, all VERIFIED and APPLIED: M1 "it can be finalized" was false for a week — Finalize is participant-only until escrowUntil+finalizeGraceBlocks (dispute.gno:440, the A13 rule) → copy "participants may finalize now; anyone may after a week" + button sub; M2 the vote buttons target the one audience the realm always rejects — VoteDispute/VoteQuality refuse the claim's author, answerer, and ANY staker (dispute.gno:550), yet the needs view routes stakers to disputed claims whose only actions were votes → electorate exclusion stated on the dispute banner, the quality lane, and the needs page-sub ("you cannot vote on a claim you've staked — but you can open the dispute"); M3 positions/needs silently missed stakes beyond the docket's 50-newest cap and moderation-hidden claims (render.gno:127/137) → both views now scan ids 1..ClaimCount-1 (chunked positionOf probes; titles/details fetched only for hits — actually FEWER live queries for needs than the docket walk); M4 the priority-day copy was unconditional but the gate arms only at qualifiedCount>=3 (records.gno:41) → "seasoned courts reserve the first day; a young court opens to anyone at once" (three spots). Smoke: node --check clean, served 200. | NOT-CLEAN (4 applied) |
| 18w | 2026-08-17 | (hold) Sweep #6 out (section-gate re-execution, courtStats reads, StartCourt/Buy vs realm validation, Adena int serialization). Fires: 8; original silent past two pings, DUPLICATE sweep launched (late original still counts). | WAIT |
| 18 | 2026-08-17 | Sweep #5 (13 tool uses; listCourts KOURT:SYMBOL tolerance, all 20+ tx functions, VoteQuality/ResolveSlashWindow through btn/CLI/Adena, demo gate partitions vs realm heights, SettleUndisputed round>0 unreachability — all CONFIRMED) | Three findings, all VERIFIED and APPLIED: P1 HIGH listClaims swallowed the court page's "## Needs review"/"## Awaiting an answer" strips (modrender.gno:199/223 emit the same link shape) — live docket showed answered-unsettled claims TWICE (dupe mislabeled "open") and the dupes propagated into positions/needs → parser is now section-gated to "## Claims" with id-dedup (unit-tested against a stripped court page; demo listClaims maps the dataset directly and never used this path); P2 HIGH the quality lane lied during disputes: OpenFlag rendered mid-dispute but always panics (quality.gno:87), the counter-window trio all panic while disputeOpen, and VoteQuality — live during disputes (quality.gno:178) and during counter re-votes — was never offered → new phase-aware lane: disputed shows "quality rides this dispute" + Vote low/mid/high; flag-vote and counter-window branches also carry the vote trio; P3 the dispute banner called FailedRounds the round number ("round 1" forever after resets, dispute.gno:293) → now "(after N failed rounds)" which is what the read means. Smoke: node --check clean, section gate unit-tested, served 200. | NOT-CLEAN (3 applied) |
| 17w | 2026-08-17 | (hold) Sweep #5 out (court/claim-page Render parsers vs render.gno, new-button existence, gate sanity on demo claims, quality-lane-during-disputes). Fires: 6, pinged. | WAIT |
| 17 | 2026-08-17 | Sweep #4 (14 tool uses; arg names AND types vs realm all CONFIRMED incl. VoteDispute yes=overturn, WithdrawBonus, DrawSlices order; how-it-works/about vs realm mechanics CONFIRMED; demo draw split 10280/1028/642 matches the 80/8/5 exactly) | Five findings, all VERIFIED against realm source and APPLIED: Q1 HIGH the live directory parser could NEVER match render.gno's actual court line ("[Name](…:slug) · SYMBOL — N claims" — the · SYMBOL infix defeated \s*—) so live mode listed zero courts and positions/needs iterated an empty list; invisible offline because demoRender emitted the dash-only dialect → regex accepts the optional infix (unit-tested against both formats in node) AND demoRender now speaks the realm's true format; Q2 the announced quality flag vote had no vote button (VoteQuality quality.gno:168, buckets 0/1/2) → Vote low/mid/high added; Q3 "Resolve counter" was wired to ResolveCounter which PANICS unless a counter re-vote ran (quality.gno:794) — the unchallenged window's closer ResolveSlashWindow was never exposed, and FlagState cannot distinguish the sub-states → both closers with explanatory subs; Q4 buttons contradicted adjacent copy: "Dispute this answer" rendered after its own "window has passed" line (OpenDispute panics past settleDelay) and "Reopen" after "window has closed" → gated on nowH vs settleAt/escrowUntil (SettleUndisputed session.gno:14 and Finalize dispute.gno:420 shown on the correct side of each window; both shown when height unknown); Q5 dead claims ("closed — never answered", render.gno:368) badged as "answered" via substring → ^closed catch-all with "never answered" label. Smoke: node --check clean, parser tested on both dialects, served 200. | NOT-CLEAN (5 applied) |
| 16w | 2026-08-17 | (hold) Sweep #4 out (arg types vs realm, VoteDispute choice values, how-it-works vs realm files, demo arithmetic). Fires: 7, pinged. | WAIT |
| 16 | 2026-08-17 | Sweep #3 (arg-name contract audited against realm source; all traces, cliCmd quoting, wallet gating, 37 qeval reads, README query surface CONFIRMED) | Five findings, all APPLIED: H1 PostAnswer passed side= but the realm parameter is verdict — gnoweb $help prefills by NAME so both Answer buttons opened with the verdict field empty (answer.gno:19) → renamed (CLI/Adena were positional and unaffected; the Adena prompt label improves too); M2 the lone Unstake button hard-coded side:0 (silently wrong for NO stakers) and provClose Withdraw likewise → Unstake YES/NO pair and Withdraw YES-side/NO-side pair; L3 dead body remnants (.foreign CSS "the claim, verbatim", unused safeBlock, comment mentioning bodies) removed; L4 README demo-state list gained provisional-in-escrow + counter-flag-window; L5 "Back the side you believe" collided with the no-"backing" word-family rule → "Take the side". Also smoothed a double-parenthetical my round-10w README edit left. Smoke: node --check clean, served 200. | NOT-CLEAN (5 applied) |
| 15w | 2026-08-17 | (hold) Sweep #3 out (state traces, README diff, per-action cliCmd audit). Fires: 5, pinged. | WAIT |
| 15 | 2026-08-17 | Fresh full-sweep #2 (all 8 demo claims traced through pill/banner/ticket — CONFIRMED agreeing; provisional side-int demo/live-consistent; no view crashes; dead-ref sweep clean) | Five findings; four APPLIED as reported, one INVERTED by realm evidence: F2 claimed live mode "never shows the claim body" — but claim.gno:284 says "No user text beyond the title, which is the claim's whole public content" — the DEMO had invented body/author fields the product cannot carry (and the site's own "one fixed sentence" copy was right all along) → body render block removed, all invented body/author fields stripped (1.4KB), demo now faithful to title-only claims. F1 the needs view's flagship ⚑ against-your-side state was unreachable offline (no opposing position in DEMO.positions) → NO-heavy position added on orem/6 (answered YES). F3 "Disputing now costs X" was dead in both modes (live fetched DisputeBondNext only for disputed/provisional; demo answered claims lacked the field) → live fetches whenever answered && !settled, demo orem/2+orem/6 carry it. F4 counter-flag window unreachable offline → orem/7 carries counterOpen (orthogonal quality lane). F5 provClose/settled claims showed "stakes are locked until the verdict is final" beside a live Withdraw button → new concluded ticket ("every stake withdraws 1×", open the rewards note on settled, refund note on provClose). Smoke: node --check clean, served 200. | NOT-CLEAN (5 applied) |
| 14w | 2026-08-17 | (hold) Fresh full-sweep critic out (per-demo-claim execution trace, flag lane, dead refs). Fires: 5. | WAIT |
| 14 | 2026-08-17 | Round-13 verifier returned + whole-file sweep (all six round-13 fixes CONFIRMED holding, race traces exhaustively re-run, sanitizer round-trips verified) | Five sweep findings, all VERIFIED (the pill bug reproduced by executing the code) and APPLIED: N1 HIGH docket pill labeled answered/provisional claims "in dispute" — /disputed|dispute/ matched "undisputed"/"disputable"/"a new dispute" inside sibling status texts → most-specific-first order (provClose, settled, provisional, answered, disput); N2 /needs was permanently empty in demo — the filter tests answered/settled/provClose booleans the demo claimDetail never synthesized → demo branch now derives them from phase, same shape as live; N3 the notFound path never set paintedSeq so a slow stale view could end on screen at a bad hash → paintedSeq=seq after painting; N4 .notice.void had no CSS rule (provClose notice untinted) → void-wash tint added; N5 no demo claim exercised the provisional state, contra the dataset's own "every screen and state" promise → orem/7 added (failed dispute round, verdict in escrow, reopenable; flows through demo listClaims' statusText automatically). Smoke: node --check clean, served 200. | NOT-CLEAN (5 applied) |
| 13w | 2026-08-17 | (hold) Round-13 verifier + whole-file sweep out. Fires: 5. | WAIT |
| 13 | 2026-08-17 | Fix-verification pass on the rounds-11/12 batch (chainHeight/wall callers, cleanCfg regexes vs all real endpoint values, mefail declaration, shortAddr sinks, demo withdraw span, copy splices — all CONFIRMED correct) | Six findings, all APPLIED: V1 my needsList warning was dead code (insertAdjacentHTML of an empty string) so "N reads failed" never showed when items rendered — warn now prepends the docket; V2 the race-guard could ping-pong (mutual respawn between two in-flight renders) → paintedSeq: a stale render redraws ONCE and only if the newer render had already painted (else the in-flight one paints over us); V3 the /me route was fire-and-forget (block-body arrow) so render() never awaited it, defeating the seq guard and heading focus — now an expression arrow like /needs; V4 cleanCfg dropped CFG.theme at load (theme reverted to OS preference every reload) → theme whitelisted; V5 the copy fallback claimed "Selected" without selecting when writeText rejected (permissions/file://) → selectPre() runs on every failure path; V6 sanitizer reverts were invisible (input kept the rejected text while requests went to the default) → rpc/gnoweb/chainid inputs resync to the effective value on change; naddr gains the Enter handler addr had. Smoke: node --check clean, served 200. | NOT-CLEAN (6 applied) |
| 12w | 2026-08-17 | (hold) Fix-verification critic out on the rounds-11/12 batch. Fires: 5. | WAIT |
| 12 | 2026-08-17 | Hostile skeptic (chain-string escaping CONFIRMED sound: esc/safeInline consistent, tx args URI-encoded; every serious issue traced to CFG trusted verbatim) | Six findings: S1 XSS via CFG.gnoweb into unescaped href APPLIED (cleanCfg shape-validates rpc/gnoweb/chainid/addr at load AND save — no quotes/spaces/metacharacters survive — plus esc() on both tx() hrefs); S3 shell injection via poisoned rpc/chainid in the copied gnokey command APPLIED (same sanitizer: charset excludes ";$\`" entirely); S6 shortAddr was the one esc gap APPLIED (escapes at source; bech32-valid addresses unchanged); S5 me/needs swallowed per-read failures and asserted "nothing needs you" APPLIED (failure counters → "Could not read everything — no conclusions" + red incomplete-view warning); S4 = cold-read G1 (same NOW bug, fixed in round 11); S2 poisoned-gnoweb phishing PARTIALLY ACCEPTED as residual: sanitizer forces a plain host, Adena registration gates unknown chainids, rail shows the active host — a user-configured look-alike host remains their own config (documented in README next round if critics agree). Sanitizer self-review: trailing-slash inputs were silently reset to defaults → trim/strip BEFORE the shape test. Smoke: node --check clean, served 200. | NOT-CLEAN (5 applied) |
| 11 | 2026-08-17 | Regression cold-read (adenaSign control flow, CLI toggle scoping, errorView focus path all CONFIRMED sound) | Six findings, all APPLIED: G1 demo constant NOW fed live-mode deadline math and even demo grammar was broken ("settles undisputed 2 hours ago if nobody disputes") → chainHeight() fetches /status in live mode (null on failure = say nothing rather than lie), claim view passes real height, past-deadline copy now truthful ("Its 72-hour window has passed — the next touch settles it"), missing "in" added; G2 live-no-wallet #/me//#needs queried the fake DEMO_ME against the real chain and asserted its emptiness → null-address explainer states, query only on explicit input; G3 positions-table withdraw link bypassed the demo-inert guard → demo renders an inert span; G4 clipboard-less origins (LAN http) threw TypeError on copy → select-text fallback + "press Ctrl/⌘-C" hint; G5 stale-render races → renderSeq token redraws the current view if a slow stale view overwrote it, tx re-render 4s→7s (block ~5s); G6 intro strip bolded 3 of "four things" → verdicts bolded; primer no longer calls unwindable staking "final". One SELF-INFLICTED syntax error en route (global replace put await in sync resolutionSection) — caught by node --check, hoisted to the async claim view. | NOT-CLEAN (6 applied) |
| 10w | 2026-08-17 | (hold) Hostile skeptic + regression cold-read out. Drafted HANDOFF-WEBSITE.md (status IN PROGRESS, owner-veto list: accent token change, hardened copy, wallet posture) so convergence is a status flip. Fire 2: README wallet section now says Sign is live-mode-only (it predated the round-8 demo gating). Fires: 3. | WAIT |
| 10 | 2026-08-17 | Copy-vs-whitepaper/code consistency review (19 tool uses; verified reward path, unstake-freely, 72h settle, bond doubling, sealed tallies, senior-queue sub, §7.4 posture — all CONFIRMED; every invoked function/read exists) | Four findings, all VERIFIED against realm code and APPLIED: C1 about-page "small refundable deposit" hid the fee and the two burn paths (quality.gno:431-441 burns deposit+fee on conclusive-low; claim.gno CloseDeadClaim burns the fee on a dead claim) → now "deposit plus a fee… a claim voted junk burns both, one that dies unanswered burns the fee (the spam price)". C2 "anyone may answer/dispute" — answer.gno:66-71 reserves the first ~24h to qualified answerers; dispute.gno:80-83 bars self-dispute → primer + notice + how-it-works reworded (three spots). C3 flag copy omitted the half-burn (quality.gno:13 inconclusive-mid) → "a vote that fails to settle can burn half" (two spots). C4 frozen-stake ticket said locked-until-final but session.gno:97-103 releases the provisional-losing side 1× early → ticket now says so. PLUS two self-caught round-9 regressions REPAIRED: the Copy button wore the clitog class so the delegated toggle handler stole its click (clicking Copy closed the block), and closing a toggle orphaned the button — own class + shared CSS + paired removal. Smoke: node --check clean, served 200. | NOT-CLEAN (6 applied) |
| 9 | 2026-08-17 | A11y/mobile review (grid collapse at 375px, keyboard reach, prompt()-flow all CONFIRMED sound) | Four findings APPLIED + one self-finding: F1 whole-page aria-live on <main> re-announced every render and drowned tx feedback, while wallet errors were silent → removed; .txnote and #wallethint are role="status"; navigation now moves focus to the new .page-h (tabindex -1, outline suppressed). F2 primary-button contrast: dark theme white-on-amber ~2.5:1 → new --accent-ink per theme (dark ink on amber ~6.8:1); light accent nudged #9c6f2a→#96692a (~4.8:1 on white). F3 --muted sub-AA in both themes at 10.5-12.5px → #6b675c light (~4.9:1) / #8f8b80 dark (~5.5:1) in all four var blocks. F4 copy-pre was pointer-only → real Copy button (keyboard-real, self-announcing "Copied ✓"), pre click kept. S1 (self): #/me and #/needs wore a REAL connected address over fabricated demo positions → address is live-gated ((isLive()&&CFG.addr)||DEMO_ME), demo always shows the sample holder, matching its own note. Smoke: node --check clean, served 200. | NOT-CLEAN (5 applied) |
| 8 | 2026-08-17 | Adena-correctness review (verified vs docs.adena.app llms-full corpus + dispute.gno arg orders; DoContract shape, envelope codes, arg ordering, escaping all CONFIRMED correct) | Four findings, all APPLIED: A1 HIGH — Sign was offered in demo mode (real gas-costing broadcasts at fabricated entities) → gated on CFG.addr && mode===live; A2 double-click double-send → in-flight disable with finally re-enable; A3 GetNetwork non-zero silently bypassed the chain guard → hard refusal with unlock hint; A4 mute cancels now say "Cancelled — nothing was signed", and Buy rejects zero amounts. Smoke: node --check clean, served 200. | NOT-CLEAN (4 applied) |
| 7 | 2026-08-17 | round-6 deferrals applied: #/me notes the sample address in demo mode; directory subhead glosses conviction at first use (stake × time held) and says "staked coin" rather than bare principal; senior-queue sub now plain ("earned payouts... paid as budget accrues"; carrots→voter rewards for visitors). Smoke: node --check clean. Adena critic still out. | NOT-CLEAN (3 applied) |
| 6 | 2026-08-17 | first-visitor walkthrough (Adena critic still out) | Five findings, all verified against the file and APPLIED: W1 (its verdict, and right) — demo mode's action buttons linked the REAL gno.land with placeholder args (slug=my-court etc.); actions are now inert buttons in demo mode with an explanatory note, links only on live. W3 strip said three things, delivered four → "Four things". W4 CC expanded at first use (court coin); "opening the rewards" glossed where it gates money ("settling and paying out"). W5 the no-loss line overstated safety → primer now says staked coin returns 1× while naming the burn ("that part was spent, not staked") — §7.4-tighter, matches the whitepaper. Deferred to next rounds: sample-address marker on #/me in demo, conviction gloss at directory first-use, senior-queue sub wording. Smoke: node --check clean. | NOT-CLEAN (5 applied) |
| 5w | 2026-08-17 | (hold) | Critic panel 1 out (Adena-correctness, first-visitor walkthrough). Fires: 3. | WAIT |
| 5 | 2026-08-17 | F2 part 2 — Adena signing: ✍ Sign on every action when connected; every argument prompt-confirmed before signing (placeholder titles must never be signed as-is); Buy prompts ugnot amount with burn language, digit-validated; GetNetwork/SwitchNetwork guard against CFG.chainid; envelope handling 0/4000/3001 with inline txnote (success shows tx hash prefix, re-renders after 4s); connect/disconnect re-render so Sign buttons appear/vanish; README gains the wallet section incl. the file:// serving requirement. Core feature set (F1-F5, F8) now COMPLETE. Smoke: node --check clean, served 200. Critic panels begin next round. | NOT-CLEAN (1 feature) |
| 4 | 2026-08-17 | F2 part 1 — Adena connect: rail button (probe on click, never at load), AddEstablish("Kourt") accepting 0 and 4001, GetAccount → CFG.addr persisted, connected-address label with one-click disconnect; me/needs routes prefer CFG.addr over the demo address and their copy explains both paths; graceful absence handling with the file:// explanation (extensions do not inject there) and adena.app install pointer. All per the verified memory shape. Smoke: node --check clean, served 200. Next: DoContract signing per action + network guard. | NOT-CLEAN (1 feature) |
| 3 | 2026-08-17 | F4 CLI builder shipped: every action button now carries a CLI toggle revealing the equivalent gnokey command in a copy-on-click block — one --args per argument, --send only on Buy (AMOUNTugnot placeholder), --gas-fee 10000ugnot/--gas-wanted 10000000 (the documented floor pair; --gas-fee is taken in full so nothing larger is suggested, per the gnokey reference), --chainid/--remote from config, YOURKEY placeholder. CFG gains chainid (presets fill dev/sapphire-1; input added). Flags verified against the gno skill gnokey reference (double-dash forms, one-args-per-arg, fee-floor trap). Smoke: node --check clean; served on :8399 → 200. | NOT-CLEAN (1 feature) |
| 2 | 2026-08-17 | F1+F8+F5+F3 applied: all five CryptoCourt/courtv2 rename misses fixed (grep now 0 in web/); README path typo fixed; preset selector added (Custom / Local gnodev 26657+8888 / Sapphire rpc.sapphire.testnets.gno.land:443 + sapphire.testnets.gno.land) wired into initControls with persist+rerender; first-visit intro strip on the directory (dismissible via localStorage cc.intro, court/coin-is-voice/claim explained pre-jargon, principal-returns-1× and verdicts-are-conclusions kept §7.4-true, links to How-it-works). Smoke: embedded script extracted (46.7k chars), node --check clean; rename grep 0. | NOT-CLEAN (4 features) |
| 1 | 2026-08-17 | Recon: git state, full read (README + structure map of index.html), Adena memory + web verification, gnoweb $help confirmation. Findings F1-F8, plan drafted. No web/ edits yet. | NOT-CLEAN (recon) |

## Owner notes

**Realm-side wishlist + erratum from D6 (moderation surfaces), 2026-08-17:**
- `HiddenBits(slug, id) → (court, meta, global, purged bool)` or equivalent per-authority read — HiddenFromListing is the union, so the overlay's banner must say "De-listed by moderation" where the chain's own render distinguishes "by this court's moderators" from "by the review court"; the mod-log link compensates, a per-bit read would close it.
- REALM ERRATUM (from the D6 critic, code-verified): modrender.gno:310 renders "**suspended** — their hides are masked and they cannot act" and MODERATION.md:246 says "bits masked at render (kept)", but HiddenFromListing (moderation.gno:869) applies NO suspension mask and no render path masks — on a suspended court the chain's own log claims masking that does not happen. Either mask on read or fix the log copy.

**Realm-side wishlist from D5 (disclosure + clocks), 2026-08-17:**
- `CountByTier(tier int) int` — a count-only read so listing surfaces can disclose "K courts are hidden" without transporting hidden slugs (the overlay counts ListByTier(0) today and renders only the length; the slugs still cross the wire).
- Per-address answering HISTORY (posted / upheld / overturned counters, or `AnsweredBy(slug, addr) → []uint64`) — `AnswerRecord` exposes only the living credential, reset to zero on overturn, so no honest history can be read today; the /me surface shows the credential and refuses to fake the rest.
- `ClaimDeadline(slug, id) → (phase, height)` — one uniform read would give every docket row its operative clock (including open-claim dead-timeouts, which no current read exposes) vs today's phase-gated SettleDeadline/EscrowUntil pair.


**Realm-side wishlist from B3 (court identity), 2026-08-17 — realm files belong to the other session; nothing here is required for the site to work, each unlocks an honest richness the overlay currently hides or bounds:**
- `CourtCreatedAt(slug)` + `CourtAdmin(slug)` reads (both fields are stored, court.gno:382/327, no accessor) — would enable a "founded / founder" seal on the court page and age in the directory.
- `CourtCensus(slug)` one-qeval aggregate (five terminal-state counts + failed rounds + overturned, whole history) — the overlay currently spends ≤5K+1 qevals for the last-K-claims approximation.
- `VerdictAt(slug,id)` (or any settle-height read) — unlocks median-time-to-settle, which is otherwise not derivable and therefore not shown.
- `CourtScope` (≤140 chars, set at StartCourt, gated like ClaimTitle) + read — would let LIVE courts show the scope line the demo carries as labeled overlay copy.

**Realm-side wishlist from D1 (live folders), 2026-08-17:** `FolderInfo(slug,fid) (name, desc string, purged bool)` — exposes the stored-but-unreadable folder desc and halves per-folder reads; document folder-id contiguity (1..FolderCount, no delete op) as an invariant the overlay now leans on, or add `FolderIDs(slug)`.

**Realm-side wishlist from C2 (curation), 2026-08-17:** `SetCourtCharter(slug, md)` (creator/mod, purgeable like folder text); `MoveInFolder(folderID, claimID, pos)` + parent-folder nesting; claim body or content-hash anchor + append-only author notes; `SetParent`/`AddArgumentEdge(from,to,stance)` per COURTS_STRUCTURE §5 (render-only); mod-gated court rename.

**Realm-side wishlist from B4 (dispute/vote), 2026-08-17:**
- `VoteCloseAt(slug,id)` — the running round's close height; the overlay currently states honestly that no close height is readable.
- `RoundOrdinal(slug,id)` — a lifetime round number that survives resets (FailedRounds resets on decided rounds).
- `FinalTally(slug,id)` — frozen yes/no/abstain of the last closed round, exposed POST-close only — would enable the reference's "by vote, 71% against" margin on settled claims (§7.4-legal: only live tallies are banned).
- `DisputeThresholdBps()` — the 5001 constant; the overlay hardcodes ">50%… stated here, not read".
- `QuorumFloorAt(slug,id)` — the running round's own bar as snapshotted at open (QuorumFloorOf recomputes at now, so the overlay labels it "the bar a new dispute faces").

- WHITEPAPER ERRATUM (from sweep #23-dup, code-verified): the hedging analysis
  (§2 area, "a hedger gives up voting weight on both halves" / staked coins
  "carry no vote") presumes a staker's UNSTAKED coin still votes that claim —
  but the realm excludes the participant's ADDRESS wholesale from that claim's
  dispute and quality votes (isParticipant, dispute.gno:548-555). Per the
  whitepaper's own footer, code governs — the whitepaper paragraph needs the
  correction. The page already states it correctly.

- Manual browser pass (loop cannot): (1) does Adena's DoContract require explicit
  gasFee/gasWanted fields? The page omits them; if the extension rejects, add
  both to the message value. (2) Confirm the ≤820px stack renders as intended
  after the round-31 grid-row fix.

- (from sweep #11, realm-side not page-side): CourtName (court.gno:528) returns
  the raw stored name ungated — one qeval defeats the purge tombstone that
  courtNameFor/modrender carefully renders as "[removed · tombstone]". ClaimTitle
  was gated for exactly this reason (claim.gno:367-369). Realm fix: gate
  CourtName the same way. The overlay uses CourtName for the court H1, so a
  purged court's name would still render there until the realm gates it.
- (from sweep #9, realm-side not page-side): render.gno:388's ClaimStatus for a
  dead claim reads "every stake exited 1×" — past tense, though stakes sit until
  each staker Unstakes. One-word realm fix ("exits") if ever touched.
