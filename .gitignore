# scripts/ became importable in v0.70 (gnoroot.py is shared by every runner),
# so running any of them now leaves bytecode beside the sources. Never source.
__pycache__/
*.selftest-backup

# built by `make chat`: the off-chain chat service binaries
bin/
